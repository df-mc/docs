---
title: "block"
description: "Package block exports implementations of the Block interface found in the server/world package."
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';
import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';

# block


```go
import "github.com/df-mc/dragonfly/server/block"
```



Package block exports implementations of the Block interface found in the server/world package. The blocks
implemented in this package are automatically registered in the server/world package using the world.RegisterBlock
and world.RegisterItem functions through an init function, so that methods in server/world that return blocks and
items are able to return them by the respective types implemented here.







## Functions

<Accordions>

<Accordion title="BreakDuration">
```go
func BreakDuration(b world.Block, i item.Stack) time.Duration
```


BreakDuration returns the base duration that breaking the block passed takes when being broken using the
item passed.

</Accordion>

<Accordion title="BreaksInstantly">
```go
func BreaksInstantly(b world.Block, i item.Stack) bool
```


BreaksInstantly checks if the block passed can be broken instantly using the item stack passed to break
it.

</Accordion>

<Accordion title="NextHash">
```go
func NextHash() uint64
```


NextHash returns the next free hash for custom blocks.

</Accordion>

<Accordion title="SlabBlocks">
```go
func SlabBlocks() []world.Block
```


SlabBlocks returns a list of all possible blocks for a slab.

</Accordion>

<Accordion title="StairsBlocks">
```go
func StairsBlocks() []world.Block
```


StairsBlocks returns a list of all possible blocks for stairs.

</Accordion>

<Accordion title="WallBlocks">
```go
func WallBlocks() []world.Block
```


WallBlocks returns a list of all possible blocks for a wall.

</Accordion>

</Accordions>



## Types


### Activatable


<Callout type="info">Interface</Callout>



```go
type Activatable interface {
	// Activate activates the block at a specific block position. The face clicked is passed, as well as the
	// world in which the block was activated and the viewer that activated it.
	// Activate returns a bool indicating if activating the block was used successfully.
	Activate(pos cube.Pos, clickedFace cube.Face, tx *world.Tx, u item.User, ctx *item.UseContext) bool
}
```



Activatable represents a block that may be activated by a viewer of the world. When activated, the block
will execute some specific logic.






---

### Air


<Callout type="info">Struct</Callout>



```go
type Air struct {
	empty
	replaceable
	transparent
}
```



Air is the block present in otherwise empty space.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    replaceable: {
      description: '',
      type: 'replaceable',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="EncodeBlock">
```go
func (Air) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Air) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Air) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Air) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Air) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Air) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (Air) ReplaceableBy(world.Block) bool
```

ReplaceableBy ...

</Accordion>

</Accordions>


---

### Amethyst


<Callout type="info">Struct</Callout>



```go
type Amethyst struct {
	solid
}
```



Amethyst is a decorative block crafted from four amethyst shards.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Amethyst) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Amethyst) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Amethyst) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Amethyst) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Amethyst) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### AncientDebris


<Callout type="info">Struct</Callout>



```go
type AncientDebris struct {
	solid
}
```



AncientDebris is a rare ore found within The Nether.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (AncientDebris) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (AncientDebris) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (AncientDebris) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (AncientDebris) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (AncientDebris) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (AncientDebris) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### Andesite




```go
type Andesite polishable
```



Andesite is a type of igneous rock.





#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Andesite) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Andesite) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Andesite) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Andesite) Hash() (uint64, uint64)
```

</Accordion>

</Accordions>


---

### Anvil


<Callout type="info">Struct</Callout>



```go
type Anvil struct {
	gravityAffected
	transparent

	// Type is the type of anvil.
	Type	AnvilType
	// Facing is the direction that the anvil is facing.
	Facing	cube.Direction
}
```



Anvil is a block that allows players to repair items, rename items, and combine enchantments.



#### Fields

<TypeTable
  type={{
    gravityAffected: {
      description: '',
      type: 'gravityAffected',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    Type: {
      description: 'Type is the type of anvil.',
      type: 'AnvilType',
      typeLink: '../block.mdx#anviltype',
    },
    Facing: {
      description: 'Facing is the direction that the anvil is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Anvil) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="Break">
```go
func (Anvil) Break() world.Block
```

Break breaks the anvil and moves it to the next damage stage. If the anvil is at the last damage stage, it will be
destroyed.

</Accordion>

<Accordion title="BreakInfo">
```go
func (Anvil) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="Damage">
```go
func (Anvil) Damage() (damagePerBlock, maxDamage float64)
```

Damage returns the damage per block fallen of the anvil and the maximum damage the anvil can deal.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Anvil) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Anvil) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Anvil) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Landed">
```go
func (Anvil) Landed(tx *world.Tx, pos cube.Pos)
```

Landed is called when a falling anvil hits the ground, used to, for example, play a sound.

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Anvil) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Anvil) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Anvil) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="Solidifies">
```go
func (Anvil) Solidifies(cube.Pos, *world.Tx) bool
```

Solidifies ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Anvil) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### AnvilType


<Callout type="info">Struct</Callout>



```go
type AnvilType struct {
	anvil
}
```



AnvilType represents a type of anvil, such as undamaged, slightly damaged, or very damaged.



#### Fields

<TypeTable
  type={{
    anvil: {
      description: '',
      type: 'anvil',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="String">
```go
func (AnvilType) String() string
```

String returns the anvil type as a string.

</Accordion>

<Accordion title="Uint8">
```go
func (AnvilType) Uint8() uint8
```

Uint8 returns the anvil type as a uint8.

</Accordion>

</Accordions>


---

### Attachment


<Callout type="info">Struct</Callout>



```go
type Attachment struct {
	hanging	bool
	facing	cube.Direction
	o	cube.Orientation
}
```



Attachment describes the attachment of a block to another block. It is either of the type WallAttachment, which can
only have 90 degree facing values, or StandingAttachment, which has more freedom using a cube.Orientation.





#### Methods

<Accordions>

<Accordion title="FaceUint8">
```go
func (Attachment) FaceUint8() uint8
```

FaceUint8 returns the facing of the Attachment as a uint8.

</Accordion>

<Accordion title="RotateLeft">
```go
func (Attachment) RotateLeft() Attachment
```

RotateLeft rotates the Attachment the left way around by 90 degrees.

</Accordion>

<Accordion title="RotateRight">
```go
func (Attachment) RotateRight() Attachment
```

RotateRight rotates the Attachment the right way around by 90 degrees.

</Accordion>

<Accordion title="Rotation">
```go
func (Attachment) Rotation() cube.Rotation
```

Rotation returns the rotation of the Attachment, based on the orientation if it's a StandingAttachment, or the
facing direction if it is a WallAttachment.

</Accordion>

<Accordion title="Uint8">
```go
func (Attachment) Uint8() uint8
```

Uint8 returns the Attachment as a uint8.

</Accordion>

</Accordions>


---

### Banner


<Callout type="info">Struct</Callout>



```go
type Banner struct {
	empty
	transparent

	// Colour is the colour of the banner.
	Colour	item.Colour
	// Attach is the attachment of the Banner. It is either of the type WallAttachment or StandingAttachment.
	Attach	Attachment
	// Patterns represents the patterns the Banner should show when rendering.
	Patterns	[]BannerPatternLayer
	// Illager returns true if the banner is an illager banner.
	Illager	bool
}
```



Banner is a tall decorative block that can be customized.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    Colour: {
      description: 'Colour is the colour of the banner.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
    Attach: {
      description: 'Attach is the attachment of the Banner. It is either of the type WallAttachment or StandingAttachment.',
      type: 'Attachment',
      typeLink: '../block.mdx#attachment',
    },
    Patterns: {
      description: 'Patterns represents the patterns the Banner should show when rendering.',
      type: '[]BannerPatternLayer',
      typeLink: '../block.mdx#bannerpatternlayer',
    },
    Illager: {
      description: 'Illager returns true if the banner is an illager banner.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Banner) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Banner) DecodeNBT(m map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Banner) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Banner) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Banner) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Banner) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Banner) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Banner) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="MaxCount">
```go
func (Banner) MaxCount() int
```

MaxCount ...

</Accordion>

<Accordion title="Model">
```go
func (Banner) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Banner) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="Pick">
```go
func (Banner) Pick() item.Stack
```

Pick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Banner) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### BannerPatternLayer


<Callout type="info">Struct</Callout>



```go
type BannerPatternLayer struct {
	// Type represents the type of banner pattern.
	Type	BannerPatternType
	// Colour is the colour the pattern should be rendered in.
	Colour	item.Colour
}
```



BannerPatternLayer is a wrapper over BannerPatternType with a colour property.



#### Fields

<TypeTable
  type={{
    Type: {
      description: 'Type represents the type of banner pattern.',
      type: 'BannerPatternType',
      typeLink: '../block.mdx#bannerpatterntype',
    },
    Colour: {
      description: 'Colour is the colour the pattern should be rendered in.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="DecodeNBT">
```go
func (BannerPatternLayer) DecodeNBT(data map[string]any) any
```

DecodeNBT decodes the given NBT map into a BannerPatternLayer and returns it.

</Accordion>

<Accordion title="EncodeNBT">
```go
func (BannerPatternLayer) EncodeNBT() map[string]any
```

EncodeNBT encodes the given BannerPatternLayer into an NBT map.

</Accordion>

</Accordions>


---

### BannerPatternType


<Callout type="info">Struct</Callout>



```go
type BannerPatternType struct {
	bannerPatternType
}
```



BannerPatternType represents a type of banner pattern, used to customize banners.



#### Fields

<TypeTable
  type={{
    bannerPatternType: {
      description: '',
      type: 'bannerPatternType',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Item">
```go
func (BannerPatternType) Item() (item.BannerPatternType, bool)
```

Item returns the equivalent item type for the pattern. If none exists, false is returned.

</Accordion>

<Accordion title="String">
```go
func (BannerPatternType) String() string
```

String returns the bannerPatternType as a string.

</Accordion>

<Accordion title="Uint8">
```go
func (BannerPatternType) Uint8() uint8
```

Uint8 returns the bannerPatternType as a uint8.

</Accordion>

</Accordions>


---

### Barrel


<Callout type="info">Struct</Callout>



```go
type Barrel struct {
	solid
	bass

	// Facing is the direction that the barrel is facing.
	Facing	cube.Face
	// Open is whether the barrel is open or not.
	Open	bool
	// CustomName is the custom name of the barrel. This name is displayed when the barrel is opened, and may
	// include colour codes.
	CustomName	string

	inventory	*inventory.Inventory
	viewerMu	*sync.RWMutex
	viewers		map[ContainerViewer]struct{}
}
```



Barrel is a fisherman's job site block, used to store items. It functions like a single chest, although
it requires no airspace above it to be opened.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    Facing: {
      description: 'Facing is the direction that the barrel is facing.',
      type: 'cube.Face',
      typeLink: '../block-cube.mdx#face',
    },
    Open: {
      description: 'Open is whether the barrel is open or not.',
      type: 'bool',
    },
    CustomName: {
      description: 'CustomName is the custom name of the barrel. This name is displayed when the barrel is opened, and may include colour codes.',
      type: 'string',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Barrel) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="AddViewer">
```go
func (Barrel) AddViewer(v ContainerViewer, tx *world.Tx, pos cube.Pos)
```

AddViewer adds a viewer to the barrel, so that it is updated whenever the inventory of the barrel is changed.

</Accordion>

<Accordion title="BreakInfo">
```go
func (Barrel) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Barrel) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Barrel) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Barrel) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Barrel) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Barrel) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Barrel) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Barrel) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Barrel) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Inventory">
```go
func (Barrel) Inventory(*world.Tx, cube.Pos) *inventory.Inventory
```

Inventory returns the inventory of the barrel. The size of the inventory will be 27.

</Accordion>

<Accordion title="Model">
```go
func (Barrel) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RemoveViewer">
```go
func (Barrel) RemoveViewer(v ContainerViewer, tx *world.Tx, pos cube.Pos)
```

RemoveViewer removes a viewer from the barrel, so that slot updates in the inventory are no longer sent to
it.

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Barrel) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

<Accordion title="WithName">
```go
func (Barrel) WithName(a ...any) world.Item
```

WithName returns the barrel after applying a specific name to the block.

</Accordion>

</Accordions>


---

### Barrier


<Callout type="info">Struct</Callout>



```go
type Barrier struct {
	sourceWaterDisplacer
	transparent
	solid
}
```



Barrier is a transparent solid block used to create invisible boundaries.



#### Fields

<TypeTable
  type={{
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="CanDisplace">
```go
func (Barrier) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Barrier) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Barrier) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Barrier) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Barrier) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Barrier) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Barrier) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

</Accordions>


---

### Basalt


<Callout type="info">Struct</Callout>



```go
type Basalt struct {
	solid
	bassDrum

	// Polished specifies if the basalt is its polished variant.
	Polished	bool
	// Axis is the axis which the basalt faces.
	Axis	cube.Axis
}
```



Basalt is a type of igneous rock found in the Nether.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Polished: {
      description: 'Polished specifies if the basalt is its polished variant.',
      type: 'bool',
    },
    Axis: {
      description: 'Axis is the axis which the basalt faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Basalt) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Basalt) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Basalt) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Basalt) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Basalt) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Basalt) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Basalt) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Beacon


<Callout type="info">Struct</Callout>



```go
type Beacon struct {
	solid
	transparent
	clicksAndSticks
	sourceWaterDisplacer

	// Primary and Secondary are the primary and secondary effects broadcast to nearby entities by the
	// beacon.
	Primary, Secondary	effect.LastingType
	// level is the amount of the pyramid's levels, it is defined by the mineral blocks which build up the
	// pyramid, and can be 0-4.
	level	int
}
```



Beacon is a block that projects a light beam skyward, and can provide status effects such as speed, Jump
Boost, haste, regeneration, resistance, or strength to nearby players.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    clicksAndSticks: {
      description: '',
      type: 'clicksAndSticks',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Primary: {
      description: 'Primary and Secondary are the primary and secondary effects broadcast to nearby entities by the beacon.',
      type: 'effect.LastingType',
      typeLink: '../entity-effect.mdx#lastingtype',
    },
    Secondary: {
      description: 'Primary and Secondary are the primary and secondary effects broadcast to nearby entities by the beacon.',
      type: 'effect.LastingType',
      typeLink: '../entity-effect.mdx#lastingtype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Beacon) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate manages the opening of a beacon by activating it.

</Accordion>

<Accordion title="BreakInfo">
```go
func (Beacon) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Beacon) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Beacon) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Beacon) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Beacon) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Beacon) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="Hash">
```go
func (Beacon) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Beacon) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Level">
```go
func (Beacon) Level() int
```

Level returns an integer 0-4 which defines the current pyramid level of the beacon.

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Beacon) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Beacon) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Beacon) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Beacon) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Tick">
```go
func (Beacon) Tick(currentTick int64, pos cube.Pos, tx *world.Tx)
```

Tick recalculates level, recalculates the active state of the beacon, and powers players,
once every 80 ticks (4 seconds).

</Accordion>

</Accordions>


---

### BeaconSource


<Callout type="info">Interface</Callout>



```go
type BeaconSource interface {
	// PowersBeacon returns a bool which indicates whether this block can contribute to powering up a
	// beacon pyramid.
	PowersBeacon() bool
}
```



BeaconSource represents a block which is capable of contributing to powering a beacon pyramid.






---

### Bedrock


<Callout type="info">Struct</Callout>



```go
type Bedrock struct {
	solid
	bassDrum

	// InfiniteBurning specifies if the bedrock block is set aflame and will burn forever. This is the case
	// for bedrock found under end crystals on top of the end pillars.
	InfiniteBurning	bool
}
```



Bedrock is a block that is indestructible in survival.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    InfiniteBurning: {
      description: 'InfiniteBurning specifies if the bedrock block is set aflame and will burn forever. This is the case for bedrock found under end crystals on top of the end pillars.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="EncodeBlock">
```go
func (Bedrock) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Bedrock) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Bedrock) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Bedrock) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Bedrock) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### BeetrootSeeds


<Callout type="info">Struct</Callout>



```go
type BeetrootSeeds struct {
	crop
}
```



BeetrootSeeds are a crop that can be harvested to craft soup or red dye.



#### Fields

<TypeTable
  type={{
    crop: {
      description: '',
      type: 'crop',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (BeetrootSeeds) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (BeetrootSeeds) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CalculateGrowthChance">
```go
func (BeetrootSeeds) CalculateGrowthChance(pos cube.Pos, tx *world.Tx) float64
```

CalculateGrowthChance calculates the chance the crop will grow during a random tick.

</Accordion>

<Accordion title="CompostChance">
```go
func (BeetrootSeeds) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (BeetrootSeeds) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (BeetrootSeeds) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="GrowthStage">
```go
func (BeetrootSeeds) GrowthStage() int
```

GrowthStage returns the current stage of growth.

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (BeetrootSeeds) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (BeetrootSeeds) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (BeetrootSeeds) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (BeetrootSeeds) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (BeetrootSeeds) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (BeetrootSeeds) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SameCrop">
```go
func (BeetrootSeeds) SameCrop(c Crop) bool
```

SameCrop ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (BeetrootSeeds) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Blackstone


<Callout type="info">Struct</Callout>



```go
type Blackstone struct {
	solid
	bassDrum

	// Type is the type of blackstone of the block.
	Type	BlackstoneType
}
```



Blackstone is a naturally generating block in the nether that can be used to craft stone tools, brewing stands and
furnaces. Gilded blackstone also has a 10% chance to drop 2-6 golden nuggets.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of blackstone of the block.',
      type: 'BlackstoneType',
      typeLink: '../block.mdx#blackstonetype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Blackstone) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Blackstone) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Blackstone) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Blackstone) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Blackstone) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Blackstone) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### BlackstoneType


<Callout type="info">Struct</Callout>



```go
type BlackstoneType struct {
	blackstone
}
```



BlackstoneType represents a type of blackstone.



#### Fields

<TypeTable
  type={{
    blackstone: {
      description: '',
      type: 'blackstone',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (BlackstoneType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (BlackstoneType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (BlackstoneType) Uint8() uint8
```

Uint8 returns the blackstone type as a uint8.

</Accordion>

</Accordions>


---

### BlastFurnace


<Callout type="info">Struct</Callout>



```go
type BlastFurnace struct {
	solid
	bassDrum
	*smelter

	// Facing is the direction the blast furnace is facing.
	Facing	cube.Direction
	// Lit is true if the blast furnace is lit.
	Lit	bool
}
```



BlastFurnace is a block that smelts ores, raw metals, iron and gold armor and tools, similar to a furnace, but at
twice the speed. It also serves as an armorer's job site block.
The empty value of BlastFurnace is not valid. It must be created using block.NewBlastFurnace(cube.Face).



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    "*smelter": {
      description: '',
      type: '*smelter',
    },
    Facing: {
      description: 'Facing is the direction the blast furnace is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Lit: {
      description: 'Lit is true if the blast furnace is lit.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (BlastFurnace) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="AddViewer">
```go
func (BlastFurnace) AddViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

AddViewer adds a viewer to the furnace, so that it is updated whenever the inventory of the furnace is changed.

</Accordion>

<Accordion title="BreakInfo">
```go
func (BlastFurnace) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="DecodeNBT">
```go
func (BlastFurnace) DecodeNBT(data map[string]interface{}) interface{}
```

DecodeNBT ...

</Accordion>

<Accordion title="Durations">
```go
func (BlastFurnace) Durations() (remaining time.Duration, max time.Duration, cook time.Duration)
```

Durations returns the remaining, maximum, and cook durations of the smelter.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (BlastFurnace) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (BlastFurnace) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (BlastFurnace) EncodeNBT() map[string]interface{}
```

EncodeNBT ...

</Accordion>

<Accordion title="Experience">
```go
func (BlastFurnace) Experience() int
```

Experience returns the collected experience of the smelter.

</Accordion>

<Accordion title="ExtractItem">
```go
func (BlastFurnace) ExtractItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

ExtractItem ...

</Accordion>

<Accordion title="Hash">
```go
func (BlastFurnace) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="InsertItem">
```go
func (BlastFurnace) InsertItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

InsertItem ...

</Accordion>

<Accordion title="Instrument">
```go
func (BlastFurnace) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Inventory">
```go
func (BlastFurnace) Inventory(*world.Tx, cube.Pos) *inventory.Inventory
```

Inventory returns the inventory of the furnace.

</Accordion>

<Accordion title="Model">
```go
func (BlastFurnace) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RemoveViewer">
```go
func (BlastFurnace) RemoveViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

RemoveViewer removes a viewer from the furnace, so that slot updates in the inventory are no longer sent to
it.

</Accordion>

<Accordion title="ResetExperience">
```go
func (BlastFurnace) ResetExperience() int
```

ResetExperience resets the collected experience of the smelter, and returns the amount of experience that was reset.

</Accordion>

<Accordion title="Tick">
```go
func (BlastFurnace) Tick(_ int64, pos cube.Pos, tx *world.Tx)
```

Tick is called to check if the blast furnace should update and start or stop smelting.

</Accordion>

<Accordion title="UseOnBlock">
```go
func (BlastFurnace) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### BlueIce


<Callout type="info">Struct</Callout>



```go
type BlueIce struct {
	solid
}
```



BlueIce is a solid block similar to packed ice.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (BlueIce) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (BlueIce) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (BlueIce) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Friction">
```go
func (BlueIce) Friction() float64
```

Friction ...

</Accordion>

<Accordion title="Hash">
```go
func (BlueIce) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (BlueIce) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Bone


<Callout type="info">Struct</Callout>



```go
type Bone struct {
	solid

	// Axis is the axis which the bone block faces.
	Axis	cube.Axis
}
```



Bone is a decorative block that can face different directions.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Axis: {
      description: 'Axis is the axis which the bone block faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Bone) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Bone) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Bone) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Bone) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Bone) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Bone) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Bone) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock handles the rotational placing of bone blocks.

</Accordion>

</Accordions>


---

### Bookshelf


<Callout type="info">Struct</Callout>



```go
type Bookshelf struct {
	solid
	bass
}
```



Bookshelf is a decorative block that primarily serves to enhance enchanting with an enchanting table.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bass: {
      description: '',
      type: 'bass',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Bookshelf) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Bookshelf) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Bookshelf) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Bookshelf) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Bookshelf) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Bookshelf) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Bookshelf) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Bookshelf) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### BreakInfo


<Callout type="info">Struct</Callout>



```go
type BreakInfo struct {
	// Hardness is the hardness of the block, which influences the speed with which the block may be mined.
	Hardness	float64
	// Harvestable is a function called to check if the block is harvestable using the tool passed. If the
	// item used to break the block is not a tool, a tool.ToolNone is passed.
	Harvestable	func(t item.Tool) bool
	// Effective is a function called to check if the block can be mined more effectively with the tool passed
	// than with an empty hand.
	Effective	func(t item.Tool) bool
	// Drops is a function called to get the drops of the block if it is broken using the item passed.
	Drops	func(t item.Tool, enchantments []item.Enchantment) []item.Stack
	// BreakHandler is called after the block has broken.
	BreakHandler	func(pos cube.Pos, w *world.Tx, u item.User)
	// XPDrops is the range of XP a block can drop when broken.
	XPDrops	XPDropRange
	// BlastResistance is the blast resistance of the block, which influences the block's ability to withstand an
	// explosive blast.
	BlastResistance	float64
}
```



BreakInfo is a struct returned by every block. It holds information on block breaking related data, such as
the tool type and tier required to break it.



#### Fields

<TypeTable
  type={{
    Hardness: {
      description: 'Hardness is the hardness of the block, which influences the speed with which the block may be mined.',
      type: 'float64',
    },
    Harvestable: {
      description: 'Harvestable is a function called to check if the block is harvestable using the tool passed. If the item used to break the block is not a tool, a tool.ToolNone is passed.',
      type: 'func(t item.Tool) bool',
    },
    Effective: {
      description: 'Effective is a function called to check if the block can be mined more effectively with the tool passed than with an empty hand.',
      type: 'func(t item.Tool) bool',
    },
    Drops: {
      description: 'Drops is a function called to get the drops of the block if it is broken using the item passed.',
      type: 'func(t item.Tool, enchantments []item.Enchantment) []item.Stack',
    },
    BreakHandler: {
      description: 'BreakHandler is called after the block has broken.',
      type: 'func(pos cube.Pos, w *world.Tx, u item.User)',
    },
    XPDrops: {
      description: 'XPDrops is the range of XP a block can drop when broken.',
      type: 'XPDropRange',
      typeLink: '../block.mdx#xpdroprange',
    },
    BlastResistance: {
      description: 'BlastResistance is the blast resistance of the block, which influences the block\'s ability to withstand an explosive blast.',
      type: 'float64',
    },
  }}
/>





---

### Breakable


<Callout type="info">Interface</Callout>



```go
type Breakable interface {
	// BreakInfo returns information of the block related to the breaking of it.
	BreakInfo() BreakInfo
}
```



Breakable represents a block that may be broken by a player in survival mode. Blocks not include are blocks
such as bedrock.






---

### BrewingStand


<Callout type="info">Struct</Callout>



```go
type BrewingStand struct {
	sourceWaterDisplacer
	transparent
	*brewer

	// LeftSlot is true if the left slot is filled.
	LeftSlot	bool
	// MiddleSlot is true if the middle slot is filled.
	MiddleSlot	bool
	// RightSlot is true if the right slot is filled.
	RightSlot	bool
}
```



BrewingStand is a block used for brewing potions, splash potions, and lingering potions. It also serves as a cleric's
job site block generated in village churches.



#### Fields

<TypeTable
  type={{
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    "*brewer": {
      description: '',
      type: '*brewer',
    },
    LeftSlot: {
      description: 'LeftSlot is true if the left slot is filled.',
      type: 'bool',
    },
    MiddleSlot: {
      description: 'MiddleSlot is true if the middle slot is filled.',
      type: 'bool',
    },
    RightSlot: {
      description: 'RightSlot is true if the right slot is filled.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (BrewingStand) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="AddViewer">
```go
func (BrewingStand) AddViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

AddViewer adds a viewer to the brewer, so that it is updated whenever the inventory of the brewer is changed.

</Accordion>

<Accordion title="BreakInfo">
```go
func (BrewingStand) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (BrewingStand) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (BrewingStand) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="Duration">
```go
func (BrewingStand) Duration() time.Duration
```

Duration returns the remaining duration of the brewing process.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (BrewingStand) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (BrewingStand) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (BrewingStand) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="ExtractItem">
```go
func (BrewingStand) ExtractItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

ExtractItem ...

</Accordion>

<Accordion title="Fuel">
```go
func (BrewingStand) Fuel() (fuel, maxFuel int32)
```

Fuel returns the fuel and maximum fuel of the brewer.

</Accordion>

<Accordion title="Hash">
```go
func (BrewingStand) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="InsertItem">
```go
func (BrewingStand) InsertItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

InsertItem ...

</Accordion>

<Accordion title="Inventory">
```go
func (BrewingStand) Inventory(*world.Tx, cube.Pos) *inventory.Inventory
```

Inventory returns the inventory of the brewer.

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (BrewingStand) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (BrewingStand) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RemoveViewer">
```go
func (BrewingStand) RemoveViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

RemoveViewer removes a viewer from the brewer, so that slot updates in the inventory are no longer sent to
it.

</Accordion>

<Accordion title="SideClosed">
```go
func (BrewingStand) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Tick">
```go
func (BrewingStand) Tick(_ int64, pos cube.Pos, tx *world.Tx)
```

Tick is called to check if the brewing stand should update and start or stop brewing.

</Accordion>

<Accordion title="UseOnBlock">
```go
func (BrewingStand) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Bricks


<Callout type="info">Struct</Callout>



```go
type Bricks struct {
	solid
	bassDrum
}
```



Bricks are decorative building blocks.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Bricks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Bricks) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Bricks) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Bricks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Bricks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Bricks) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Cactus


<Callout type="info">Struct</Callout>



```go
type Cactus struct {
	transparent

	// Age is the growth state of cactus. Values range from 0 to 15.
	Age	int
}
```



Cactus is a plant block that generates naturally in dry areas and causes damage.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    Age: {
      description: 'Age is the growth state of cactus. Values range from 0 to 15.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Cactus) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (Cactus) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Cactus) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Cactus) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EntityInside">
```go
func (Cactus) EntityInside(_ cube.Pos, _ *world.Tx, e world.Entity)
```

EntityInside ...

</Accordion>

<Accordion title="Hash">
```go
func (Cactus) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Cactus) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Cactus) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Cactus) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Cactus) RandomTick(pos cube.Pos, tx *world.Tx, _ *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Cactus) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock handles making sure the neighbouring blocks are air.

</Accordion>

</Accordions>


---

### Cake


<Callout type="info">Struct</Callout>



```go
type Cake struct {
	transparent
	sourceWaterDisplacer

	// Bites is the amount of bites taken out of the cake.
	Bites	int
}
```



Cake is an edible block.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Bites: {
      description: 'Bites is the amount of bites taken out of the cake.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Cake) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Cake) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Cake) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Cake) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Cake) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Cake) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Cake) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Cake) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Cake) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Cake) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Cake) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Calcite


<Callout type="info">Struct</Callout>



```go
type Calcite struct {
	solid
	bassDrum
}
```



Calcite is a carbonate mineral found as part of amethyst geodes.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Calcite) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Calcite) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Calcite) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Calcite) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Calcite) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Calcite) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Campfire


<Callout type="info">Struct</Callout>



```go
type Campfire struct {
	transparent
	bass
	sourceWaterDisplacer

	// Items represents the items in the campfire that are being cooked.
	Items	[4]CampfireItem
	// Facing represents the direction that the campfire is facing.
	Facing	cube.Direction
	// Extinguished is true if the campfire was extinguished by a water source.
	Extinguished	bool
	// Type represents the type of Campfire, currently there are Normal and Soul campfires.
	Type	FireType
}
```



Campfire is a block that can be used to cook food, pacify bees, act as a spread-proof light source, smoke signal or
damaging trap block.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Items: {
      description: 'Items represents the items in the campfire that are being cooked.',
      type: '[4]CampfireItem',
    },
    Facing: {
      description: 'Facing represents the direction that the campfire is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Extinguished: {
      description: 'Extinguished is true if the campfire was extinguished by a water source.',
      type: 'bool',
    },
    Type: {
      description: 'Type represents the type of Campfire, currently there are Normal and Soul campfires.',
      type: 'FireType',
      typeLink: '../block.mdx#firetype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Campfire) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, ctx *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Campfire) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Campfire) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Campfire) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Campfire) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Campfire) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Campfire) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="EntityInside">
```go
func (Campfire) EntityInside(pos cube.Pos, tx *world.Tx, e world.Entity)
```

EntityInside ...

</Accordion>

<Accordion title="Hash">
```go
func (Campfire) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Ignite">
```go
func (Campfire) Ignite(pos cube.Pos, tx *world.Tx, _ world.Entity) bool
```

Ignite ...

</Accordion>

<Accordion title="Instrument">
```go
func (Campfire) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Campfire) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Campfire) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Campfire) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Campfire) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Campfire) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Splash">
```go
func (Campfire) Splash(tx *world.Tx, pos cube.Pos)
```

Splash ...

</Accordion>

<Accordion title="Tick">
```go
func (Campfire) Tick(_ int64, pos cube.Pos, tx *world.Tx)
```

Tick is called to cook the items within the campfire.

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Campfire) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### CampfireItem


<Callout type="info">Struct</Callout>



```go
type CampfireItem struct {
	// Item is a specific item being cooked on top of the campfire.
	Item	item.Stack
	// Time is the countdown of ticks until the food item is cooked (when 0).
	Time	time.Duration
}
```



CampfireItem holds data about the items in the campfire.



#### Fields

<TypeTable
  type={{
    Item: {
      description: 'Item is a specific item being cooked on top of the campfire.',
      type: 'item.Stack',
      typeLink: '../item.mdx#stack',
    },
    Time: {
      description: 'Time is the countdown of ticks until the food item is cooked (when 0).',
      type: 'time.Duration',
    },
  }}
/>





---

### Carpet


<Callout type="info">Struct</Callout>



```go
type Carpet struct {
	carpet
	transparent
	sourceWaterDisplacer

	// Colour is the colour of the carpet.
	Colour	item.Colour
}
```



Carpet is a colourful block that can be obtained by killing/shearing sheep, or crafted using four string.



#### Fields

<TypeTable
  type={{
    carpet: {
      description: '',
      type: 'carpet',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Colour: {
      description: 'Colour is the colour of the carpet.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Carpet) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Carpet) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Carpet) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Carpet) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Carpet) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Carpet) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Carpet) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Carpet) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Carpet) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Carpet) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Carpet) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Carpet) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock handles not placing carpets on top of air blocks.

</Accordion>

</Accordions>


---

### Carrot


<Callout type="info">Struct</Callout>



```go
type Carrot struct {
	crop
}
```



Carrot is a crop that can be consumed raw.



#### Fields

<TypeTable
  type={{
    crop: {
      description: '',
      type: 'crop',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="AlwaysConsumable">
```go
func (Carrot) AlwaysConsumable() bool
```

AlwaysConsumable ...

</Accordion>

<Accordion title="BoneMeal">
```go
func (Carrot) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Carrot) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CalculateGrowthChance">
```go
func (Carrot) CalculateGrowthChance(pos cube.Pos, tx *world.Tx) float64
```

CalculateGrowthChance calculates the chance the crop will grow during a random tick.

</Accordion>

<Accordion title="CompostChance">
```go
func (Carrot) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="Consume">
```go
func (Carrot) Consume(_ *world.Tx, co item.Consumer) item.Stack
```

Consume ...

</Accordion>

<Accordion title="ConsumeDuration">
```go
func (Carrot) ConsumeDuration() time.Duration
```

ConsumeDuration ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Carrot) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Carrot) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="GrowthStage">
```go
func (Carrot) GrowthStage() int
```

GrowthStage returns the current stage of growth.

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Carrot) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Carrot) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Carrot) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Carrot) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Carrot) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Carrot) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SameCrop">
```go
func (Carrot) SameCrop(c Crop) bool
```

SameCrop ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Carrot) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Chest


<Callout type="info">Struct</Callout>



```go
type Chest struct {
	chest
	transparent
	bass
	sourceWaterDisplacer

	// Facing is the direction that the chest is facing.
	Facing	cube.Direction
	// CustomName is the custom name of the chest. This name is displayed when the chest is opened, and may
	// include colour codes.
	CustomName	string

	paired		bool
	pairX, pairZ	int
	pairInv		*inventory.Inventory

	inventory	*inventory.Inventory
	viewerMu	*sync.RWMutex
	viewers		map[ContainerViewer]struct{}
}
```



Chest is a container block which may be used to store items. Chests may also be paired to create a bigger
single container.
The empty value of Chest is not valid. It must be created using block.NewChest().



#### Fields

<TypeTable
  type={{
    chest: {
      description: '',
      type: 'chest',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Facing: {
      description: 'Facing is the direction that the chest is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    CustomName: {
      description: 'CustomName is the custom name of the chest. This name is displayed when the chest is opened, and may include colour codes.',
      type: 'string',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Chest) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="AddViewer">
```go
func (Chest) AddViewer(v ContainerViewer, tx *world.Tx, pos cube.Pos)
```

AddViewer adds a viewer to the chest, so that it is updated whenever the inventory of the chest is changed.

</Accordion>

<Accordion title="BreakInfo">
```go
func (Chest) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Chest) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Chest) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Chest) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Chest) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Chest) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Chest) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Chest) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Chest) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Chest) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Inventory">
```go
func (Chest) Inventory(tx *world.Tx, pos cube.Pos) *inventory.Inventory
```

Inventory returns the inventory of the chest. The size of the inventory will be 27 or 54, depending on
whether the chest is single or double.

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Chest) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Chest) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="Paired">
```go
func (Chest) Paired() bool
```

Paired returns whether the chest is paired with another chest.

</Accordion>

<Accordion title="RemoveViewer">
```go
func (Chest) RemoveViewer(v ContainerViewer, tx *world.Tx, pos cube.Pos)
```

RemoveViewer removes a viewer from the chest, so that slot updates in the inventory are no longer sent to
it.

</Accordion>

<Accordion title="SideClosed">
```go
func (Chest) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Chest) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

<Accordion title="WithName">
```go
func (Chest) WithName(a ...any) world.Item
```

WithName returns the chest after applying a specific name to the block.

</Accordion>

</Accordions>


---

### ChiseledQuartz


<Callout type="info">Struct</Callout>



```go
type ChiseledQuartz struct {
	solid
	bassDrum
}
```



ChiseledQuartz is a mineral block used only for decoration.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (ChiseledQuartz) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (ChiseledQuartz) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (ChiseledQuartz) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (ChiseledQuartz) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (ChiseledQuartz) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (ChiseledQuartz) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Clay


<Callout type="info">Struct</Callout>



```go
type Clay struct {
	solid
}
```



Clay is a block that can be found underwater.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Clay) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Clay) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Clay) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Clay) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Clay) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Clay) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Clay) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### CloseAction


<Callout type="info">Struct</Callout>



```go
type CloseAction struct{ action }
```



CloseAction is a world.BlockAction to close a block at a position, complementary to the OpenAction action.



#### Fields

<TypeTable
  type={{
    action: {
      description: '',
      type: 'action',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BlockAction">
```go
func (CloseAction) BlockAction()
```

BlockAction serves to implement the world.BlockAction interface.

</Accordion>

</Accordions>


---

### Coal


<Callout type="info">Struct</Callout>



```go
type Coal struct {
	solid
	bassDrum
}
```



Coal is a precious mineral block made from 9 coal.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Coal) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Coal) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Coal) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Coal) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Coal) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Coal) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Coal) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Coal) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### CoalOre


<Callout type="info">Struct</Callout>



```go
type CoalOre struct {
	solid
	bassDrum

	// Type is the type of coal ore.
	Type	OreType
}
```



CoalOre is a common ore.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of coal ore.',
      type: 'OreType',
      typeLink: '../block.mdx#oretype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (CoalOre) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CoalOre) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CoalOre) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (CoalOre) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (CoalOre) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (CoalOre) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (CoalOre) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### Cobblestone


<Callout type="info">Struct</Callout>



```go
type Cobblestone struct {
	solid
	bassDrum

	// Mossy specifies if the cobblestone is mossy. This variant of cobblestone is typically found in
	// dungeons or in small clusters in the giant tree taiga biome.
	Mossy	bool
}
```



Cobblestone is a common block, obtained from mining stone.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Mossy: {
      description: 'Mossy specifies if the cobblestone is mossy. This variant of cobblestone is typically found in dungeons or in small clusters in the giant tree taiga biome.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Cobblestone) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Cobblestone) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Cobblestone) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Cobblestone) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Cobblestone) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Cobblestone) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RepairsStoneTools">
```go
func (Cobblestone) RepairsStoneTools() bool
```

RepairsStoneTools ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Cobblestone) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### CocoaBean


<Callout type="info">Struct</Callout>



```go
type CocoaBean struct {
	transparent

	// Facing is the direction from the cocoa bean to the log.
	Facing	cube.Direction
	// Age is the stage of the cocoa bean's growth. 2 is fully grown.
	Age	int
}
```



CocoaBean is a crop block found in jungle biomes.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    Facing: {
      description: 'Facing is the direction from the cocoa bean to the log.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Age: {
      description: 'Age is the stage of the cocoa bean\'s growth. 2 is fully grown.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (CocoaBean) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (CocoaBean) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (CocoaBean) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CocoaBean) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CocoaBean) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (CocoaBean) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (CocoaBean) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (CocoaBean) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (CocoaBean) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (CocoaBean) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (CocoaBean) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (CocoaBean) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Composter


<Callout type="info">Struct</Callout>



```go
type Composter struct {
	bass
	transparent
	sourceWaterDisplacer

	// Level is the level of compost inside the composter. At level 8 it can be collected in the form of bone meal.
	Level	int
}
```



Composter is a block that can turn biological matter in to compost which can then produce bone meal. It is also the
work station for a farming villager.



#### Fields

<TypeTable
  type={{
    bass: {
      description: '',
      type: 'bass',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Level: {
      description: 'Level is the level of compost inside the composter. At level 8 it can be collected in the form of bone meal.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Composter) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, ctx *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Composter) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Composter) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Composter) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Composter) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="ExtractItem">
```go
func (Composter) ExtractItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

ExtractItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Composter) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Composter) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Composter) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="InsertItem">
```go
func (Composter) InsertItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

InsertItem ...

</Accordion>

<Accordion title="Instrument">
```go
func (Composter) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Composter) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Composter) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="ScheduledTick">
```go
func (Composter) ScheduledTick(pos cube.Pos, tx *world.Tx, _ *rand.Rand)
```

ScheduledTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Composter) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

</Accordions>


---

### Concrete


<Callout type="info">Struct</Callout>



```go
type Concrete struct {
	solid
	bassDrum

	// Colour is the colour of the concrete block.
	Colour	item.Colour
}
```



Concrete is a solid block which comes in the 16 regular dye colors, created by placing concrete powder
adjacent to water.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Colour: {
      description: 'Colour is the colour of the concrete block.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Concrete) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Concrete) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Concrete) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Concrete) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Concrete) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Concrete) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### ConcretePowder


<Callout type="info">Struct</Callout>



```go
type ConcretePowder struct {
	gravityAffected
	solid
	snare

	// Colour is the colour of the concrete powder.
	Colour	item.Colour
}
```



ConcretePowder is a gravity affected block that comes in 16 different colours. When interacting with water,
it becomes concrete.



#### Fields

<TypeTable
  type={{
    gravityAffected: {
      description: '',
      type: 'gravityAffected',
    },
    solid: {
      description: '',
      type: 'solid',
    },
    snare: {
      description: '',
      type: 'snare',
    },
    Colour: {
      description: 'Colour is the colour of the concrete powder.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (ConcretePowder) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (ConcretePowder) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (ConcretePowder) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (ConcretePowder) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (ConcretePowder) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (ConcretePowder) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (ConcretePowder) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="Solidifies">
```go
func (ConcretePowder) Solidifies(pos cube.Pos, tx *world.Tx) bool
```

Solidifies ...

</Accordion>

</Accordions>


---

### Container


<Callout type="info">Interface</Callout>



```go
type Container interface {
	AddViewer(v ContainerViewer, tx *world.Tx, pos cube.Pos)
	RemoveViewer(v ContainerViewer, tx *world.Tx, pos cube.Pos)
	Inventory(tx *world.Tx, pos cube.Pos) *inventory.Inventory
}
```



Container represents a container of items, typically a block such as a chest. Containers may have their
inventory opened by viewers.






---

### ContainerOpener


<Callout type="info">Interface</Callout>



```go
type ContainerOpener interface {
	// OpenBlockContainer opens a block container at the position passed.
	OpenBlockContainer(pos cube.Pos, tx *world.Tx)
}
```



ContainerOpener represents an entity that is able to open a container.






---

### ContainerViewer


<Callout type="info">Interface</Callout>



```go
type ContainerViewer interface {
	world.Viewer
	// ViewSlotChange views a change of a single slot in the inventory, in which the item was changed to the
	// new item passed.
	ViewSlotChange(slot int, newItem item.Stack)
}
```



ContainerViewer represents a viewer that is able to view a container and its inventory.






---

### ContinueCrackAction


<Callout type="info">Struct</Callout>



```go
type ContinueCrackAction struct {
	action
	BreakTime	time.Duration
}
```



ContinueCrackAction is a world.BlockAction sent every so often to continue the cracking process of the block. It is
only ever sent after a StartCrackAction action, and may have an altered break time if the player is not on the
ground, submerged or is using a different item than at first.



#### Fields

<TypeTable
  type={{
    action: {
      description: '',
      type: 'action',
    },
    BreakTime: {
      description: '',
      type: 'time.Duration',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BlockAction">
```go
func (ContinueCrackAction) BlockAction()
```

BlockAction serves to implement the world.BlockAction interface.

</Accordion>

</Accordions>


---

### Copper


<Callout type="info">Struct</Callout>



```go
type Copper struct {
	solid
	bassDrum

	// Type is the type of copper of the block.
	Type	CopperType
	// Oxidation is the level of oxidation of the copper block.
	Oxidation	OxidationType
	// Waxed bool is whether the copper block has been waxed with honeycomb.
	Waxed	bool
}
```



Copper is a solid block commonly found in deserts and beaches underneath sand.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of copper of the block.',
      type: 'CopperType',
      typeLink: '../block.mdx#coppertype',
    },
    Oxidation: {
      description: 'Oxidation is the level of oxidation of the copper block.',
      type: 'OxidationType',
      typeLink: '../block.mdx#oxidationtype',
    },
    Waxed: {
      description: 'Waxed bool is whether the copper block has been waxed with honeycomb.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Copper) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanOxidate">
```go
func (Copper) CanOxidate() bool
```

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Copper) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Copper) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Copper) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Copper) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Copper) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="OxidationLevel">
```go
func (Copper) OxidationLevel() OxidationType
```

</Accordion>

<Accordion title="RandomTick">
```go
func (Copper) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

</Accordion>

<Accordion title="Strip">
```go
func (Copper) Strip() (world.Block, world.Sound, bool)
```

</Accordion>

<Accordion title="Wax">
```go
func (Copper) Wax(cube.Pos, mgl64.Vec3) (world.Block, bool)
```

Wax waxes the copper block to stop it from oxidising further.

</Accordion>

<Accordion title="WithOxidationLevel">
```go
func (Copper) WithOxidationLevel(o OxidationType) Oxidisable
```

</Accordion>

</Accordions>


---

### CopperBars


<Callout type="info">Struct</Callout>



```go
type CopperBars struct {
	transparent
	thin
	sourceWaterDisplacer

	// Oxidation is the level of oxidation of the copper bars.
	Oxidation	OxidationType
	// Waxed bool is whether the copper bars has been waxed with honeycomb.
	Waxed	bool
}
```



CopperBars are blocks that serve a similar purpose to glass panes, but made of copper instead of glass.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    thin: {
      description: '',
      type: 'thin',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Oxidation: {
      description: 'Oxidation is the level of oxidation of the copper bars.',
      type: 'OxidationType',
      typeLink: '../block.mdx#oxidationtype',
    },
    Waxed: {
      description: 'Waxed bool is whether the copper bars has been waxed with honeycomb.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (CopperBars) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (CopperBars) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CanOxidate">
```go
func (CopperBars) CanOxidate() bool
```

CanOxidate ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CopperBars) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CopperBars) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (CopperBars) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (CopperBars) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (CopperBars) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="OxidationLevel">
```go
func (CopperBars) OxidationLevel() OxidationType
```

OxidationLevel ...

</Accordion>

<Accordion title="RandomTick">
```go
func (CopperBars) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (CopperBars) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Strip">
```go
func (CopperBars) Strip() (world.Block, world.Sound, bool)
```

Strip ...

</Accordion>

<Accordion title="Wax">
```go
func (CopperBars) Wax(cube.Pos, mgl64.Vec3) (world.Block, bool)
```

Wax waxes the copper bars to stop it from oxidising further.

</Accordion>

<Accordion title="WithOxidationLevel">
```go
func (CopperBars) WithOxidationLevel(o OxidationType) Oxidisable
```

WithOxidationLevel ...

</Accordion>

</Accordions>


---

### CopperChain


<Callout type="info">Struct</Callout>



```go
type CopperChain struct {
	transparent
	sourceWaterDisplacer

	// Axis is the axis which the chain faces.
	Axis	cube.Axis
	// Oxidation is the level of oxidation of the copper chain.
	Oxidation	OxidationType
	// Waxed bool is whether the copper chain has been waxed with honeycomb.
	Waxed	bool
}
```



CopperChain is a metallic decoration block.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Axis: {
      description: 'Axis is the axis which the chain faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
    Oxidation: {
      description: 'Oxidation is the level of oxidation of the copper chain.',
      type: 'OxidationType',
      typeLink: '../block.mdx#oxidationtype',
    },
    Waxed: {
      description: 'Waxed bool is whether the copper chain has been waxed with honeycomb.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (CopperChain) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (CopperChain) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CanOxidate">
```go
func (CopperChain) CanOxidate() bool
```

CanOxidate ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CopperChain) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CopperChain) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (CopperChain) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (CopperChain) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (CopperChain) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="OxidationLevel">
```go
func (CopperChain) OxidationLevel() OxidationType
```

OxidationLevel ...

</Accordion>

<Accordion title="RandomTick">
```go
func (CopperChain) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (CopperChain) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Strip">
```go
func (CopperChain) Strip() (world.Block, world.Sound, bool)
```

Strip ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (CopperChain) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

<Accordion title="Wax">
```go
func (CopperChain) Wax(cube.Pos, mgl64.Vec3) (world.Block, bool)
```

Wax waxes the copper chain to stop it from oxidising further.

</Accordion>

<Accordion title="WithOxidationLevel">
```go
func (CopperChain) WithOxidationLevel(o OxidationType) Oxidisable
```

WithOxidationLevel ...

</Accordion>

</Accordions>


---

### CopperDoor


<Callout type="info">Struct</Callout>



```go
type CopperDoor struct {
	transparent
	bass
	sourceWaterDisplacer

	// Oxidation is the level of oxidation of the copper door.
	Oxidation	OxidationType
	// Waxed bool is whether the copper door has been waxed with honeycomb.
	Waxed	bool
	// Facing is the direction that the door opens towards. When closed, the door sits on the side of its
	// block on the opposite direction.
	Facing	cube.Direction
	// Open is whether the door is open.
	Open	bool
	// Top is whether the block is the top or bottom half of a door.
	Top	bool
	// Right is whether the door hinge is on the right side.
	Right	bool
}
```



CopperDoor is a block that can be used as an openable 1x2 barrier.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Oxidation: {
      description: 'Oxidation is the level of oxidation of the copper door.',
      type: 'OxidationType',
      typeLink: '../block.mdx#oxidationtype',
    },
    Waxed: {
      description: 'Waxed bool is whether the copper door has been waxed with honeycomb.',
      type: 'bool',
    },
    Facing: {
      description: 'Facing is the direction that the door opens towards. When closed, the door sits on the side of its block on the opposite direction.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Open: {
      description: 'Open is whether the door is open.',
      type: 'bool',
    },
    Top: {
      description: 'Top is whether the block is the top or bottom half of a door.',
      type: 'bool',
    },
    Right: {
      description: 'Right is whether the door hinge is on the right side.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (CopperDoor) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, _ item.User, _ *item.UseContext) bool
```

</Accordion>

<Accordion title="BreakInfo">
```go
func (CopperDoor) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (CopperDoor) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CanOxidate">
```go
func (CopperDoor) CanOxidate() bool
```

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CopperDoor) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CopperDoor) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (CopperDoor) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (CopperDoor) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (CopperDoor) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (CopperDoor) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (CopperDoor) NeighbourUpdateTick(pos, changedNeighbour cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="OxidationLevel">
```go
func (CopperDoor) OxidationLevel() OxidationType
```

</Accordion>

<Accordion title="RandomTick">
```go
func (CopperDoor) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

</Accordion>

<Accordion title="SideClosed">
```go
func (CopperDoor) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Strip">
```go
func (CopperDoor) Strip() (world.Block, world.Sound, bool)
```

</Accordion>

<Accordion title="UseOnBlock">
```go
func (CopperDoor) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock handles the directional placing of doors

</Accordion>

<Accordion title="Wax">
```go
func (CopperDoor) Wax(cube.Pos, mgl64.Vec3) (world.Block, bool)
```

Wax waxes the copper door to stop it from oxidising further.

</Accordion>

<Accordion title="WithOxidationLevel">
```go
func (CopperDoor) WithOxidationLevel(o OxidationType) Oxidisable
```

</Accordion>

</Accordions>


---

### CopperGolemPose


<Callout type="info">Struct</Callout>



```go
type CopperGolemPose struct {
	pose
}
```



CopperGolemPose represents a pose of a copper golem statue.



#### Fields

<TypeTable
  type={{
    pose: {
      description: '',
      type: 'pose',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (CopperGolemPose) Name() string
```

Name returns the pose as a string.

</Accordion>

<Accordion title="Uint8">
```go
func (CopperGolemPose) Uint8() uint8
```

Uint8 returns the pose as a uint8.

</Accordion>

</Accordions>


---

### CopperGolemStatue


<Callout type="info">Struct</Callout>



```go
type CopperGolemStatue struct {
	transparent
	sourceWaterDisplacer
	solid

	// Facing is the direction the copper golem statue is facing.
	Facing	cube.Direction
	// Pose is the pose of the copper golem statue.
	Pose	CopperGolemPose
	// Oxidation is the level of oxidation of the copper lantern.
	Oxidation	OxidationType
	// Waxed bool is whether the copper lantern has been waxed with honeycomb.
	Waxed	bool
}
```



CopperGolemStatue is the result of a copper golem fully oxidizing and petrifying into a block.
It can be posed in four different ways.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    solid: {
      description: '',
      type: 'solid',
    },
    Facing: {
      description: 'Facing is the direction the copper golem statue is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Pose: {
      description: 'Pose is the pose of the copper golem statue.',
      type: 'CopperGolemPose',
      typeLink: '../block.mdx#coppergolempose',
    },
    Oxidation: {
      description: 'Oxidation is the level of oxidation of the copper lantern.',
      type: 'OxidationType',
      typeLink: '../block.mdx#oxidationtype',
    },
    Waxed: {
      description: 'Waxed bool is whether the copper lantern has been waxed with honeycomb.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (CopperGolemStatue) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (CopperGolemStatue) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (CopperGolemStatue) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CanOxidate">
```go
func (CopperGolemStatue) CanOxidate() bool
```

CanOxidate ...

</Accordion>

<Accordion title="DecodeNBT">
```go
func (CopperGolemStatue) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CopperGolemStatue) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CopperGolemStatue) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (CopperGolemStatue) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="Hash">
```go
func (CopperGolemStatue) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (CopperGolemStatue) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (CopperGolemStatue) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="OxidationLevel">
```go
func (CopperGolemStatue) OxidationLevel() OxidationType
```

OxidationLevel ...

</Accordion>

<Accordion title="RandomTick">
```go
func (CopperGolemStatue) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="Strip">
```go
func (CopperGolemStatue) Strip() (world.Block, world.Sound, bool)
```

Strip ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (CopperGolemStatue) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

<Accordion title="Wax">
```go
func (CopperGolemStatue) Wax(cube.Pos, mgl64.Vec3) (world.Block, bool)
```

Wax waxes the copper lantern to stop it from oxidising further.

</Accordion>

<Accordion title="WithOxidationLevel">
```go
func (CopperGolemStatue) WithOxidationLevel(o OxidationType) Oxidisable
```

WithOxidationLevel ...

</Accordion>

</Accordions>


---

### CopperGrate


<Callout type="info">Struct</Callout>



```go
type CopperGrate struct {
	sourceWaterDisplacer
	solid
	transparent
	bassDrum

	// Oxidation is the level of oxidation of the copper grate.
	Oxidation	OxidationType
	// Waxed bool is whether the copper grate has been waxed with honeycomb.
	Waxed	bool
}
```



CopperGrate is a solid block commonly found in deserts and beaches underneath sand.



#### Fields

<TypeTable
  type={{
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    solid: {
      description: '',
      type: 'solid',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Oxidation: {
      description: 'Oxidation is the level of oxidation of the copper grate.',
      type: 'OxidationType',
      typeLink: '../block.mdx#oxidationtype',
    },
    Waxed: {
      description: 'Waxed bool is whether the copper grate has been waxed with honeycomb.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (CopperGrate) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (CopperGrate) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CanOxidate">
```go
func (CopperGrate) CanOxidate() bool
```

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CopperGrate) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CopperGrate) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (CopperGrate) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (CopperGrate) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (CopperGrate) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (CopperGrate) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="OxidationLevel">
```go
func (CopperGrate) OxidationLevel() OxidationType
```

</Accordion>

<Accordion title="RandomTick">
```go
func (CopperGrate) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

</Accordion>

<Accordion title="Strip">
```go
func (CopperGrate) Strip() (world.Block, world.Sound, bool)
```

</Accordion>

<Accordion title="Wax">
```go
func (CopperGrate) Wax(cube.Pos, mgl64.Vec3) (world.Block, bool)
```

Wax waxes the copper grate to stop it from oxidising further.

</Accordion>

<Accordion title="WithOxidationLevel">
```go
func (CopperGrate) WithOxidationLevel(o OxidationType) Oxidisable
```

</Accordion>

</Accordions>


---

### CopperLantern


<Callout type="info">Struct</Callout>



```go
type CopperLantern struct {
	transparent
	sourceWaterDisplacer

	// Hanging determines if a lantern is hanging off a block.
	Hanging	bool
	// Oxidation is the level of oxidation of the copper lantern.
	Oxidation	OxidationType
	// Waxed bool is whether the copper lantern has been waxed with honeycomb.
	Waxed	bool
}
```



CopperLantern is a light emitting block.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Hanging: {
      description: 'Hanging determines if a lantern is hanging off a block.',
      type: 'bool',
    },
    Oxidation: {
      description: 'Oxidation is the level of oxidation of the copper lantern.',
      type: 'OxidationType',
      typeLink: '../block.mdx#oxidationtype',
    },
    Waxed: {
      description: 'Waxed bool is whether the copper lantern has been waxed with honeycomb.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (CopperLantern) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (CopperLantern) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CanOxidate">
```go
func (CopperLantern) CanOxidate() bool
```

CanOxidate ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CopperLantern) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CopperLantern) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (CopperLantern) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (CopperLantern) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (CopperLantern) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (CopperLantern) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (CopperLantern) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="OxidationLevel">
```go
func (CopperLantern) OxidationLevel() OxidationType
```

OxidationLevel ...

</Accordion>

<Accordion title="RandomTick">
```go
func (CopperLantern) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (CopperLantern) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Strip">
```go
func (CopperLantern) Strip() (world.Block, world.Sound, bool)
```

Strip ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (CopperLantern) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

<Accordion title="Wax">
```go
func (CopperLantern) Wax(cube.Pos, mgl64.Vec3) (world.Block, bool)
```

Wax waxes the copper lantern to stop it from oxidising further.

</Accordion>

<Accordion title="WithOxidationLevel">
```go
func (CopperLantern) WithOxidationLevel(o OxidationType) Oxidisable
```

WithOxidationLevel ...

</Accordion>

</Accordions>


---

### CopperOre


<Callout type="info">Struct</Callout>



```go
type CopperOre struct {
	solid
	bassDrum

	// Type is the type of copper ore.
	Type	OreType
}
```



CopperOre is a rare mineral block found underground.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of copper ore.',
      type: 'OreType',
      typeLink: '../block.mdx#oretype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (CopperOre) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CopperOre) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CopperOre) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (CopperOre) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (CopperOre) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (CopperOre) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (CopperOre) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### CopperTorch


<Callout type="info">Struct</Callout>



```go
type CopperTorch struct {
	transparent
	empty

	// Facing is the direction from the torch to the block.
	Facing	cube.Face
}
```



CopperTorch are non-solid blocks that emit light.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    empty: {
      description: '',
      type: 'empty',
    },
    Facing: {
      description: 'Facing is the direction from the torch to the block.',
      type: 'cube.Face',
      typeLink: '../block-cube.mdx#face',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (CopperTorch) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CopperTorch) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CopperTorch) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (CopperTorch) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (CopperTorch) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (CopperTorch) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (CopperTorch) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (CopperTorch) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (CopperTorch) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (CopperTorch) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### CopperTrapdoor


<Callout type="info">Struct</Callout>



```go
type CopperTrapdoor struct {
	transparent
	bass
	sourceWaterDisplacer

	// Oxidation is the level of oxidation of the copper trapdoor.
	Oxidation	OxidationType
	// Waxed bool is whether the copper trapdoor has been waxed with honeycomb.
	Waxed	bool
	// Facing is the direction the trapdoor is facing.
	Facing	cube.Direction
	// Open is whether the trapdoor is open.
	Open	bool
	// Top is whether the trapdoor occupies the top or bottom part of a block.
	Top	bool
}
```



CopperTrapdoor is a block that can be used as an openable 1x1 barrier.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Oxidation: {
      description: 'Oxidation is the level of oxidation of the copper trapdoor.',
      type: 'OxidationType',
      typeLink: '../block.mdx#oxidationtype',
    },
    Waxed: {
      description: 'Waxed bool is whether the copper trapdoor has been waxed with honeycomb.',
      type: 'bool',
    },
    Facing: {
      description: 'Facing is the direction the trapdoor is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Open: {
      description: 'Open is whether the trapdoor is open.',
      type: 'bool',
    },
    Top: {
      description: 'Top is whether the trapdoor occupies the top or bottom part of a block.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (CopperTrapdoor) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, _ item.User, _ *item.UseContext) bool
```

</Accordion>

<Accordion title="BreakInfo">
```go
func (CopperTrapdoor) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (CopperTrapdoor) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CanOxidate">
```go
func (CopperTrapdoor) CanOxidate() bool
```

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CopperTrapdoor) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CopperTrapdoor) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (CopperTrapdoor) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (CopperTrapdoor) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (CopperTrapdoor) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (CopperTrapdoor) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="OxidationLevel">
```go
func (CopperTrapdoor) OxidationLevel() OxidationType
```

</Accordion>

<Accordion title="RandomTick">
```go
func (CopperTrapdoor) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

</Accordion>

<Accordion title="SideClosed">
```go
func (CopperTrapdoor) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Strip">
```go
func (CopperTrapdoor) Strip() (world.Block, world.Sound, bool)
```

</Accordion>

<Accordion title="UseOnBlock">
```go
func (CopperTrapdoor) UseOnBlock(pos cube.Pos, face cube.Face, clickPos mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock handles the directional placing of trapdoors and makes sure they are properly placed upside down
when needed.

</Accordion>

<Accordion title="Wax">
```go
func (CopperTrapdoor) Wax(cube.Pos, mgl64.Vec3) (world.Block, bool)
```

Wax waxes the copper trapdoor to stop it from oxidising further.

</Accordion>

<Accordion title="WithOxidationLevel">
```go
func (CopperTrapdoor) WithOxidationLevel(o OxidationType) Oxidisable
```

</Accordion>

</Accordions>


---

### CopperType


<Callout type="info">Struct</Callout>



```go
type CopperType struct {
	copper
}
```



CopperType represents a type of copper.



#### Fields

<TypeTable
  type={{
    copper: {
      description: '',
      type: 'copper',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (CopperType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (CopperType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (CopperType) Uint8() uint8
```

Uint8 returns the copper as a uint8.

</Accordion>

</Accordions>


---

### Coral


<Callout type="info">Struct</Callout>



```go
type Coral struct {
	empty
	transparent
	bassDrum
	sourceWaterDisplacer

	// Type is the type of coral of the block.
	Type	CoralType
	// Dead is whether the coral is dead.
	Dead	bool
}
```



Coral is a non-solid block that comes in 5 variants.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Type: {
      description: 'Type is the type of coral of the block.',
      type: 'CoralType',
      typeLink: '../block.mdx#coraltype',
    },
    Dead: {
      description: 'Dead is whether the coral is dead.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Coral) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Coral) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Coral) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Coral) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Coral) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Coral) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Coral) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Coral) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Coral) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Coral) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="ScheduledTick">
```go
func (Coral) ScheduledTick(pos cube.Pos, tx *world.Tx, _ *rand.Rand)
```

ScheduledTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Coral) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Coral) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### CoralBlock


<Callout type="info">Struct</Callout>



```go
type CoralBlock struct {
	solid
	bassDrum

	// Type is the type of coral of the block.
	Type	CoralType
	// Dead is whether the coral block is dead.
	Dead	bool
}
```



CoralBlock is a solid block that comes in 5 variants.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of coral of the block.',
      type: 'CoralType',
      typeLink: '../block.mdx#coraltype',
    },
    Dead: {
      description: 'Dead is whether the coral block is dead.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (CoralBlock) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CoralBlock) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CoralBlock) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (CoralBlock) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (CoralBlock) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (CoralBlock) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (CoralBlock) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="ScheduledTick">
```go
func (CoralBlock) ScheduledTick(pos cube.Pos, tx *world.Tx, _ *rand.Rand)
```

ScheduledTick ...

</Accordion>

</Accordions>


---

### CoralType


<Callout type="info">Struct</Callout>



```go
type CoralType struct {
	coral
}
```



CoralType represents a type of coral of a block. CoralType, coral fans, and coral blocks carry one of these types.



#### Fields

<TypeTable
  type={{
    coral: {
      description: '',
      type: 'coral',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Colour">
```go
func (CoralType) Colour() item.Colour
```

Colour returns the colour of the CoralType.

</Accordion>

<Accordion title="Name">
```go
func (CoralType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (CoralType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (CoralType) Uint8() uint8
```

Uint8 returns the coral as a uint8.

</Accordion>

</Accordions>


---

### CraftingTable


<Callout type="info">Struct</Callout>



```go
type CraftingTable struct {
	bass
	solid
}
```



CraftingTable is a utility block that allows the player to craft a variety of blocks and items.



#### Fields

<TypeTable
  type={{
    bass: {
      description: '',
      type: 'bass',
    },
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (CraftingTable) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (CraftingTable) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (CraftingTable) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (CraftingTable) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (CraftingTable) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (CraftingTable) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (CraftingTable) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (CraftingTable) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Crop


<Callout type="info">Interface</Callout>



```go
type Crop interface {
	// GrowthStage returns the crop's current stage of growth. The max value is 7.
	GrowthStage() int
	// SameCrop checks if two crops are of the same type.
	SameCrop(c Crop) bool
}
```



Crop is an interface for all crops that are grown on farmland. A crop has a random chance to grow during random ticks.






---

### DamageSource


<Callout type="info">Struct</Callout>



```go
type DamageSource struct {
	// Block is the block that caused the damage.
	Block world.Block
}
```



DamageSource is passed as world.DamageSource for damage caused by a block,
such as a cactus or a falling anvil.



#### Fields

<TypeTable
  type={{
    Block: {
      description: 'Block is the block that caused the damage.',
      type: 'world.Block',
      typeLink: '../world.mdx#block',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Fire">
```go
func (DamageSource) Fire() bool
```

</Accordion>

<Accordion title="IgnoreTotem">
```go
func (DamageSource) IgnoreTotem() bool
```

</Accordion>

<Accordion title="ReducedByArmour">
```go
func (DamageSource) ReducedByArmour() bool
```

</Accordion>

<Accordion title="ReducedByResistance">
```go
func (DamageSource) ReducedByResistance() bool
```

</Accordion>

</Accordions>


---

### DeadBush


<Callout type="info">Struct</Callout>



```go
type DeadBush struct {
	empty
	replaceable
	transparent
	sourceWaterDisplacer
}
```



DeadBush is a transparent block in the form of an aesthetic plant.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    replaceable: {
      description: '',
      type: 'replaceable',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (DeadBush) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (DeadBush) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DeadBush) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DeadBush) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (DeadBush) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (DeadBush) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (DeadBush) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (DeadBush) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (DeadBush) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (DeadBush) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (DeadBush) ReplaceableBy(world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="SideClosed">
```go
func (DeadBush) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (DeadBush) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### DecoratedPot


<Callout type="info">Struct</Callout>



```go
type DecoratedPot struct {
	transparent
	sourceWaterDisplacer

	// Item is the item being stored in the decorated pot.
	Item	item.Stack
	// Facing is the direction the pot is facing. The first decoration will be facing opposite of this direction.
	Facing	cube.Direction
	// Decorations are the four decorations displayed on the sides of the pot. If a decoration is a brick or nil,
	// the side will appear to be empty.
	Decorations	[4]PotDecoration
}
```



DecoratedPot is a decoration block that can be crafted from up to four pottery sherds, and bricks on the sides where
no pattern should be displayed.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Item: {
      description: 'Item is the item being stored in the decorated pot.',
      type: 'item.Stack',
      typeLink: '../item.mdx#stack',
    },
    Facing: {
      description: 'Facing is the direction the pot is facing. The first decoration will be facing opposite of this direction.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Decorations: {
      description: 'Decorations are the four decorations displayed on the sides of the pot. If a decoration is a brick or nil, the side will appear to be empty.',
      type: '[4]PotDecoration',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (DecoratedPot) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, ctx *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (DecoratedPot) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (DecoratedPot) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (DecoratedPot) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DecoratedPot) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DecoratedPot) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (DecoratedPot) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="ExtractItem">
```go
func (DecoratedPot) ExtractItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

ExtractItem ...

</Accordion>

<Accordion title="Hash">
```go
func (DecoratedPot) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="InsertItem">
```go
func (DecoratedPot) InsertItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

InsertItem ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (DecoratedPot) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (DecoratedPot) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="Pick">
```go
func (DecoratedPot) Pick() item.Stack
```

Pick ...

</Accordion>

<Accordion title="ProjectileHit">
```go
func (DecoratedPot) ProjectileHit(pos cube.Pos, tx *world.Tx, _ world.Entity, _ cube.Face)
```

ProjectileHit ...

</Accordion>

<Accordion title="SideClosed">
```go
func (DecoratedPot) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (DecoratedPot) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### DecoratedPotWobbleAction


<Callout type="info">Struct</Callout>



```go
type DecoratedPotWobbleAction struct {
	action
	DecoratedPot	DecoratedPot
	// Success is whether an item was successfully inserted into the decorated pot.
	Success	bool
}
```



DecoratedPotWobbleAction is a world.BlockAction to make a decorated pot wobble when interacted with.



#### Fields

<TypeTable
  type={{
    action: {
      description: '',
      type: 'action',
    },
    DecoratedPot: {
      description: '',
      type: 'DecoratedPot',
      typeLink: '../block.mdx#decoratedpot',
    },
    Success: {
      description: 'Success is whether an item was successfully inserted into the decorated pot.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BlockAction">
```go
func (DecoratedPotWobbleAction) BlockAction()
```

BlockAction serves to implement the world.BlockAction interface.

</Accordion>

</Accordions>


---

### Deepslate


<Callout type="info">Struct</Callout>



```go
type Deepslate struct {
	solid
	bassDrum

	// Type is the type of deepslate of the block.
	Type	DeepslateType
	// Axis is the axis which the deepslate faces.
	Axis	cube.Axis
}
```



Deepslate is similar to stone but is naturally found deep underground around Y0 and below, and is harder to break.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of deepslate of the block.',
      type: 'DeepslateType',
      typeLink: '../block.mdx#deepslatetype',
    },
    Axis: {
      description: 'Axis is the axis which the deepslate faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Deepslate) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Deepslate) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Deepslate) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Deepslate) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Deepslate) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Deepslate) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Deepslate) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Deepslate) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### DeepslateBricks


<Callout type="info">Struct</Callout>



```go
type DeepslateBricks struct {
	solid
	bassDrum

	// Cracked specifies if the deepslate bricks is its cracked variant.
	Cracked	bool
}
```



DeepslateBricks are a brick variant of deepslate and can spawn in ancient cities.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Cracked: {
      description: 'Cracked specifies if the deepslate bricks is its cracked variant.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (DeepslateBricks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DeepslateBricks) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DeepslateBricks) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (DeepslateBricks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (DeepslateBricks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (DeepslateBricks) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (DeepslateBricks) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### DeepslateTiles


<Callout type="info">Struct</Callout>



```go
type DeepslateTiles struct {
	solid
	bassDrum

	// Cracked specifies if the deepslate tiles is its cracked variant.
	Cracked	bool
}
```



DeepslateTiles are a tiled variant of deepslate and can spawn in ancient cities.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Cracked: {
      description: 'Cracked specifies if the deepslate tiles is its cracked variant.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (DeepslateTiles) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DeepslateTiles) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DeepslateTiles) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (DeepslateTiles) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (DeepslateTiles) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (DeepslateTiles) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (DeepslateTiles) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### DeepslateType


<Callout type="info">Struct</Callout>



```go
type DeepslateType struct {
	deepslate
}
```



DeepslateType represents a type of deepslate.



#### Fields

<TypeTable
  type={{
    deepslate: {
      description: '',
      type: 'deepslate',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (DeepslateType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (DeepslateType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (DeepslateType) Uint8() uint8
```

Uint8 returns the deepslate type as a uint8.

</Accordion>

</Accordions>


---

### Diamond


<Callout type="info">Struct</Callout>



```go
type Diamond struct {
	solid
}
```



Diamond is a block which can only be gained by crafting it.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Diamond) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Diamond) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Diamond) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Diamond) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Diamond) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="PowersBeacon">
```go
func (Diamond) PowersBeacon() bool
```

PowersBeacon ...

</Accordion>

</Accordions>


---

### DiamondOre


<Callout type="info">Struct</Callout>



```go
type DiamondOre struct {
	solid
	bassDrum

	// Type is the type of diamond ore.
	Type	OreType
}
```



DiamondOre is a rare ore that generates underground.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of diamond ore.',
      type: 'OreType',
      typeLink: '../block.mdx#oretype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (DiamondOre) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DiamondOre) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DiamondOre) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (DiamondOre) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (DiamondOre) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (DiamondOre) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (DiamondOre) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### Diorite




```go
type Diorite polishable
```



Diorite is a type of igneous rock.





#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Diorite) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Diorite) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Diorite) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Diorite) Hash() (uint64, uint64)
```

</Accordion>

</Accordions>


---

### Dirt


<Callout type="info">Struct</Callout>



```go
type Dirt struct {
	solid

	// Coarse specifies if the dirt should be off the coarse dirt variant. Grass blocks won't spread on
	// the block if set to true.
	Coarse	bool
}
```



Dirt is a block found abundantly in most biomes under a layer of grass blocks at the top of the normal
world.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Coarse: {
      description: 'Coarse specifies if the dirt should be off the coarse dirt variant. Grass blocks won\'t spread on the block if set to true.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Dirt) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Dirt) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Dirt) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Dirt) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Dirt) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="Shovel">
```go
func (Dirt) Shovel() (world.Block, bool)
```

Shovel ...

</Accordion>

<Accordion title="SoilFor">
```go
func (Dirt) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

<Accordion title="Till">
```go
func (Dirt) Till() (world.Block, bool)
```

Till ...

</Accordion>

</Accordions>


---

### DirtPath


<Callout type="info">Struct</Callout>



```go
type DirtPath struct {
	tilledGrass
	transparent
}
```



DirtPath is a decorative block that can be created by using a shovel on a dirt or grass block.



#### Fields

<TypeTable
  type={{
    tilledGrass: {
      description: '',
      type: 'tilledGrass',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (DirtPath) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DirtPath) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DirtPath) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (DirtPath) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (DirtPath) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (DirtPath) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (DirtPath) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick handles the turning from dirt path into dirt if a block is placed on top of it.

</Accordion>

<Accordion title="Till">
```go
func (DirtPath) Till() (world.Block, bool)
```

Till ...

</Accordion>

</Accordions>


---

### DoubleFlower


<Callout type="info">Struct</Callout>



```go
type DoubleFlower struct {
	transparent
	empty

	// UpperPart is set if the plant is the upper part.
	UpperPart	bool
	// Type is the type of the double plant.
	Type	DoubleFlowerType
}
```



DoubleFlower is a two block high flower consisting of an upper and lower part.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    empty: {
      description: '',
      type: 'empty',
    },
    UpperPart: {
      description: 'UpperPart is set if the plant is the upper part.',
      type: 'bool',
    },
    Type: {
      description: 'Type is the type of the double plant.',
      type: 'DoubleFlowerType',
      typeLink: '../block.mdx#doubleflowertype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (DoubleFlower) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (DoubleFlower) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (DoubleFlower) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DoubleFlower) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DoubleFlower) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (DoubleFlower) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (DoubleFlower) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (DoubleFlower) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (DoubleFlower) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (DoubleFlower) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (DoubleFlower) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (DoubleFlower) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### DoubleFlowerType


<Callout type="info">Struct</Callout>



```go
type DoubleFlowerType struct {
	doubleFlower
}
```



DoubleFlowerType represents a type of double flower.



#### Fields

<TypeTable
  type={{
    doubleFlower: {
      description: '',
      type: 'doubleFlower',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (DoubleFlowerType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (DoubleFlowerType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (DoubleFlowerType) Uint8() uint8
```

Uint8 returns the double plant as a uint8.

</Accordion>

</Accordions>


---

### DoubleTallGrass


<Callout type="info">Struct</Callout>



```go
type DoubleTallGrass struct {
	transparent
	replaceable
	empty

	// UpperPart is set if the plant is the upper part.
	UpperPart	bool
	// Type is the type of double tall grass.
	Type	DoubleTallGrassType
}
```



DoubleTallGrass is a two-block high variety of grass.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    replaceable: {
      description: '',
      type: 'replaceable',
    },
    empty: {
      description: '',
      type: 'empty',
    },
    UpperPart: {
      description: 'UpperPart is set if the plant is the upper part.',
      type: 'bool',
    },
    Type: {
      description: 'Type is the type of double tall grass.',
      type: 'DoubleTallGrassType',
      typeLink: '../block.mdx#doubletallgrasstype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (DoubleTallGrass) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (DoubleTallGrass) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DoubleTallGrass) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DoubleTallGrass) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (DoubleTallGrass) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (DoubleTallGrass) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (DoubleTallGrass) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (DoubleTallGrass) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (DoubleTallGrass) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (DoubleTallGrass) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (DoubleTallGrass) ReplaceableBy(world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (DoubleTallGrass) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### DoubleTallGrassType


<Callout type="info">Struct</Callout>



```go
type DoubleTallGrassType struct {
	doubleTallGrass
}
```



DoubleTallGrassType represents a type of double tall grass, which can be placed on top of grass blocks.



#### Fields

<TypeTable
  type={{
    doubleTallGrass: {
      description: '',
      type: 'doubleTallGrass',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (DoubleTallGrassType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (DoubleTallGrassType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (DoubleTallGrassType) Uint8() uint8
```

Uint8 ...

</Accordion>

</Accordions>


---

### DragonEgg


<Callout type="info">Struct</Callout>



```go
type DragonEgg struct {
	solid
	transparent
	gravityAffected
	sourceWaterDisplacer
}
```



DragonEgg is a decorative block or a "trophy item", and the rarest item in the game.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    gravityAffected: {
      description: '',
      type: 'gravityAffected',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (DragonEgg) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, _ item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (DragonEgg) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (DragonEgg) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DragonEgg) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DragonEgg) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (DragonEgg) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (DragonEgg) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (DragonEgg) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (DragonEgg) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (DragonEgg) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="Punch">
```go
func (DragonEgg) Punch(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User)
```

Punch ...

</Accordion>

<Accordion title="SideClosed">
```go
func (DragonEgg) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Solidifies">
```go
func (DragonEgg) Solidifies(cube.Pos, *world.Tx) bool
```

Solidifies ...

</Accordion>

</Accordions>


---

### DriedKelp


<Callout type="info">Struct</Callout>



```go
type DriedKelp struct {
	solid
}
```



DriedKelp is a block primarily used as fuel in furnaces.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (DriedKelp) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (DriedKelp) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (DriedKelp) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (DriedKelp) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (DriedKelp) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (DriedKelp) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (DriedKelp) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (DriedKelp) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Dripstone


<Callout type="info">Struct</Callout>



```go
type Dripstone struct {
	solid
	bassDrum
}
```



Dripstone is a rock block that allows pointed dripstone to grow beneath it.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Dripstone) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Dripstone) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Dripstone) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Dripstone) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Dripstone) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Dripstone) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Emerald


<Callout type="info">Struct</Callout>



```go
type Emerald struct {
	solid
}
```



Emerald is a precious mineral block crafted using 9 emeralds.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Emerald) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Emerald) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Emerald) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Emerald) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Emerald) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Emerald) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="PowersBeacon">
```go
func (Emerald) PowersBeacon() bool
```

PowersBeacon ...

</Accordion>

</Accordions>


---

### EmeraldOre


<Callout type="info">Struct</Callout>



```go
type EmeraldOre struct {
	solid
	bassDrum

	// Type is the type of emerald ore.
	Type	OreType
}
```



EmeraldOre is an ore generating exclusively under mountain biomes.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of emerald ore.',
      type: 'OreType',
      typeLink: '../block.mdx#oretype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (EmeraldOre) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (EmeraldOre) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (EmeraldOre) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (EmeraldOre) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (EmeraldOre) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (EmeraldOre) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (EmeraldOre) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### EnchantingTable


<Callout type="info">Struct</Callout>



```go
type EnchantingTable struct {
	transparent
	bassDrum
	sourceWaterDisplacer
}
```



EnchantingTable is a block that allows players to spend their experience point levels to enchant tools, weapons,
books, armor, and certain other items.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (EnchantingTable) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (EnchantingTable) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (EnchantingTable) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (EnchantingTable) DecodeNBT(map[string]any) any
```

DecodeNBT is used to implement world.NBTer.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (EnchantingTable) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (EnchantingTable) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (EnchantingTable) EncodeNBT() map[string]any
```

EncodeNBT is used to encode the block to NBT, so that the enchanting table book will be rendered properly client-side.
The actual rotation value doesn't need to be set in the NBT, we just need to write the default NBT for the block.

</Accordion>

<Accordion title="Hash">
```go
func (EnchantingTable) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (EnchantingTable) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (EnchantingTable) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (EnchantingTable) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (EnchantingTable) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (EnchantingTable) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

</Accordions>


---

### EndBricks


<Callout type="info">Struct</Callout>



```go
type EndBricks struct {
	solid
	bassDrum
}
```



EndBricks is a block made from combining four endstone blocks together.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (EndBricks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (EndBricks) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (EndBricks) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (EndBricks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (EndBricks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (EndBricks) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### EndRod


<Callout type="info">Struct</Callout>



```go
type EndRod struct {
	transparent
	flowingWaterDisplacer

	// Facing is the direction the white end point of the end rod is facing.
	Facing	cube.Face
}
```



EndRod is a decorative light source that emits white particles.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    flowingWaterDisplacer: {
      description: '',
      type: 'flowingWaterDisplacer',
    },
    Facing: {
      description: 'Facing is the direction the white end point of the end rod is facing.',
      type: 'cube.Face',
      typeLink: '../block-cube.mdx#face',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (EndRod) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (EndRod) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (EndRod) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (EndRod) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (EndRod) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (EndRod) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (EndRod) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (EndRod) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (EndRod) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (EndRod) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### EndStone


<Callout type="info">Struct</Callout>



```go
type EndStone struct {
	solid
	bassDrum
}
```



EndStone is a block found in The End.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (EndStone) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (EndStone) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (EndStone) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (EndStone) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (EndStone) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (EndStone) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### EnderChest


<Callout type="info">Struct</Callout>



```go
type EnderChest struct {
	chest
	transparent
	bass
	sourceWaterDisplacer

	// Facing is the direction that the ender chest is facing.
	Facing	cube.Direction

	viewers	*atomic.Int64
}
```



EnderChest is a type of chest whose contents are exclusive to each player, and can be accessed from anywhere.
The empty value of EnderChest is not valid. It must be created using block.NewEnderChest().



#### Fields

<TypeTable
  type={{
    chest: {
      description: '',
      type: 'chest',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Facing: {
      description: 'Facing is the direction that the ender chest is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (EnderChest) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="AddViewer">
```go
func (EnderChest) AddViewer(tx *world.Tx, pos cube.Pos)
```

AddViewer ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (EnderChest) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (EnderChest) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (EnderChest) DecodeNBT(map[string]interface{}) interface{}
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (EnderChest) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (EnderChest) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (EnderChest) EncodeNBT() map[string]interface{}
```

EncodeNBT ...

</Accordion>

<Accordion title="Hash">
```go
func (EnderChest) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (EnderChest) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (EnderChest) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (EnderChest) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (EnderChest) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RemoveViewer">
```go
func (EnderChest) RemoveViewer(tx *world.Tx, pos cube.Pos)
```

RemoveViewer ...

</Accordion>

<Accordion title="SideClosed">
```go
func (EnderChest) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (EnderChest) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### EntityInsider


<Callout type="info">Interface</Callout>



```go
type EntityInsider interface {
	// EntityInside is called when an entity goes inside the block's 1x1x1 axis aligned bounding box.
	EntityInside(pos cube.Pos, tx *world.Tx, e world.Entity)
}
```



EntityInsider represents a block that reacts to an entity going inside its 1x1x1 axis
aligned bounding box.






---

### EntityLander


<Callout type="info">Interface</Callout>



```go
type EntityLander interface {
	// EntityLand is called when an entity lands on the block.
	EntityLand(pos cube.Pos, tx *world.Tx, e world.Entity, distance *float64)
}
```



EntityLander represents a block that reacts to an entity landing on it after falling.






---

### Explodable


<Callout type="info">Interface</Callout>



```go
type Explodable interface {
	// Explode is called when an explosion occurs. The block can react to the explosion using the configuration passed.
	Explode(explosionPos mgl64.Vec3, pos cube.Pos, tx *world.Tx, c ExplosionConfig)
}
```



Explodable represents a block that can be exploded.






---

### ExplodableEntity


<Callout type="info">Interface</Callout>



```go
type ExplodableEntity interface {
	// Explode is called when an explosion occurs. The entity can then react to the explosion using the configuration
	// and impact provided.
	Explode(explosionPos mgl64.Vec3, impact float64, c ExplosionConfig)
}
```



ExplodableEntity represents an entity that can be exploded.






---

### ExplosionConfig


<Callout type="info">Struct</Callout>



```go
type ExplosionConfig struct {
	// Size is the size of the explosion, it is effectively the radius which entities/blocks will be affected within.
	Size	float64
	// RandSource is the source to use for the explosion "randomness". If set
	// to nil, RandSource defaults to a `rand.PCG`source seeded with
	// `time.Now().UnixNano()`.
	RandSource	rand.Source
	// SpawnFire will cause the explosion to randomly start fires in 1/3 of all destroyed air blocks that are
	// above opaque blocks.
	SpawnFire	bool
	// ItemDropChance specifies how item drops should be handled. By default,
	// the item drop chance is 1/Size. If negative, no items will be dropped by
	// the explosion. If set to 1 or higher, all items are dropped.
	ItemDropChance	float64

	// Sound is the sound to play when the explosion is created. If set to nil, this will default to the sound of a
	// regular explosion.
	Sound	world.Sound
	// Particle is the particle to spawn when the explosion is created. If set to nil, this will default to the particle
	// of a regular huge explosion.
	Particle	world.Particle
}
```



ExplosionConfig is the configuration for an explosion. The world, position, size, sound, particle, and more can all
be configured through this configuration.



#### Fields

<TypeTable
  type={{
    Size: {
      description: 'Size is the size of the explosion, it is effectively the radius which entities/blocks will be affected within.',
      type: 'float64',
    },
    RandSource: {
      description: 'RandSource is the source to use for the explosion "randomness". If set to nil, RandSource defaults to a `rand.PCG`source seeded with `time.Now().UnixNano()`.',
      type: 'rand.Source',
    },
    SpawnFire: {
      description: 'SpawnFire will cause the explosion to randomly start fires in 1/3 of all destroyed air blocks that are above opaque blocks.',
      type: 'bool',
    },
    ItemDropChance: {
      description: 'ItemDropChance specifies how item drops should be handled. By default, the item drop chance is 1/Size. If negative, no items will be dropped by the explosion. If set to 1 or higher, all items are dropped.',
      type: 'float64',
    },
    Sound: {
      description: 'Sound is the sound to play when the explosion is created. If set to nil, this will default to the sound of a regular explosion.',
      type: 'world.Sound',
      typeLink: '../world.mdx#sound',
    },
    Particle: {
      description: 'Particle is the particle to spawn when the explosion is created. If set to nil, this will default to the particle of a regular huge explosion.',
      type: 'world.Particle',
      typeLink: '../world.mdx#particle',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Explode">
```go
func (ExplosionConfig) Explode(tx *world.Tx, explosionPos mgl64.Vec3)
```

Explode performs the explosion as specified by the configuration.

</Accordion>

</Accordions>


---

### Farmland


<Callout type="info">Struct</Callout>



```go
type Farmland struct {
	tilledGrass

	// Hydration is how much moisture the farmland block has. Hydration starts at 0 & caps at 7. During a random tick
	// update, if there is water within 4 blocks from the farmland block, hydration is set to 7. Otherwise, it
	// decrements until it turns into dirt.
	Hydration	int
}
```



Farmland is a block that crops are grown on. Farmland is created by interacting with a grass or dirt block using a
hoe. Farmland can be hydrated by nearby water, with no hydration resulting in it turning into a dirt block.



#### Fields

<TypeTable
  type={{
    tilledGrass: {
      description: '',
      type: 'tilledGrass',
    },
    Hydration: {
      description: 'Hydration is how much moisture the farmland block has. Hydration starts at 0 & caps at 7. During a random tick update, if there is water within 4 blocks from the farmland block, hydration is set to 7. Otherwise, it decrements until it turns into dirt.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Farmland) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Farmland) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Farmland) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EntityLand">
```go
func (Farmland) EntityLand(pos cube.Pos, tx *world.Tx, e world.Entity, _ *float64)
```

EntityLand ...

</Accordion>

<Accordion title="Hash">
```go
func (Farmland) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Farmland) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Farmland) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Farmland) RandomTick(pos cube.Pos, tx *world.Tx, _ *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SoilFor">
```go
func (Farmland) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

</Accordions>


---

### Fern


<Callout type="info">Struct</Callout>



```go
type Fern struct {
	replaceable
	transparent
	empty
}
```



Fern is a transparent plant block which can be used to obtain seeds and as decoration.



#### Fields

<TypeTable
  type={{
    replaceable: {
      description: '',
      type: 'replaceable',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    empty: {
      description: '',
      type: 'empty',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (Fern) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal attempts to affect the block using a bone meal item.

</Accordion>

<Accordion title="BreakInfo">
```go
func (Fern) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (Fern) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Fern) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Fern) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Fern) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Fern) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Fern) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Fern) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Fern) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Fern) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (Fern) ReplaceableBy(world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Fern) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Fire


<Callout type="info">Struct</Callout>



```go
type Fire struct {
	replaceable
	transparent
	empty

	// Type is the type of fire.
	Type	FireType
	// Age affects how fire extinguishes. Newly placed fire starts at 0 and the value has a 1/3 chance of incrementing
	// each block tick.
	Age	int
}
```



Fire is a non-solid block that can spread to nearby flammable blocks.



#### Fields

<TypeTable
  type={{
    replaceable: {
      description: '',
      type: 'replaceable',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    empty: {
      description: '',
      type: 'empty',
    },
    Type: {
      description: 'Type is the type of fire.',
      type: 'FireType',
      typeLink: '../block.mdx#firetype',
    },
    Age: {
      description: 'Age affects how fire extinguishes. Newly placed fire starts at 0 and the value has a 1/3 chance of incrementing each block tick.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="EncodeBlock">
```go
func (Fire) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EntityInside">
```go
func (Fire) EntityInside(_ cube.Pos, _ *world.Tx, e world.Entity)
```

EntityInside ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Fire) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Fire) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Fire) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Fire) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Fire) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Fire) NeighbourUpdateTick(pos, changedNeighbour cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Fire) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (Fire) ReplaceableBy(world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="ScheduledTick">
```go
func (Fire) ScheduledTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

ScheduledTick ...

</Accordion>

<Accordion title="Start">
```go
func (Fire) Start(tx *world.Tx, pos cube.Pos)
```

Start starts a fire at a position in the world. The position passed must be either air or tall grass and conditions
for a fire to be present must be present.

</Accordion>

</Accordions>


---

### FireDamageSource


<Callout type="info">Struct</Callout>



```go
type FireDamageSource struct{}
```



FireDamageSource is used for damage caused by being in fire.





#### Methods

<Accordions>

<Accordion title="AffectedByEnchantment">
```go
func (FireDamageSource) AffectedByEnchantment(e item.EnchantmentType) bool
```

</Accordion>

<Accordion title="Fire">
```go
func (FireDamageSource) Fire() bool
```

</Accordion>

<Accordion title="IgnoreTotem">
```go
func (FireDamageSource) IgnoreTotem() bool
```

</Accordion>

<Accordion title="ReducedByArmour">
```go
func (FireDamageSource) ReducedByArmour() bool
```

</Accordion>

<Accordion title="ReducedByResistance">
```go
func (FireDamageSource) ReducedByResistance() bool
```

</Accordion>

</Accordions>


---

### FireType


<Callout type="info">Struct</Callout>



```go
type FireType struct {
	fire
}
```



FireType represents a type of fire. Used by flaming blocks such as torches, lanterns, fire, and campfires.



#### Fields

<TypeTable
  type={{
    fire: {
      description: '',
      type: 'fire',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Damage">
```go
func (FireType) Damage() float64
```

Damage returns the amount of damage taken by entities inside the fire.

</Accordion>

<Accordion title="LightLevel">
```go
func (FireType) LightLevel() uint8
```

LightLevel returns the light level of the fire.

</Accordion>

<Accordion title="Name">
```go
func (FireType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (FireType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (FireType) Uint8() uint8
```

Uint8 returns the fire as a uint8.

</Accordion>

</Accordions>


---

### FlammabilityInfo


<Callout type="info">Struct</Callout>



```go
type FlammabilityInfo struct {
	// Encouragement is the chance a block will catch on fire during attempted fire spread.
	Encouragement	int
	// Flammability is the chance a block will burn away during a fire block tick.
	Flammability	int
	// LavaFlammable returns whether it can catch on fire from lava.
	LavaFlammable	bool
}
```



FlammabilityInfo contains values related to block behaviors involving fire.



#### Fields

<TypeTable
  type={{
    Encouragement: {
      description: 'Encouragement is the chance a block will catch on fire during attempted fire spread.',
      type: 'int',
    },
    Flammability: {
      description: 'Flammability is the chance a block will burn away during a fire block tick.',
      type: 'int',
    },
    LavaFlammable: {
      description: 'LavaFlammable returns whether it can catch on fire from lava.',
      type: 'bool',
    },
  }}
/>





---

### Flammable


<Callout type="info">Interface</Callout>



```go
type Flammable interface {
	// FlammabilityInfo returns information about a block's behavior involving fire.
	FlammabilityInfo() FlammabilityInfo
}
```



Flammable is an interface for blocks that can catch on fire.






---

### FletchingTable


<Callout type="info">Struct</Callout>



```go
type FletchingTable struct {
	solid
	bass
}
```



FletchingTable is a block in villages that turn an unemployed villager into a Fletcher.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bass: {
      description: '',
      type: 'bass',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (FletchingTable) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (FletchingTable) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (FletchingTable) EncodeItem() (string, int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (FletchingTable) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (FletchingTable) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (FletchingTable) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (FletchingTable) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Flower


<Callout type="info">Struct</Callout>



```go
type Flower struct {
	empty
	transparent

	// Type is the type of flower.
	Type	FlowerType
}
```



Flower is a non-solid plant that occur in a variety of shapes and colours. They are primarily used for decoration
and crafted into dyes.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    Type: {
      description: 'Type is the type of flower.',
      type: 'FlowerType',
      typeLink: '../block.mdx#flowertype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (Flower) BoneMeal(pos cube.Pos, tx *world.Tx) (success bool)
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Flower) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (Flower) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Flower) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Flower) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EntityInside">
```go
func (Flower) EntityInside(_ cube.Pos, _ *world.Tx, e world.Entity)
```

EntityInside ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Flower) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Flower) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Flower) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Flower) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Flower) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Flower) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Flower) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### FlowerType


<Callout type="info">Struct</Callout>



```go
type FlowerType struct {
	flower
}
```



FlowerType represents a type of flower.



#### Fields

<TypeTable
  type={{
    flower: {
      description: '',
      type: 'flower',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (FlowerType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (FlowerType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (FlowerType) Uint8() uint8
```

Uint8 returns the flower as a uint8.

</Accordion>

</Accordions>


---

### Frictional


<Callout type="info">Interface</Callout>



```go
type Frictional interface {
	// Friction returns the block's friction value.
	Friction() float64
}
```



Frictional represents a block that may have a custom friction value. Friction is used for entity drag when the
entity is on ground. If a block does not implement this interface, it should be assumed that its friction is 0.6.






---

### Froglight


<Callout type="info">Struct</Callout>



```go
type Froglight struct {
	solid

	// Type is the type of froglight.
	Type	FroglightType
	// Axis is the axis which the froglight block faces.
	Axis	cube.Axis
}
```



Froglight is a luminous natural block that can be obtained if a frog eats a tiny magma cube.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Type: {
      description: 'Type is the type of froglight.',
      type: 'FroglightType',
      typeLink: '../block.mdx#froglighttype',
    },
    Axis: {
      description: 'Axis is the axis which the froglight block faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Froglight) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Froglight) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Froglight) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Froglight) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Froglight) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Froglight) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Froglight) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### FroglightType


<Callout type="info">Struct</Callout>



```go
type FroglightType struct {
	froglight
}
```



FroglightType represents a type of froglight.



#### Fields

<TypeTable
  type={{
    froglight: {
      description: '',
      type: 'froglight',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (FroglightType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (FroglightType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (FroglightType) Uint8() uint8
```

Uint8 ...

</Accordion>

</Accordions>


---

### Furnace


<Callout type="info">Struct</Callout>



```go
type Furnace struct {
	solid
	bassDrum
	*smelter

	// Facing is the direction the furnace is facing.
	Facing	cube.Direction
	// Lit is true if the furnace is lit.
	Lit	bool
}
```



Furnace is a utility block used for the smelting of blocks and items.
The empty value of Furnace is not valid. It must be created using block.NewFurnace(cube.Face).



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    "*smelter": {
      description: '',
      type: '*smelter',
    },
    Facing: {
      description: 'Facing is the direction the furnace is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Lit: {
      description: 'Lit is true if the furnace is lit.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Furnace) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="AddViewer">
```go
func (Furnace) AddViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

AddViewer adds a viewer to the furnace, so that it is updated whenever the inventory of the furnace is changed.

</Accordion>

<Accordion title="BreakInfo">
```go
func (Furnace) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Furnace) DecodeNBT(data map[string]interface{}) interface{}
```

DecodeNBT ...

</Accordion>

<Accordion title="Durations">
```go
func (Furnace) Durations() (remaining time.Duration, max time.Duration, cook time.Duration)
```

Durations returns the remaining, maximum, and cook durations of the smelter.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Furnace) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Furnace) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Furnace) EncodeNBT() map[string]interface{}
```

EncodeNBT ...

</Accordion>

<Accordion title="Experience">
```go
func (Furnace) Experience() int
```

Experience returns the collected experience of the smelter.

</Accordion>

<Accordion title="ExtractItem">
```go
func (Furnace) ExtractItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

ExtractItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Furnace) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="InsertItem">
```go
func (Furnace) InsertItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

InsertItem ...

</Accordion>

<Accordion title="Instrument">
```go
func (Furnace) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Inventory">
```go
func (Furnace) Inventory(*world.Tx, cube.Pos) *inventory.Inventory
```

Inventory returns the inventory of the furnace.

</Accordion>

<Accordion title="Model">
```go
func (Furnace) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RemoveViewer">
```go
func (Furnace) RemoveViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

RemoveViewer removes a viewer from the furnace, so that slot updates in the inventory are no longer sent to
it.

</Accordion>

<Accordion title="ResetExperience">
```go
func (Furnace) ResetExperience() int
```

ResetExperience resets the collected experience of the smelter, and returns the amount of experience that was reset.

</Accordion>

<Accordion title="Tick">
```go
func (Furnace) Tick(_ int64, pos cube.Pos, tx *world.Tx)
```

Tick is called to check if the furnace should update and start or stop smelting.

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Furnace) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Glass


<Callout type="info">Struct</Callout>



```go
type Glass struct {
	solid
	transparent
	clicksAndSticks
}
```



Glass is a decorative, fully transparent solid block that can be dyed into stained-glass.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    clicksAndSticks: {
      description: '',
      type: 'clicksAndSticks',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Glass) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Glass) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Glass) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Glass) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Glass) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Glass) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Glass) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### GlassPane


<Callout type="info">Struct</Callout>



```go
type GlassPane struct {
	transparent
	thin
	clicksAndSticks
	sourceWaterDisplacer
}
```



GlassPane is a transparent block that can be used as a more efficient alternative to glass blocks.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    thin: {
      description: '',
      type: 'thin',
    },
    clicksAndSticks: {
      description: '',
      type: 'clicksAndSticks',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (GlassPane) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (GlassPane) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (GlassPane) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (GlassPane) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (GlassPane) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (GlassPane) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (GlassPane) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (GlassPane) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (GlassPane) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

</Accordions>


---

### GlazedTerracotta


<Callout type="info">Struct</Callout>



```go
type GlazedTerracotta struct {
	solid
	bassDrum

	// Colour specifies the colour of the block.
	Colour	item.Colour
	// Facing specifies the face of the block.
	Facing	cube.Direction
}
```



GlazedTerracotta is a vibrant solid block that comes in the 16 regular dye colours.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Colour: {
      description: 'Colour specifies the colour of the block.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
    Facing: {
      description: 'Facing specifies the face of the block.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (GlazedTerracotta) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (GlazedTerracotta) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (GlazedTerracotta) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (GlazedTerracotta) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (GlazedTerracotta) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (GlazedTerracotta) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (GlazedTerracotta) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ensures the proper facing is used when placing a glazed terracotta block, by using the opposite of the player.

</Accordion>

</Accordions>


---

### Glowstone


<Callout type="info">Struct</Callout>



```go
type Glowstone struct {
	solid
}
```



Glowstone is commonly found on the ceiling of the nether dimension.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Glowstone) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Glowstone) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Glowstone) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Glowstone) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Glowstone) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Glowstone) LightEmissionLevel() uint8
```

LightEmissionLevel returns 15.

</Accordion>

<Accordion title="Model">
```go
func (Glowstone) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Gold


<Callout type="info">Struct</Callout>



```go
type Gold struct {
	solid
}
```



Gold is a precious metal block crafted from 9 gold ingots.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Gold) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Gold) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Gold) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Gold) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Gold) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Gold) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="PowersBeacon">
```go
func (Gold) PowersBeacon() bool
```

PowersBeacon ...

</Accordion>

</Accordions>


---

### GoldOre


<Callout type="info">Struct</Callout>



```go
type GoldOre struct {
	solid
	bassDrum

	// Type is the type of gold ore.
	Type	OreType
}
```



GoldOre is a rare mineral block found underground.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of gold ore.',
      type: 'OreType',
      typeLink: '../block.mdx#oretype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (GoldOre) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (GoldOre) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (GoldOre) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (GoldOre) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (GoldOre) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (GoldOre) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (GoldOre) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### Granite




```go
type Granite polishable
```



Granite is a type of igneous rock.





#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Granite) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Granite) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Granite) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Granite) Hash() (uint64, uint64)
```

</Accordion>

</Accordions>


---

### Grass


<Callout type="info">Struct</Callout>



```go
type Grass struct {
	solid
}
```



Grass blocks generate abundantly across the surface of the world.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (Grass) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Grass) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (Grass) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Grass) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Grass) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Grass) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Grass) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Grass) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick handles the ticking of grass, which may or may not result in the spreading of grass onto dirt.

</Accordion>

<Accordion title="Shovel">
```go
func (Grass) Shovel() (world.Block, bool)
```

Shovel ...

</Accordion>

<Accordion title="SoilFor">
```go
func (Grass) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

<Accordion title="Till">
```go
func (Grass) Till() (world.Block, bool)
```

Till ...

</Accordion>

</Accordions>


---

### Gravel


<Callout type="info">Struct</Callout>



```go
type Gravel struct {
	gravityAffected
	solid
	snare
}
```



Gravel is a block affected by gravity. It has a 10% chance of dropping flint instead of itself on break.



#### Fields

<TypeTable
  type={{
    gravityAffected: {
      description: '',
      type: 'gravityAffected',
    },
    solid: {
      description: '',
      type: 'solid',
    },
    snare: {
      description: '',
      type: 'snare',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Gravel) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Gravel) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Gravel) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Gravel) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Gravel) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Gravel) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Gravel) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="Solidifies">
```go
func (Gravel) Solidifies(cube.Pos, *world.Tx) bool
```

Solidifies ...

</Accordion>

</Accordions>


---

### Grindstone


<Callout type="info">Struct</Callout>



```go
type Grindstone struct {
	transparent

	// Attach represents the attachment type of the Grindstone.
	Attach	GrindstoneAttachment
	// Facing represents the direction the Grindstone is facing.
	Facing	cube.Direction
}
```



Grindstone is a block that repairs items and tools as well as removing enchantments from them. It also serves as a
weaponsmith's job site block.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    Attach: {
      description: 'Attach represents the attachment type of the Grindstone.',
      type: 'GrindstoneAttachment',
      typeLink: '../block.mdx#grindstoneattachment',
    },
    Facing: {
      description: 'Facing represents the direction the Grindstone is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Grindstone) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Grindstone) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Grindstone) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Grindstone) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Grindstone) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Grindstone) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Grindstone) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Grindstone) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Grindstone) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### GrindstoneAttachment


<Callout type="info">Struct</Callout>



```go
type GrindstoneAttachment struct {
	grindstoneAttachment
}
```



GrindstoneAttachment represents a type of attachment for a Grindstone.



#### Fields

<TypeTable
  type={{
    grindstoneAttachment: {
      description: '',
      type: 'grindstoneAttachment',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="String">
```go
func (GrindstoneAttachment) String() string
```

String returns the GrindstoneAttachment as a string.

</Accordion>

<Accordion title="Uint8">
```go
func (GrindstoneAttachment) Uint8() uint8
```

Uint8 returns the GrindstoneAttachment as a uint8.

</Accordion>

</Accordions>


---

### HayBale


<Callout type="info">Struct</Callout>



```go
type HayBale struct {
	solid

	// Axis is the axis which the hay bale block faces.
	Axis	cube.Axis
}
```



HayBale is a decorative, flammable block that can also be used to
feed horses, breed llamas, reduce fall damage, and extend campfire smokes.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Axis: {
      description: 'Axis is the axis which the hay bale block faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (HayBale) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (HayBale) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (HayBale) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (HayBale) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EntityLand">
```go
func (HayBale) EntityLand(_ cube.Pos, _ *world.Tx, e world.Entity, distance *float64)
```

EntityLand ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (HayBale) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (HayBale) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (HayBale) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (HayBale) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (HayBale) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Honeycomb


<Callout type="info">Struct</Callout>



```go
type Honeycomb struct {
	solid
}
```



Honeycomb is a decorative blocks crafted from honeycombs.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Honeycomb) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Honeycomb) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Honeycomb) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Honeycomb) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Honeycomb) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Honeycomb) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Hopper


<Callout type="info">Struct</Callout>



```go
type Hopper struct {
	transparent
	sourceWaterDisplacer

	// Facing is the direction the hopper is facing.
	Facing	cube.Face
	// Powered is whether the hopper is powered or not. If the hopper is powered it will be locked and will stop
	// moving items into or out of itself.
	Powered	bool
	// CustomName is the custom name of the hopper. This name is displayed when the hopper is opened, and may include
	// colour codes.
	CustomName	string

	// LastTick is the last world tick that the hopper was ticked.
	LastTick	int64
	// TransferCooldown is the duration in ticks until the hopper can transfer items again.
	TransferCooldown	int64
	// CollectCooldown is the duration in ticks until the hopper can collect items again.
	CollectCooldown	int64

	inventory	*inventory.Inventory
	viewerMu	*sync.RWMutex
	viewers		map[ContainerViewer]struct{}
}
```



Hopper is a low-capacity storage block that can be used to collect item entities directly above it, as well as to
transfer items into and out of other containers.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Facing: {
      description: 'Facing is the direction the hopper is facing.',
      type: 'cube.Face',
      typeLink: '../block-cube.mdx#face',
    },
    Powered: {
      description: 'Powered is whether the hopper is powered or not. If the hopper is powered it will be locked and will stop moving items into or out of itself.',
      type: 'bool',
    },
    CustomName: {
      description: 'CustomName is the custom name of the hopper. This name is displayed when the hopper is opened, and may include colour codes.',
      type: 'string',
    },
    LastTick: {
      description: 'LastTick is the last world tick that the hopper was ticked.',
      type: 'int64',
    },
    TransferCooldown: {
      description: 'TransferCooldown is the duration in ticks until the hopper can transfer items again.',
      type: 'int64',
    },
    CollectCooldown: {
      description: 'CollectCooldown is the duration in ticks until the hopper can collect items again.',
      type: 'int64',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Hopper) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="AddViewer">
```go
func (Hopper) AddViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

AddViewer adds a viewer to the hopper, so that it is updated whenever the inventory of the hopper is changed.

</Accordion>

<Accordion title="BreakInfo">
```go
func (Hopper) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Hopper) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Hopper) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Hopper) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Hopper) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Hopper) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="Hash">
```go
func (Hopper) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Inventory">
```go
func (Hopper) Inventory(*world.Tx, cube.Pos) *inventory.Inventory
```

Inventory returns the inventory of the hopper.

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Hopper) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Hopper) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RemoveViewer">
```go
func (Hopper) RemoveViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

RemoveViewer removes a viewer from the hopper, so that slot updates in the inventory are no longer sent to it.

</Accordion>

<Accordion title="SideClosed">
```go
func (Hopper) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Tick">
```go
func (Hopper) Tick(currentTick int64, pos cube.Pos, tx *world.Tx)
```

Tick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Hopper) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

<Accordion title="WithName">
```go
func (Hopper) WithName(a ...any) world.Item
```

WithName returns the hopper after applying a specific name to the block.

</Accordion>

</Accordions>


---

### HopperExtractable


<Callout type="info">Interface</Callout>



```go
type HopperExtractable interface {
	// ExtractItem handles the extract logic for that block.
	ExtractItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
}
```



HopperExtractable represents a block that can have its contents extracted by a hopper.






---

### HopperInsertable


<Callout type="info">Interface</Callout>



```go
type HopperInsertable interface {
	// InsertItem handles the insert logic for that block.
	InsertItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
}
```



HopperInsertable represents a block that can have its contents inserted into by a hopper.






---

### InvisibleBedrock


<Callout type="info">Struct</Callout>



```go
type InvisibleBedrock struct {
	transparent
	solid
}
```



InvisibleBedrock is an indestructible, solid block, similar to bedrock and has the appearance of air.
It shares many of its properties with barriers.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="EncodeBlock">
```go
func (InvisibleBedrock) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (InvisibleBedrock) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (InvisibleBedrock) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (InvisibleBedrock) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (InvisibleBedrock) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Iron


<Callout type="info">Struct</Callout>



```go
type Iron struct {
	solid
}
```



Iron is a precious metal block made from 9 iron ingots.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Iron) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Iron) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Iron) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Iron) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Iron) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Iron) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="PowersBeacon">
```go
func (Iron) PowersBeacon() bool
```

PowersBeacon ...

</Accordion>

</Accordions>


---

### IronBars


<Callout type="info">Struct</Callout>



```go
type IronBars struct {
	transparent
	thin
	sourceWaterDisplacer
}
```



IronBars are blocks that serve a similar purpose to glass panes, but made of iron instead of glass.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    thin: {
      description: '',
      type: 'thin',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (IronBars) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (IronBars) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (IronBars) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (IronBars) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (IronBars) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (IronBars) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (IronBars) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (IronBars) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

</Accordions>


---

### IronChain


<Callout type="info">Struct</Callout>



```go
type IronChain struct {
	transparent
	sourceWaterDisplacer

	// Axis is the axis which the chain faces.
	Axis	cube.Axis
}
```



IronChain is a metallic decoration block.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Axis: {
      description: 'Axis is the axis which the chain faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (IronChain) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (IronChain) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (IronChain) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (IronChain) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (IronChain) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (IronChain) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (IronChain) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (IronChain) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (IronChain) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### IronOre


<Callout type="info">Struct</Callout>



```go
type IronOre struct {
	solid
	bassDrum

	// Type is the type of iron ore.
	Type	OreType
}
```



IronOre is a mineral block found underground.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of iron ore.',
      type: 'OreType',
      typeLink: '../block.mdx#oretype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (IronOre) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (IronOre) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (IronOre) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (IronOre) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (IronOre) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (IronOre) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (IronOre) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### ItemFrame


<Callout type="info">Struct</Callout>



```go
type ItemFrame struct {
	empty
	transparent
	sourceWaterDisplacer

	// Facing is the direction from the frame to the block.
	Facing	cube.Face
	// Item is the item that is displayed inside the frame.
	Item	item.Stack
	// Rotations is the number of rotations for the item in the frame. Each rotation is 45 degrees, with the exception
	// being maps having 90 degree rotations.
	Rotations	int
	// DropChance is the chance of the item dropping when the frame is broken. In vanilla, this is always 1.0.
	DropChance	float64
	// Glowing makes the frame the glowing variant.
	Glowing	bool
}
```



ItemFrame is a block entity that displays the item or block that is inside it.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Facing: {
      description: 'Facing is the direction from the frame to the block.',
      type: 'cube.Face',
      typeLink: '../block-cube.mdx#face',
    },
    Item: {
      description: 'Item is the item that is displayed inside the frame.',
      type: 'item.Stack',
      typeLink: '../item.mdx#stack',
    },
    Rotations: {
      description: 'Rotations is the number of rotations for the item in the frame. Each rotation is 45 degrees, with the exception being maps having 90 degree rotations.',
      type: 'int',
    },
    DropChance: {
      description: 'DropChance is the chance of the item dropping when the frame is broken. In vanilla, this is always 1.0.',
      type: 'float64',
    },
    Glowing: {
      description: 'Glowing makes the frame the glowing variant.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (ItemFrame) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, ctx *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (ItemFrame) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (ItemFrame) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (ItemFrame) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (ItemFrame) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (ItemFrame) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (ItemFrame) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="Hash">
```go
func (ItemFrame) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (ItemFrame) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (ItemFrame) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (ItemFrame) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="Pick">
```go
func (ItemFrame) Pick() item.Stack
```

Pick returns the item that is picked when the block is picked.

</Accordion>

<Accordion title="Punch">
```go
func (ItemFrame) Punch(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User)
```

Punch ...

</Accordion>

<Accordion title="SideClosed">
```go
func (ItemFrame) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (ItemFrame) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Jukebox


<Callout type="info">Struct</Callout>



```go
type Jukebox struct {
	solid
	bass

	// Item is the music disc played by the jukebox.
	Item	item.Stack
}
```



Jukebox is a block used to play music discs.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    Item: {
      description: 'Item is the music disc played by the jukebox.',
      type: 'item.Stack',
      typeLink: '../item.mdx#stack',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Jukebox) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, ctx *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Jukebox) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Jukebox) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="Disc">
```go
func (Jukebox) Disc() (sound.DiscType, bool)
```

Disc returns the currently playing music disc

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Jukebox) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Jukebox) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Jukebox) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="ExtractItem">
```go
func (Jukebox) ExtractItem(_ Hopper, _ cube.Pos, _ *world.Tx) bool
```

ExtractItem ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Jukebox) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Jukebox) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="InsertItem">
```go
func (Jukebox) InsertItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

InsertItem ...

</Accordion>

<Accordion title="Instrument">
```go
func (Jukebox) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Jukebox) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Kelp


<Callout type="info">Struct</Callout>



```go
type Kelp struct {
	empty
	transparent
	sourceWaterDisplacer

	// Age is the age of the kelp block which can be 0-25. If age is 25, kelp won't grow any further.
	Age	int
}
```



Kelp is an underwater block which can grow on top of solids underwater.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Age: {
      description: 'Age is the age of the kelp block which can be 0-25. If age is 25, kelp won\'t grow any further.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (Kelp) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Kelp) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Kelp) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CompostChance">
```go
func (Kelp) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Kelp) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Kelp) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Kelp) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Kelp) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Kelp) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Kelp) NeighbourUpdateTick(pos, changedNeighbour cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Kelp) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Kelp) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed will always return false since kelp doesn't close any side.

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Kelp) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Kelp) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Ladder


<Callout type="info">Struct</Callout>



```go
type Ladder struct {
	transparent
	sourceWaterDisplacer

	// Facing is the side of the block the ladder is currently attached to.
	Facing	cube.Direction
}
```



Ladder is a wooden block used for climbing walls either vertically or horizontally. They can be placed only on
the sides of other blocks.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Facing: {
      description: 'Facing is the side of the block the ladder is currently attached to.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Ladder) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Ladder) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Ladder) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Ladder) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EntityInside">
```go
func (Ladder) EntityInside(_ cube.Pos, _ *world.Tx, e world.Entity)
```

EntityInside ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Ladder) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Ladder) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Ladder) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Ladder) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Ladder) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Ladder) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Ladder) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Lantern


<Callout type="info">Struct</Callout>



```go
type Lantern struct {
	transparent
	sourceWaterDisplacer

	// Hanging determines if a lantern is hanging off a block.
	Hanging	bool
	// Type of fire lighting the lantern.
	Type	FireType
}
```



Lantern is a light emitting block.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Hanging: {
      description: 'Hanging determines if a lantern is hanging off a block.',
      type: 'bool',
    },
    Type: {
      description: 'Type of fire lighting the lantern.',
      type: 'FireType',
      typeLink: '../block.mdx#firetype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Lantern) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Lantern) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Lantern) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Lantern) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Lantern) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Lantern) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Lantern) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Lantern) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Lantern) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Lantern) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Lantern) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Lapis


<Callout type="info">Struct</Callout>



```go
type Lapis struct {
	solid
}
```



Lapis is a decorative mineral block that is crafted from lapis lazuli.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Lapis) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Lapis) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Lapis) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Lapis) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Lapis) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### LapisOre


<Callout type="info">Struct</Callout>



```go
type LapisOre struct {
	solid
	bassDrum

	// Type is the type of lapis ore.
	Type	OreType
}
```



LapisOre is an ore block from which lapis lazuli is obtained.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of lapis ore.',
      type: 'OreType',
      typeLink: '../block.mdx#oretype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (LapisOre) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (LapisOre) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (LapisOre) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (LapisOre) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (LapisOre) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (LapisOre) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (LapisOre) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### Lava


<Callout type="info">Struct</Callout>



```go
type Lava struct {
	empty

	// Still makes the lava not spread whenever it is updated. Still lava cannot be acquired in the game
	// without world editing.
	Still	bool
	// Depth is the depth of the water. This is a number from 1-8, where 8 is a source block and 1 is the
	// smallest possible lava block.
	Depth	int
	// Falling specifies if the lava is falling. Falling lava will always appear as a source block, but its
	// behaviour differs when it starts spreading.
	Falling	bool
}
```



Lava is a light-emitting fluid block that causes fire damage.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    Still: {
      description: 'Still makes the lava not spread whenever it is updated. Still lava cannot be acquired in the game without world editing.',
      type: 'bool',
    },
    Depth: {
      description: 'Depth is the depth of the water. This is a number from 1-8, where 8 is a source block and 1 is the smallest possible lava block.',
      type: 'int',
    },
    Falling: {
      description: 'Falling specifies if the lava is falling. Falling lava will always appear as a source block, but its behaviour differs when it starts spreading.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BlastResistance">
```go
func (Lava) BlastResistance() float64
```

BlastResistance always returns 500.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Lava) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EntityInside">
```go
func (Lava) EntityInside(_ cube.Pos, _ *world.Tx, e world.Entity)
```

EntityInside ...

</Accordion>

<Accordion title="Harden">
```go
func (Lava) Harden(pos cube.Pos, tx *world.Tx, flownIntoBy *cube.Pos) bool
```

Harden handles the hardening logic of lava.

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Lava) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Lava) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Lava) LightDiffusionLevel() uint8
```

LightDiffusionLevel always returns 2.

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Lava) LightEmissionLevel() uint8
```

LightEmissionLevel returns 15.

</Accordion>

<Accordion title="LiquidDepth">
```go
func (Lava) LiquidDepth() int
```

LiquidDepth returns the depth of the lava.

</Accordion>

<Accordion title="LiquidFalling">
```go
func (Lava) LiquidFalling() bool
```

LiquidFalling checks if the lava is falling.

</Accordion>

<Accordion title="LiquidType">
```go
func (Lava) LiquidType() string
```

LiquidType returns 10 as a unique identifier for the lava liquid.

</Accordion>

<Accordion title="Model">
```go
func (Lava) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Lava) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Lava) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (Lava) ReplaceableBy(b world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="ScheduledTick">
```go
func (Lava) ScheduledTick(pos cube.Pos, tx *world.Tx, _ *rand.Rand)
```

ScheduledTick ...

</Accordion>

<Accordion title="SpreadDecay">
```go
func (Lava) SpreadDecay() int
```

SpreadDecay always returns 2.

</Accordion>

<Accordion title="WithDepth">
```go
func (Lava) WithDepth(depth int, falling bool) world.Liquid
```

WithDepth returns a new Lava block with the depth passed and falling if set to true.

</Accordion>

</Accordions>


---

### LavaDamageSource


<Callout type="info">Struct</Callout>



```go
type LavaDamageSource struct{}
```



LavaDamageSource is used for damage caused by being in lava.





#### Methods

<Accordions>

<Accordion title="Fire">
```go
func (LavaDamageSource) Fire() bool
```

</Accordion>

<Accordion title="IgnoreTotem">
```go
func (LavaDamageSource) IgnoreTotem() bool
```

</Accordion>

<Accordion title="ReducedByArmour">
```go
func (LavaDamageSource) ReducedByArmour() bool
```

</Accordion>

<Accordion title="ReducedByResistance">
```go
func (LavaDamageSource) ReducedByResistance() bool
```

</Accordion>

</Accordions>


---

### Leaves


<Callout type="info">Struct</Callout>



```go
type Leaves struct {
	leaves
	sourceWaterDisplacer

	// Wood is the type of wood of the leaves. This field must have one of the values found in the material
	// package.
	Wood	WoodType
	// Persistent specifies if the leaves are persistent, meaning they will not decay as a result of no wood
	// being nearby.
	Persistent	bool

	ShouldUpdate	bool
}
```



Leaves are blocks that grow as part of trees which mainly drop saplings and sticks.



#### Fields

<TypeTable
  type={{
    leaves: {
      description: '',
      type: 'leaves',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Wood: {
      description: 'Wood is the type of wood of the leaves. This field must have one of the values found in the material package.',
      type: 'WoodType',
      typeLink: '../block.mdx#woodtype',
    },
    Persistent: {
      description: 'Persistent specifies if the leaves are persistent, meaning they will not decay as a result of no wood being nearby.',
      type: 'bool',
    },
    ShouldUpdate: {
      description: '',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Leaves) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Leaves) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CompostChance">
```go
func (Leaves) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Leaves) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Leaves) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Leaves) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Leaves) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Leaves) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Leaves) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Leaves) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Leaves) RandomTick(pos cube.Pos, tx *world.Tx, _ *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Leaves) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Leaves) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock makes leaves persistent when they are placed so that they don't decay.

</Accordion>

</Accordions>


---

### Lectern


<Callout type="info">Struct</Callout>



```go
type Lectern struct {
	bass
	sourceWaterDisplacer

	// Facing represents the direction the Lectern is facing.
	Facing	cube.Direction
	// Book is the book currently held by the Lectern.
	Book	item.Stack
	// Page is the page the Lectern is currently on in the book.
	Page	int
}
```



Lectern is a librarian's job site block found in villages. It is used to hold books for multiple players to read in
multiplayer.
TODO: Redstone functionality.



#### Fields

<TypeTable
  type={{
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Facing: {
      description: 'Facing represents the direction the Lectern is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Book: {
      description: 'Book is the book currently held by the Lectern.',
      type: 'item.Stack',
      typeLink: '../item.mdx#stack',
    },
    Page: {
      description: 'Page is the page the Lectern is currently on in the book.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Lectern) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, ctx *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Lectern) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Lectern) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Lectern) DecodeNBT(m map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Lectern) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Lectern) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Lectern) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Lectern) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Lectern) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Lectern) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Lectern) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="Punch">
```go
func (Lectern) Punch(pos cube.Pos, _ cube.Face, tx *world.Tx, _ item.User)
```

Punch ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Lectern) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="TurnPage">
```go
func (Lectern) TurnPage(pos cube.Pos, tx *world.Tx, page int) error
```

TurnPage updates the page the lectern is currently on to the page given.

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Lectern) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Light


<Callout type="info">Struct</Callout>



```go
type Light struct {
	empty
	replaceable
	transparent
	flowingWaterDisplacer

	// Level is the light level that the light block produces. It is a number from 0-15, where 15 is the
	// brightest and 0 is no light at all.
	Level	int
}
```



Light is an invisible block that can produce any light level.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    replaceable: {
      description: '',
      type: 'replaceable',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    flowingWaterDisplacer: {
      description: '',
      type: 'flowingWaterDisplacer',
    },
    Level: {
      description: 'Level is the light level that the light block produces. It is a number from 0-15, where 15 is the brightest and 0 is no light at all.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="CanDisplace">
```go
func (Light) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Light) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Light) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Light) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Light) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Light) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Light) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (Light) ReplaceableBy(world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Light) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

</Accordions>


---

### LightDiffuser


<Callout type="info">Interface</Callout>



```go
type LightDiffuser interface {
	// LightDiffusionLevel returns the amount of light levels that is subtracted when light passes through
	// this block. Some blocks, such as leaves, have this behaviour. A diffusion level of 15 means that all
	// light will be completely blocked when it passes through the block.
	LightDiffusionLevel() uint8
}
```



LightDiffuser represents a block that diffuses light. This means that a specific amount of light levels
will be subtracted when light passes through the block.
Blocks that do not implement LightDiffuser will be assumed to be solid: Light will not be able to pass
through these blocks.






---

### LightEmitter


<Callout type="info">Interface</Callout>



```go
type LightEmitter interface {
	// LightEmissionLevel returns the light emission level of the block, a number from 0-15 where 15 is the
	// brightest and 0 means it doesn't emit light at all.
	LightEmissionLevel() uint8
}
```



LightEmitter represents a block that emits light when placed. Blocks such as torches or lanterns implement
this interface.






---

### LilyPad


<Callout type="info">Struct</Callout>



```go
type LilyPad struct {
	transparent
}
```



LilyPad is a short, flat solid block that can be found naturally growing only on water, in swamps and wheat
farm rooms in woodland mansions.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (LilyPad) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (LilyPad) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (LilyPad) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (LilyPad) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (LilyPad) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (LilyPad) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (LilyPad) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (LilyPad) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (LilyPad) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (LilyPad) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### LiquidRemovable


<Callout type="info">Interface</Callout>



```go
type LiquidRemovable interface {
	HasLiquidDrops() bool
}
```



LiquidRemovable represents a block that may be removed by a liquid flowing into it. When this happens, the
block's drops are dropped at the position if HasLiquidDrops returns true.






---

### LitPumpkin


<Callout type="info">Struct</Callout>



```go
type LitPumpkin struct {
	solid

	// Facing is the direction the pumpkin is facing.
	Facing	cube.Direction
}
```



LitPumpkin is a decorative light emitting block crafted with a Carved Pumpkin & Torch



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Facing: {
      description: 'Facing is the direction the pumpkin is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (LitPumpkin) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (LitPumpkin) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (LitPumpkin) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (LitPumpkin) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (LitPumpkin) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (LitPumpkin) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (LitPumpkin) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Log


<Callout type="info">Struct</Callout>



```go
type Log struct {
	solid
	bass

	// Wood is the type of wood of the log. This field must have one of the values found in the material
	// package.
	Wood	WoodType
	// Stripped specifies if the log is stripped or not.
	Stripped	bool
	// Axis is the axis which the log block faces.
	Axis	cube.Axis
}
```



Log is a naturally occurring block found in trees, primarily used to create planks. It comes in six
species: oak, spruce, birch, jungle, acacia, and dark oak.
Stripped log is a variant obtained by using an axe on a log.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    Wood: {
      description: 'Wood is the type of wood of the log. This field must have one of the values found in the material package.',
      type: 'WoodType',
      typeLink: '../block.mdx#woodtype',
    },
    Stripped: {
      description: 'Stripped specifies if the log is stripped or not.',
      type: 'bool',
    },
    Axis: {
      description: 'Axis is the axis which the log block faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Log) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Log) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Log) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Log) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Log) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Log) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Log) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Log) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Log) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="Strip">
```go
func (Log) Strip() (world.Block, world.Sound, bool)
```

Strip ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Log) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock handles the rotational placing of logs.

</Accordion>

</Accordions>


---

### Loom


<Callout type="info">Struct</Callout>



```go
type Loom struct {
	solid
	bass

	// Facing is the direction the loom is facing.
	Facing	cube.Direction
}
```



Loom is a block used to apply patterns on banners. It is also used as a shepherd's job site block that is found in
villages.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    Facing: {
      description: 'Facing is the direction the loom is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Loom) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Loom) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Loom) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Loom) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Loom) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Loom) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Loom) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Loom) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Loom) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Melon


<Callout type="info">Struct</Callout>



```go
type Melon struct {
	solid
}
```



Melon is a fruit block that grows from melon stems.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Melon) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (Melon) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Melon) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Melon) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Melon) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Melon) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### MelonSeeds


<Callout type="info">Struct</Callout>



```go
type MelonSeeds struct {
	crop

	// direction is the direction from the stem to the melon.
	Direction	cube.Face
}
```



MelonSeeds grow melon blocks.



#### Fields

<TypeTable
  type={{
    crop: {
      description: '',
      type: 'crop',
    },
    Direction: {
      description: 'direction is the direction from the stem to the melon.',
      type: 'cube.Face',
      typeLink: '../block-cube.mdx#face',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (MelonSeeds) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (MelonSeeds) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CalculateGrowthChance">
```go
func (MelonSeeds) CalculateGrowthChance(pos cube.Pos, tx *world.Tx) float64
```

CalculateGrowthChance calculates the chance the crop will grow during a random tick.

</Accordion>

<Accordion title="CompostChance">
```go
func (MelonSeeds) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (MelonSeeds) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (MelonSeeds) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="GrowthStage">
```go
func (MelonSeeds) GrowthStage() int
```

GrowthStage returns the current stage of growth.

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (MelonSeeds) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (MelonSeeds) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (MelonSeeds) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (MelonSeeds) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (MelonSeeds) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (MelonSeeds) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SameCrop">
```go
func (MelonSeeds) SameCrop(c Crop) bool
```

SameCrop ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (MelonSeeds) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### MossCarpet


<Callout type="info">Struct</Callout>



```go
type MossCarpet struct {
	carpet
	transparent
	sourceWaterDisplacer
}
```



MossCarpet is a thin decorative variant of the moss block.



#### Fields

<TypeTable
  type={{
    carpet: {
      description: '',
      type: 'carpet',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (MossCarpet) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (MossCarpet) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CompostChance">
```go
func (MossCarpet) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (MossCarpet) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (MossCarpet) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (MossCarpet) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (MossCarpet) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (MossCarpet) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (MossCarpet) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (MossCarpet) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (MossCarpet) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (MossCarpet) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Mud


<Callout type="info">Struct</Callout>



```go
type Mud struct {
	solid
}
```



Mud is a decorative block obtained by using a water bottle on a dirt block.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Mud) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Mud) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Mud) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Mud) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Mud) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SoilFor">
```go
func (Mud) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

</Accordions>


---

### MudBricks


<Callout type="info">Struct</Callout>



```go
type MudBricks struct {
	solid
	bassDrum
}
```



MudBricks are a decorative block obtained by crafting 4 packed mud.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (MudBricks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (MudBricks) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (MudBricks) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (MudBricks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (MudBricks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (MudBricks) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### MuddyMangroveRoots


<Callout type="info">Struct</Callout>



```go
type MuddyMangroveRoots struct {
	solid

	// Axis is the axis which the muddy mangrove roots faces.
	Axis	cube.Axis
}
```



MuddyMangroveRoots are a decorative variant of mangrove roots.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Axis: {
      description: 'Axis is the axis which the muddy mangrove roots faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (MuddyMangroveRoots) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (MuddyMangroveRoots) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (MuddyMangroveRoots) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (MuddyMangroveRoots) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (MuddyMangroveRoots) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SoilFor">
```go
func (MuddyMangroveRoots) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (MuddyMangroveRoots) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### NetherBrickFence


<Callout type="info">Struct</Callout>



```go
type NetherBrickFence struct {
	transparent
	sourceWaterDisplacer
}
```



NetherBrickFence is the nether brick variant of the fence block.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (NetherBrickFence) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (NetherBrickFence) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (NetherBrickFence) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (NetherBrickFence) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (NetherBrickFence) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (NetherBrickFence) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (NetherBrickFence) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (NetherBrickFence) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

</Accordions>


---

### NetherBricks


<Callout type="info">Struct</Callout>



```go
type NetherBricks struct {
	solid
	bassDrum

	// NetherBricksType is the type of nether bricks of the block.
	Type	NetherBricksType
}
```



NetherBricks are blocks used to form nether fortresses in the Nether.
Red Nether bricks, Cracked Nether bricks and Chiseled Nether bricks are decorative variants that do not naturally generate.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'NetherBricksType is the type of nether bricks of the block.',
      type: 'NetherBricksType',
      typeLink: '../block.mdx#netherbrickstype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (NetherBricks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (NetherBricks) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (NetherBricks) EncodeItem() (id string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (NetherBricks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (NetherBricks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (NetherBricks) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (NetherBricks) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### NetherBricksType


<Callout type="info">Struct</Callout>



```go
type NetherBricksType struct {
	netherBricks
}
```



NetherBricksType represents a type of nether bricks.



#### Fields

<TypeTable
  type={{
    netherBricks: {
      description: '',
      type: 'netherBricks',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (NetherBricksType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (NetherBricksType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (NetherBricksType) Uint8() uint8
```

Uint8 returns the nether bricks as a unit8.

</Accordion>

</Accordions>


---

### NetherGoldOre


<Callout type="info">Struct</Callout>



```go
type NetherGoldOre struct {
	solid
}
```



NetherGoldOre is a variant of gold ore found exclusively in The Nether.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (NetherGoldOre) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (NetherGoldOre) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (NetherGoldOre) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (NetherGoldOre) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (NetherGoldOre) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (NetherGoldOre) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### NetherQuartzOre


<Callout type="info">Struct</Callout>



```go
type NetherQuartzOre struct {
	solid
	bassDrum
}
```



NetherQuartzOre is ore found in the Nether.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (NetherQuartzOre) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (NetherQuartzOre) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (NetherQuartzOre) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (NetherQuartzOre) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (NetherQuartzOre) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (NetherQuartzOre) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (NetherQuartzOre) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### NetherSprouts


<Callout type="info">Struct</Callout>



```go
type NetherSprouts struct {
	transparent
	replaceable
	empty
}
```



NetherSprouts are a non-solid plant block that generate in warped forests.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    replaceable: {
      description: '',
      type: 'replaceable',
    },
    empty: {
      description: '',
      type: 'empty',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (NetherSprouts) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (NetherSprouts) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (NetherSprouts) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (NetherSprouts) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (NetherSprouts) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (NetherSprouts) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (NetherSprouts) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (NetherSprouts) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (NetherSprouts) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (NetherSprouts) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (NetherSprouts) ReplaceableBy(world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (NetherSprouts) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### NetherWart


<Callout type="info">Struct</Callout>



```go
type NetherWart struct {
	transparent
	empty

	// Age is the age of the nether wart block. 3 is fully grown.
	Age	int
}
```



NetherWart is a fungus found in the Nether that is vital in the creation of potions.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    empty: {
      description: '',
      type: 'empty',
    },
    Age: {
      description: 'Age is the age of the nether wart block. 3 is fully grown.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (NetherWart) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (NetherWart) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (NetherWart) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (NetherWart) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (NetherWart) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (NetherWart) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (NetherWart) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (NetherWart) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (NetherWart) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (NetherWart) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (NetherWart) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### NetherWartBlock


<Callout type="info">Struct</Callout>



```go
type NetherWartBlock struct {
	solid

	// Warped is the turquoise variant found in warped forests, but cannot be crafted unlike Nether wart block.
	Warped	bool
}
```



NetherWartBlock is a decorative block found in crimson forests and crafted using Nether wart.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Warped: {
      description: 'Warped is the turquoise variant found in warped forests, but cannot be crafted unlike Nether wart block.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (NetherWartBlock) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (NetherWartBlock) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (NetherWartBlock) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (NetherWartBlock) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (NetherWartBlock) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (NetherWartBlock) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Netherite


<Callout type="info">Struct</Callout>



```go
type Netherite struct {
	solid
	bassDrum
}
```



Netherite is a precious mineral block made from 9 netherite ingots.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Netherite) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Netherite) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Netherite) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Netherite) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Netherite) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Netherite) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="PowersBeacon">
```go
func (Netherite) PowersBeacon() bool
```

PowersBeacon ...

</Accordion>

</Accordions>


---

### Netherrack


<Callout type="info">Struct</Callout>



```go
type Netherrack struct {
	solid
	bassDrum
}
```



Netherrack is a block found in The Nether.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Netherrack) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Netherrack) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Netherrack) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Netherrack) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Netherrack) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Netherrack) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Netherrack) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="SoilFor">
```go
func (Netherrack) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

</Accordions>


---

### Note


<Callout type="info">Struct</Callout>



```go
type Note struct {
	solid
	bass

	// Pitch is the current pitch the note block is set to. Value ranges from 0-24.
	Pitch	int
}
```



Note is a musical block that emits sounds when powered with redstone.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    Pitch: {
      description: 'Pitch is the current pitch the note block is set to. Value ranges from 0-24.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Note) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, _ item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Note) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Note) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Note) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Note) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Note) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Note) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Note) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Note) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Note) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Obsidian


<Callout type="info">Struct</Callout>



```go
type Obsidian struct {
	solid
	bassDrum
	// Crying specifies if the block is a crying obsidian block. If true, the block is blue and emits light.
	Crying	bool
}
```



Obsidian is a dark purple block known for its high blast resistance and strength, most commonly found when
water flows over lava.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Crying: {
      description: 'Crying specifies if the block is a crying obsidian block. If true, the block is blue and emits light.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Obsidian) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Obsidian) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Obsidian) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Obsidian) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Obsidian) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Obsidian) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Obsidian) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### OpenAction


<Callout type="info">Struct</Callout>



```go
type OpenAction struct{ action }
```



OpenAction is a world.BlockAction to open a block at a position. It is sent for blocks such as chests.



#### Fields

<TypeTable
  type={{
    action: {
      description: '',
      type: 'action',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BlockAction">
```go
func (OpenAction) BlockAction()
```

BlockAction serves to implement the world.BlockAction interface.

</Accordion>

</Accordions>


---

### OreType


<Callout type="info">Struct</Callout>



```go
type OreType struct {
	ore
}
```



OreType represents a variant of ore blocks.



#### Fields

<TypeTable
  type={{
    ore: {
      description: '',
      type: 'ore',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Hardness">
```go
func (OreType) Hardness() float64
```

Hardness ...

</Accordion>

<Accordion title="Name">
```go
func (OreType) Name() string
```

Name ...

</Accordion>

<Accordion title="Prefix">
```go
func (OreType) Prefix() string
```

Prefix ...

</Accordion>

<Accordion title="String">
```go
func (OreType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (OreType) Uint8() uint8
```

Uint8 returns the ore as a uint8.

</Accordion>

</Accordions>


---

### OxidationType


<Callout type="info">Struct</Callout>



```go
type OxidationType struct {
	oxidation
}
```



OxidationType represents a type of oxidation.



#### Fields

<TypeTable
  type={{
    oxidation: {
      description: '',
      type: 'oxidation',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Decrease">
```go
func (OxidationType) Decrease() (OxidationType, bool)
```

Decrease attempts to decrease the oxidation level by one. It returns the new oxidation level and if the
decrease was successful.

</Accordion>

<Accordion title="Increase">
```go
func (OxidationType) Increase() (OxidationType, bool)
```

Increase attempts to increase the oxidation level by one. It returns the new oxidation level and if the
increase was successful.

</Accordion>

<Accordion title="Name">
```go
func (OxidationType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (OxidationType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (OxidationType) Uint8() uint8
```

Uint8 returns the oxidation as a uint8.

</Accordion>

</Accordions>


---

### Oxidisable


<Callout type="info">Interface</Callout>



```go
type Oxidisable interface {
	world.Block
	// CanOxidate returns whether the block can oxidate, i.e. if it's not waxed.
	CanOxidate() bool
	// OxidationLevel returns the currently level of oxidation of the block.
	OxidationLevel() OxidationType
	// WithOxidationLevel returns the oxidizable block with the oxidation level passed.
	WithOxidationLevel(OxidationType) Oxidisable
}
```



Oxidisable is a block that can naturally oxidise over time, such as copper.






---

### PackedIce


<Callout type="info">Struct</Callout>



```go
type PackedIce struct {
	solid
}
```



PackedIce is an opaque solid block variant of ice. Unlike regular ice, it does not melt near bright light sources.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (PackedIce) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (PackedIce) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (PackedIce) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Friction">
```go
func (PackedIce) Friction() float64
```

Friction ...

</Accordion>

<Accordion title="Hash">
```go
func (PackedIce) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (PackedIce) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (PackedIce) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### PackedMud


<Callout type="info">Struct</Callout>



```go
type PackedMud struct {
	solid
}
```



PackedMud is a block crafted from mud and wheat. It is used to create mud bricks.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (PackedMud) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (PackedMud) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (PackedMud) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (PackedMud) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (PackedMud) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Permutable


<Callout type="info">Interface</Callout>



```go
type Permutable interface {
	// States returns a map of all the different properties for the block. The key is the property name, and the value
	// is a slice of all the possible values for that property. It is important that a block is registered in dragonfly
	// for each of the possible combinations of properties and values.
	States() map[string][]any
	// Permutations returns a slice of all the different permutations for the block. Multiple permutations can be
	// applied at once if their conditions are met.
	Permutations() []customblock.Permutation
}
```



Permutable represents a custom block that can have more permutations than its default state.






---

### Pickable


<Callout type="info">Interface</Callout>



```go
type Pickable interface {
	// Pick returns the item that is picked when the block is picked.
	Pick() item.Stack
}
```



Pickable represents a block that may give a different item then the block itself when picked.






---

### PinkPetals


<Callout type="info">Struct</Callout>



```go
type PinkPetals struct {
	empty
	transparent

	// AdditionalCount is the amount of additional pink petals. This can range
	// from 0-7, where only 0-3 can occur in-game.
	AdditionalCount	int
	// Facing is the direction the pink petals are facing. This is opposite to
	// the direction the player is facing when placing.
	Facing	cube.Direction
}
```



PinkPetals is a decorative block that generates in cherry grove biomes.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    AdditionalCount: {
      description: 'AdditionalCount is the amount of additional pink petals. This can range from 0-7, where only 0-3 can occur in-game.',
      type: 'int',
    },
    Facing: {
      description: 'Facing is the direction the pink petals are facing. This is opposite to the direction the player is facing when placing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (PinkPetals) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (PinkPetals) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (PinkPetals) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (PinkPetals) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (PinkPetals) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (PinkPetals) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (PinkPetals) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (PinkPetals) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (PinkPetals) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (PinkPetals) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (PinkPetals) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (PinkPetals) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Placer


<Callout type="info">Interface</Callout>



```go
type Placer interface {
	item.User
	PlaceBlock(pos cube.Pos, b world.Block, ctx *item.UseContext)
}
```



Placer represents an entity that is able to place a block at a specific position in the world.






---

### Planks


<Callout type="info">Struct</Callout>



```go
type Planks struct {
	solid
	bass

	// Wood is the type of wood of the planks. This field must have one of the values found in the material
	// package.
	Wood	WoodType
}
```



Planks are common blocks used in crafting recipes. They are made by crafting logs into planks.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    Wood: {
      description: 'Wood is the type of wood of the planks. This field must have one of the values found in the material package.',
      type: 'WoodType',
      typeLink: '../block.mdx#woodtype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Planks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Planks) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Planks) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Planks) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Planks) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Planks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Planks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Planks) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RepairsWoodTools">
```go
func (Planks) RepairsWoodTools() bool
```

RepairsWoodTools ...

</Accordion>

</Accordions>


---

### Podzol


<Callout type="info">Struct</Callout>



```go
type Podzol struct {
	solid
}
```



Podzol is a dirt-type block that naturally blankets the surface of the giant tree taiga and bamboo jungles, along
with their respective variants.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Podzol) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Podzol) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Podzol) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Podzol) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Podzol) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="Shovel">
```go
func (Podzol) Shovel() (world.Block, bool)
```

Shovel ...

</Accordion>

<Accordion title="SoilFor">
```go
func (Podzol) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

</Accordions>


---

### PolishedBlackstoneBrick


<Callout type="info">Struct</Callout>



```go
type PolishedBlackstoneBrick struct {
	solid
	bassDrum

	// Cracked specifies if the polished blackstone bricks is its cracked variant.
	Cracked	bool
}
```



PolishedBlackstoneBrick are a brick variant of polished blackstone and can spawn in bastion remnants and ruined portals.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Cracked: {
      description: 'Cracked specifies if the polished blackstone bricks is its cracked variant.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (PolishedBlackstoneBrick) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (PolishedBlackstoneBrick) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (PolishedBlackstoneBrick) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (PolishedBlackstoneBrick) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (PolishedBlackstoneBrick) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (PolishedBlackstoneBrick) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (PolishedBlackstoneBrick) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### PolishedTuff


<Callout type="info">Struct</Callout>



```go
type PolishedTuff struct {
	solid
	bassDrum
}
```



PolishedTuff is a decorational variant of Tuff that can be crafted or found naturally in Trial Chambers.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (PolishedTuff) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (PolishedTuff) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (PolishedTuff) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (PolishedTuff) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (PolishedTuff) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (PolishedTuff) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### PotDecoration


<Callout type="info">Interface</Callout>



```go
type PotDecoration interface {
	world.Item
	PotDecoration() bool
}
```



PotDecoration represents an item that can be used as a decoration on a pot.






---

### Potato


<Callout type="info">Struct</Callout>



```go
type Potato struct {
	crop
}
```



Potato is a crop that can be consumed raw or cooked to make baked potatoes.



#### Fields

<TypeTable
  type={{
    crop: {
      description: '',
      type: 'crop',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="AlwaysConsumable">
```go
func (Potato) AlwaysConsumable() bool
```

AlwaysConsumable ...

</Accordion>

<Accordion title="BoneMeal">
```go
func (Potato) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Potato) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CalculateGrowthChance">
```go
func (Potato) CalculateGrowthChance(pos cube.Pos, tx *world.Tx) float64
```

CalculateGrowthChance calculates the chance the crop will grow during a random tick.

</Accordion>

<Accordion title="CompostChance">
```go
func (Potato) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="Consume">
```go
func (Potato) Consume(_ *world.Tx, c item.Consumer) item.Stack
```

Consume ...

</Accordion>

<Accordion title="ConsumeDuration">
```go
func (Potato) ConsumeDuration() time.Duration
```

ConsumeDuration ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Potato) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Potato) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="GrowthStage">
```go
func (Potato) GrowthStage() int
```

GrowthStage returns the current stage of growth.

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Potato) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Potato) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Potato) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Potato) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Potato) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Potato) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SameCrop">
```go
func (Potato) SameCrop(c Crop) bool
```

SameCrop ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Potato) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Potato) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Prismarine


<Callout type="info">Struct</Callout>



```go
type Prismarine struct {
	solid
	bassDrum

	// Type is the type of prismarine of the block.
	Type	PrismarineType
}
```



Prismarine is a type of stone that appears underwater in ruins and ocean monuments.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of prismarine of the block.',
      type: 'PrismarineType',
      typeLink: '../block.mdx#prismarinetype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Prismarine) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Prismarine) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Prismarine) EncodeItem() (id string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Prismarine) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Prismarine) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Prismarine) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### PrismarineType


<Callout type="info">Struct</Callout>



```go
type PrismarineType struct {
	prismarine
}
```



PrismarineType represents a type of prismarine.



#### Fields

<TypeTable
  type={{
    prismarine: {
      description: '',
      type: 'prismarine',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (PrismarineType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (PrismarineType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (PrismarineType) Uint8() uint8
```

Uint8 returns the prismarine as a uint8.

</Accordion>

</Accordions>


---

### ProjectileHitter


<Callout type="info">Interface</Callout>



```go
type ProjectileHitter interface {
	// ProjectileHit is called when a projectile hits the block.
	ProjectileHit(pos cube.Pos, tx *world.Tx, e world.Entity, face cube.Face)
}
```



ProjectileHitter represents a block that handles being hit by a projectile.






---

### Pumpkin


<Callout type="info">Struct</Callout>



```go
type Pumpkin struct {
	solid

	// Carved is whether the pumpkin is carved.
	Carved	bool
	// Facing is the direction the pumpkin is facing.
	Facing	cube.Direction
}
```



Pumpkin is a crop block. Interacting with shears results in the carved variant.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Carved: {
      description: 'Carved is whether the pumpkin is carved.',
      type: 'bool',
    },
    Facing: {
      description: 'Facing is the direction the pumpkin is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Pumpkin) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="Carve">
```go
func (Pumpkin) Carve(f cube.Face) (world.Block, bool)
```

Carve ...

</Accordion>

<Accordion title="CompostChance">
```go
func (Pumpkin) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="DefencePoints">
```go
func (Pumpkin) DefencePoints() float64
```

DefencePoints ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Pumpkin) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Pumpkin) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Pumpkin) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Helmet">
```go
func (Pumpkin) Helmet() bool
```

Helmet ...

</Accordion>

<Accordion title="Instrument">
```go
func (Pumpkin) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="KnockBackResistance">
```go
func (Pumpkin) KnockBackResistance() float64
```

KnockBackResistance ...

</Accordion>

<Accordion title="Model">
```go
func (Pumpkin) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="Toughness">
```go
func (Pumpkin) Toughness() float64
```

Toughness ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Pumpkin) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### PumpkinSeeds


<Callout type="info">Struct</Callout>



```go
type PumpkinSeeds struct {
	crop

	// Direction is the direction from the stem to the pumpkin.
	Direction	cube.Face
}
```



PumpkinSeeds grow pumpkin blocks.



#### Fields

<TypeTable
  type={{
    crop: {
      description: '',
      type: 'crop',
    },
    Direction: {
      description: 'Direction is the direction from the stem to the pumpkin.',
      type: 'cube.Face',
      typeLink: '../block-cube.mdx#face',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (PumpkinSeeds) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (PumpkinSeeds) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CalculateGrowthChance">
```go
func (PumpkinSeeds) CalculateGrowthChance(pos cube.Pos, tx *world.Tx) float64
```

CalculateGrowthChance calculates the chance the crop will grow during a random tick.

</Accordion>

<Accordion title="CompostChance">
```go
func (PumpkinSeeds) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (PumpkinSeeds) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (PumpkinSeeds) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="GrowthStage">
```go
func (PumpkinSeeds) GrowthStage() int
```

GrowthStage returns the current stage of growth.

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (PumpkinSeeds) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (PumpkinSeeds) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (PumpkinSeeds) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (PumpkinSeeds) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (PumpkinSeeds) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (PumpkinSeeds) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SameCrop">
```go
func (PumpkinSeeds) SameCrop(c Crop) bool
```

SameCrop ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (PumpkinSeeds) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Punchable


<Callout type="info">Interface</Callout>



```go
type Punchable interface {
	// Punch punches the block at a specific block position. The face clicked is passed, as well as the
	// world in which the block was punched and the viewer that punched it.
	Punch(pos cube.Pos, clickedFace cube.Face, tx *world.Tx, u item.User)
}
```



Punchable represents a block that may be punched by a viewer of the world. When punched, the block
will execute some specific logic.






---

### Purpur


<Callout type="info">Struct</Callout>



```go
type Purpur struct {
	solid
	bassDrum
}
```



Purpur is a decorative block that is naturally generated in End cities and End ships.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Purpur) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Purpur) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Purpur) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Purpur) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Purpur) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Purpur) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### PurpurPillar


<Callout type="info">Struct</Callout>



```go
type PurpurPillar struct {
	solid
	bassDrum

	// Axis is the axis which the purpur pillar block faces.
	Axis	cube.Axis
}
```



PurpurPillar is a variant of Purpur that can be rotated.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Axis: {
      description: 'Axis is the axis which the purpur pillar block faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (PurpurPillar) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (PurpurPillar) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (PurpurPillar) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (PurpurPillar) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (PurpurPillar) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (PurpurPillar) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (PurpurPillar) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Quartz


<Callout type="info">Struct</Callout>



```go
type Quartz struct {
	solid
	bassDrum
	// Smooth specifies if the quartz block is smooth or not.
	Smooth	bool
}
```



Quartz is a mineral block used only for decoration.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Smooth: {
      description: 'Smooth specifies if the quartz block is smooth or not.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Quartz) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Quartz) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Quartz) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Quartz) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Quartz) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Quartz) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Quartz) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### QuartzBricks


<Callout type="info">Struct</Callout>



```go
type QuartzBricks struct {
	solid
	bassDrum
}
```



QuartzBricks is a mineral block used only for decoration.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (QuartzBricks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (QuartzBricks) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (QuartzBricks) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (QuartzBricks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (QuartzBricks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (QuartzBricks) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### QuartzPillar


<Callout type="info">Struct</Callout>



```go
type QuartzPillar struct {
	solid
	bassDrum
	// Axis is the axis which the quartz pillar block faces.
	Axis	cube.Axis
}
```



QuartzPillar is a mineral block used only for decoration.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Axis: {
      description: 'Axis is the axis which the quartz pillar block faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (QuartzPillar) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (QuartzPillar) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (QuartzPillar) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (QuartzPillar) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (QuartzPillar) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (QuartzPillar) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (QuartzPillar) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock handles the rotational placing of quartz pillar blocks.

</Accordion>

</Accordions>


---

### RawCopper


<Callout type="info">Struct</Callout>



```go
type RawCopper struct {
	solid
	bassDrum
}
```



RawCopper is a raw metal block equivalent to nine raw copper.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (RawCopper) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (RawCopper) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (RawCopper) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (RawCopper) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (RawCopper) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (RawCopper) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### RawGold


<Callout type="info">Struct</Callout>



```go
type RawGold struct {
	solid
	bassDrum
}
```



RawGold is a raw metal block equivalent to nine raw gold.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (RawGold) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (RawGold) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (RawGold) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (RawGold) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (RawGold) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (RawGold) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### RawIron


<Callout type="info">Struct</Callout>



```go
type RawIron struct {
	solid
	bassDrum
}
```



RawIron is a raw metal block equivalent to nine raw iron.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (RawIron) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (RawIron) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (RawIron) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (RawIron) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (RawIron) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (RawIron) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### ReinforcedDeepslate


<Callout type="info">Struct</Callout>



```go
type ReinforcedDeepslate struct {
	solid
	bassDrum
}
```



ReinforcedDeepslate is a tough decorative block that spawns in ancient cities.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (ReinforcedDeepslate) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (ReinforcedDeepslate) EncodeBlock() (string, map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (ReinforcedDeepslate) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (ReinforcedDeepslate) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (ReinforcedDeepslate) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (ReinforcedDeepslate) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Replaceable


<Callout type="info">Interface</Callout>



```go
type Replaceable interface {
	// ReplaceableBy returns a bool which indicates if the block is replaceableWith by another block.
	ReplaceableBy(b world.Block) bool
}
```



Replaceable represents a block that may be replaced by another block automatically. An example is grass,
which may be replaced by clicking it with another block.






---

### Resin


<Callout type="info">Struct</Callout>



```go
type Resin struct {
	solid
}
```



Resin is a block equivalent to nine resin clumps.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Resin) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Resin) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Resin) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Resin) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Resin) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### ResinBricks


<Callout type="info">Struct</Callout>



```go
type ResinBricks struct {
	solid
	bassDrum

	// Chiseled specifies if the resin bricks is its chiseled variant.
	Chiseled	bool
}
```



ResinBricks is a block crafted from resin brick.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Chiseled: {
      description: 'Chiseled specifies if the resin bricks is its chiseled variant.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (ResinBricks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (ResinBricks) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (ResinBricks) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (ResinBricks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (ResinBricks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (ResinBricks) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Sand


<Callout type="info">Struct</Callout>



```go
type Sand struct {
	gravityAffected
	solid
	snare

	// Red toggles the red sand variant.
	Red	bool
}
```



Sand is a block affected by gravity. It can come in a red variant.



#### Fields

<TypeTable
  type={{
    gravityAffected: {
      description: '',
      type: 'gravityAffected',
    },
    solid: {
      description: '',
      type: 'solid',
    },
    snare: {
      description: '',
      type: 'snare',
    },
    Red: {
      description: 'Red toggles the red sand variant.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Sand) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Sand) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Sand) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Sand) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Sand) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Sand) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Sand) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Sand) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="SoilFor">
```go
func (Sand) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

<Accordion title="Solidifies">
```go
func (Sand) Solidifies(cube.Pos, *world.Tx) bool
```

Solidifies ...

</Accordion>

</Accordions>


---

### Sandstone


<Callout type="info">Struct</Callout>



```go
type Sandstone struct {
	solid
	bassDrum

	// Type is the type of sandstone of the block.
	Type	SandstoneType

	// Red specifies if the sandstone type is red or not. When set to true, the sandstone type will represent its
	// red variant, for example red sandstone.
	Red	bool
}
```



Sandstone is a solid block commonly found in deserts and beaches underneath sand.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of sandstone of the block.',
      type: 'SandstoneType',
      typeLink: '../block.mdx#sandstonetype',
    },
    Red: {
      description: 'Red specifies if the sandstone type is red or not. When set to true, the sandstone type will represent its red variant, for example red sandstone.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Sandstone) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Sandstone) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Sandstone) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Sandstone) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Sandstone) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Sandstone) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Sandstone) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### SandstoneType


<Callout type="info">Struct</Callout>



```go
type SandstoneType struct {
	sandstone
}
```



SandstoneType represents a type of sandstone.



#### Fields

<TypeTable
  type={{
    sandstone: {
      description: '',
      type: 'sandstone',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (SandstoneType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (SandstoneType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (SandstoneType) Uint8() uint8
```

Uint8 returns the sandstone as a uint8.

</Accordion>

</Accordions>


---

### SeaLantern


<Callout type="info">Struct</Callout>



```go
type SeaLantern struct {
	transparent
	solid
	clicksAndSticks
}
```



SeaLantern is an underwater light sources that appear in ocean monuments and underwater ruins.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    solid: {
      description: '',
      type: 'solid',
    },
    clicksAndSticks: {
      description: '',
      type: 'clicksAndSticks',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (SeaLantern) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (SeaLantern) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (SeaLantern) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (SeaLantern) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (SeaLantern) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (SeaLantern) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (SeaLantern) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (SeaLantern) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### SeaPickle


<Callout type="info">Struct</Callout>



```go
type SeaPickle struct {
	empty
	transparent
	sourceWaterDisplacer

	// AdditionalCount is the amount of additional sea pickles clustered together.
	AdditionalCount	int
	// Dead is whether the sea pickles are not alive. Sea pickles are only considered alive when inside of water. While
	// alive, sea pickles emit light & can be grown with bone meal.
	Dead	bool
}
```



SeaPickle is a small stationary underwater block that emits light, and is typically found in colonies of up to
four sea pickles.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    AdditionalCount: {
      description: 'AdditionalCount is the amount of additional sea pickles clustered together.',
      type: 'int',
    },
    Dead: {
      description: 'Dead is whether the sea pickles are not alive. Sea pickles are only considered alive when inside of water. While alive, sea pickles emit light & can be grown with bone meal.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (SeaPickle) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (SeaPickle) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (SeaPickle) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CompostChance">
```go
func (SeaPickle) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (SeaPickle) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (SeaPickle) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (SeaPickle) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (SeaPickle) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (SeaPickle) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (SeaPickle) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (SeaPickle) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (SeaPickle) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (SeaPickle) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (SeaPickle) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (SeaPickle) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (SeaPickle) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### ShortGrass


<Callout type="info">Struct</Callout>



```go
type ShortGrass struct {
	replaceable
	transparent
	empty

	Double	bool
}
```



ShortGrass is a transparent plant block which can be used to obtain seeds and as decoration.



#### Fields

<TypeTable
  type={{
    replaceable: {
      description: '',
      type: 'replaceable',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    empty: {
      description: '',
      type: 'empty',
    },
    Double: {
      description: '',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (ShortGrass) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal attempts to affect the block using a bone meal item.

</Accordion>

<Accordion title="BreakInfo">
```go
func (ShortGrass) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (ShortGrass) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (ShortGrass) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (ShortGrass) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (ShortGrass) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (ShortGrass) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (ShortGrass) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (ShortGrass) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (ShortGrass) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (ShortGrass) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (ShortGrass) ReplaceableBy(world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (ShortGrass) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Shroomlight


<Callout type="info">Struct</Callout>



```go
type Shroomlight struct {
	solid
}
```



Shroomlight are light-emitting blocks that generate in huge fungi.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Shroomlight) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (Shroomlight) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Shroomlight) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Shroomlight) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Shroomlight) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Shroomlight) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Shroomlight) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Sign


<Callout type="info">Struct</Callout>



```go
type Sign struct {
	transparent
	empty
	bass
	sourceWaterDisplacer

	// Wood is the type of wood of the sign. This field must have one of the values found in the material
	// package.
	Wood	WoodType
	// Attach is the attachment of the Sign. It is either of the type WallAttachment or StandingAttachment.
	Attach	Attachment
	// Waxed specifies if the Sign has been waxed by a player. If set to true, the Sign can no longer be edited by
	// anyone and must be destroyed if the text needs to be changed.
	Waxed	bool
	// Front is the text of the front side of the sign. Anyone can edit this unless the sign is Waxed.
	Front	SignText
	// Back is the text of the back side of the sign. Anyone can edit this unless the sign is Waxed.
	Back	SignText
}
```



Sign is a non-solid block that can display text on the front and back of the block.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    empty: {
      description: '',
      type: 'empty',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Wood: {
      description: 'Wood is the type of wood of the sign. This field must have one of the values found in the material package.',
      type: 'WoodType',
      typeLink: '../block.mdx#woodtype',
    },
    Attach: {
      description: 'Attach is the attachment of the Sign. It is either of the type WallAttachment or StandingAttachment.',
      type: 'Attachment',
      typeLink: '../block.mdx#attachment',
    },
    Waxed: {
      description: 'Waxed specifies if the Sign has been waxed by a player. If set to true, the Sign can no longer be edited by anyone and must be destroyed if the text needs to be changed.',
      type: 'bool',
    },
    Front: {
      description: 'Front is the text of the front side of the sign. Anyone can edit this unless the sign is Waxed.',
      type: 'SignText',
      typeLink: '../block.mdx#signtext',
    },
    Back: {
      description: 'Back is the text of the back side of the sign. Anyone can edit this unless the sign is Waxed.',
      type: 'SignText',
      typeLink: '../block.mdx#signtext',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Sign) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Sign) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Sign) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Sign) DecodeNBT(data map[string]any) any
```

DecodeNBT ...

</Accordion>

<Accordion title="Dye">
```go
func (Sign) Dye(pos cube.Pos, userPos mgl64.Vec3, c item.Colour) (world.Block, bool)
```

Dye dyes the Sign, changing its base colour to that of the colour passed.

</Accordion>

<Accordion title="EditingFrontSide">
```go
func (Sign) EditingFrontSide(pos cube.Pos, userPos mgl64.Vec3) bool
```

EditingFrontSide returns if the user is editing the front side of the sign based on their position relative to the
position and direction of the sign.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Sign) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Sign) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Sign) EncodeNBT() map[string]any
```

EncodeNBT ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Sign) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Sign) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Sign) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Ink">
```go
func (Sign) Ink(pos cube.Pos, userPos mgl64.Vec3, glowing bool) (world.Block, bool)
```

Ink inks the sign either glowing or non-glowing.

</Accordion>

<Accordion title="Instrument">
```go
func (Sign) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Sign) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="MaxCount">
```go
func (Sign) MaxCount() int
```

MaxCount ...

</Accordion>

<Accordion title="Model">
```go
func (Sign) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Sign) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Sign) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Sign) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

<Accordion title="Wax">
```go
func (Sign) Wax(cube.Pos, mgl64.Vec3) (world.Block, bool)
```

Wax waxes a sign to prevent it from further editing.

</Accordion>

</Accordions>


---

### SignEditor


<Callout type="info">Interface</Callout>



```go
type SignEditor interface {
	OpenSign(pos cube.Pos, frontSide bool)
}
```



SignEditor represents something that can edit a sign, typically players.






---

### SignText


<Callout type="info">Struct</Callout>



```go
type SignText struct {
	// Text is the text displayed on this side of the sign. The text is automatically wrapped if it does not fit on a line.
	Text	string
	// BaseColour is the base colour of the text on this side of the sign, changed when using a dye on the sign. The default
	// colour is black.
	BaseColour	color.RGBA
	// Glowing specifies if the Sign has glowing text on the current side. If set to true, the text will be visible even
	// in the dark, and it will have an outline to improve visibility.
	Glowing	bool
	// Owner holds the XUID of the player that most recently edited this side of the sign.
	Owner	string
}
```



SignText represents the data for a single side of a sign. The sign can be edited on the front and back side.



#### Fields

<TypeTable
  type={{
    Text: {
      description: 'Text is the text displayed on this side of the sign. The text is automatically wrapped if it does not fit on a line.',
      type: 'string',
    },
    BaseColour: {
      description: 'BaseColour is the base colour of the text on this side of the sign, changed when using a dye on the sign. The default colour is black.',
      type: 'color.RGBA',
    },
    Glowing: {
      description: 'Glowing specifies if the Sign has glowing text on the current side. If set to true, the text will be visible even in the dark, and it will have an outline to improve visibility.',
      type: 'bool',
    },
    Owner: {
      description: 'Owner holds the XUID of the player that most recently edited this side of the sign.',
      type: 'string',
    },
  }}
/>





---

### Skull


<Callout type="info">Struct</Callout>



```go
type Skull struct {
	transparent
	sourceWaterDisplacer

	// Type is the type of the skull.
	Type	SkullType

	// Attach is the attachment of the Skull. It is either of the type WallAttachment or StandingAttachment.
	//blockhash:facing_only
	Attach	Attachment
}
```



Skull is a decorative block. There are seven types of skulls: player, zombie, skeleton, wither skeleton, creeper,
dragon, and piglin.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Type: {
      description: 'Type is the type of the skull.',
      type: 'SkullType',
      typeLink: '../block.mdx#skulltype',
    },
    Attach: {
      description: 'Attach is the attachment of the Skull. It is either of the type WallAttachment or StandingAttachment.',
      type: 'Attachment',
      typeLink: '../block.mdx#attachment',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Skull) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Skull) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Skull) DecodeNBT(data map[string]interface{}) interface{}
```

DecodeNBT ...

</Accordion>

<Accordion title="DefencePoints">
```go
func (Skull) DefencePoints() float64
```

DefencePoints ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Skull) EncodeBlock() (string, map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Skull) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Skull) EncodeNBT() map[string]interface{}
```

EncodeNBT ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Skull) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Skull) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Helmet">
```go
func (Skull) Helmet() bool
```

Helmet ...

</Accordion>

<Accordion title="KnockBackResistance">
```go
func (Skull) KnockBackResistance() float64
```

KnockBackResistance ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Skull) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Skull) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Skull) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="Toughness">
```go
func (Skull) Toughness() float64
```

Toughness ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Skull) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### SkullType


<Callout type="info">Struct</Callout>



```go
type SkullType struct {
	skull
}
```



SkullType represents a mob variant of a skull.



#### Fields

<TypeTable
  type={{
    skull: {
      description: '',
      type: 'skull',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (SkullType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (SkullType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (SkullType) Uint8() uint8
```

Uint8 ...

</Accordion>

</Accordions>


---

### Slab


<Callout type="info">Struct</Callout>



```go
type Slab struct {
	// Block is the block to use for the type of slab.
	Block	world.Block
	// Top specifies if the slab is in the top part of the block.
	Top	bool
	// Double specifies if the slab is a double slab. These double slabs can be made by placing another slab
	// on an existing slab.
	Double	bool
}
```



Slab is a half block that allows entities to walk up blocks without jumping.



#### Fields

<TypeTable
  type={{
    Block: {
      description: 'Block is the block to use for the type of slab.',
      type: 'world.Block',
      typeLink: '../world.mdx#block',
    },
    Top: {
      description: 'Top specifies if the slab is in the top part of the block.',
      type: 'bool',
    },
    Double: {
      description: 'Double specifies if the slab is a double slab. These double slabs can be made by placing another slab on an existing slab.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Slab) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Slab) CanDisplace(b world.Liquid) bool
```

CanDisplace ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Slab) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Slab) EncodeItem() (string, int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Slab) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Slab) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Slab) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Slab) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Slab) LightDiffusionLevel() uint8
```

LightDiffusionLevel returns 0 if the slab is a half slab, or 15 if it is double.

</Accordion>

<Accordion title="Model">
```go
func (Slab) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Slab) SideClosed(pos, side cube.Pos, _ *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Slab) UseOnBlock(pos cube.Pos, face cube.Face, clickPos mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock handles the placement of slabs with relation to them being upside down or not and handles slabs
being turned into double slabs.

</Accordion>

</Accordions>


---

### SmithingTable


<Callout type="info">Struct</Callout>



```go
type SmithingTable struct {
	bass
	solid
}
```



SmithingTable is a toolsmith's job site block that generates in villages. It can be used to upgrade diamond gear into
netherite gear.



#### Fields

<TypeTable
  type={{
    bass: {
      description: '',
      type: 'bass',
    },
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (SmithingTable) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (SmithingTable) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (SmithingTable) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (SmithingTable) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (SmithingTable) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (SmithingTable) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (SmithingTable) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Smoker


<Callout type="info">Struct</Callout>



```go
type Smoker struct {
	solid
	bassDrum
	*smelter

	// Facing is the direction the smoker is facing.
	Facing	cube.Direction
	// Lit is true if the smoker is lit.
	Lit	bool
}
```



Smoker is a type of furnace that cooks food items, similar to a furnace, but twice as fast. It also serves as a
butcher's job site block.
The empty value of Smoker is not valid. It must be created using block.NewSmoker(cube.Face).



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    "*smelter": {
      description: '',
      type: '*smelter',
    },
    Facing: {
      description: 'Facing is the direction the smoker is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Lit: {
      description: 'Lit is true if the smoker is lit.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Smoker) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="AddViewer">
```go
func (Smoker) AddViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

AddViewer adds a viewer to the furnace, so that it is updated whenever the inventory of the furnace is changed.

</Accordion>

<Accordion title="BreakInfo">
```go
func (Smoker) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="DecodeNBT">
```go
func (Smoker) DecodeNBT(data map[string]interface{}) interface{}
```

DecodeNBT ...

</Accordion>

<Accordion title="Durations">
```go
func (Smoker) Durations() (remaining time.Duration, max time.Duration, cook time.Duration)
```

Durations returns the remaining, maximum, and cook durations of the smelter.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Smoker) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Smoker) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EncodeNBT">
```go
func (Smoker) EncodeNBT() map[string]interface{}
```

EncodeNBT ...

</Accordion>

<Accordion title="Experience">
```go
func (Smoker) Experience() int
```

Experience returns the collected experience of the smelter.

</Accordion>

<Accordion title="ExtractItem">
```go
func (Smoker) ExtractItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

ExtractItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Smoker) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="InsertItem">
```go
func (Smoker) InsertItem(h Hopper, pos cube.Pos, tx *world.Tx) bool
```

InsertItem ...

</Accordion>

<Accordion title="Instrument">
```go
func (Smoker) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Inventory">
```go
func (Smoker) Inventory(*world.Tx, cube.Pos) *inventory.Inventory
```

Inventory returns the inventory of the furnace.

</Accordion>

<Accordion title="Model">
```go
func (Smoker) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="RemoveViewer">
```go
func (Smoker) RemoveViewer(v ContainerViewer, _ *world.Tx, _ cube.Pos)
```

RemoveViewer removes a viewer from the furnace, so that slot updates in the inventory are no longer sent to
it.

</Accordion>

<Accordion title="ResetExperience">
```go
func (Smoker) ResetExperience() int
```

ResetExperience resets the collected experience of the smelter, and returns the amount of experience that was reset.

</Accordion>

<Accordion title="Tick">
```go
func (Smoker) Tick(_ int64, pos cube.Pos, tx *world.Tx)
```

Tick is called to check if the smoker should update and start or stop smelting.

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Smoker) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Snow


<Callout type="info">Struct</Callout>



```go
type Snow struct {
	solid
}
```



Snow is a full-sized block of snow.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Snow) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Snow) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Snow) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Snow) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Snow) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Soil


<Callout type="info">Interface</Callout>



```go
type Soil interface {
	// SoilFor returns whether the vegetation can exist on the block.
	SoilFor(world.Block) bool
}
```



Soil represents a block that can support vegetation.






---

### SoulSand


<Callout type="info">Struct</Callout>



```go
type SoulSand struct {
	solid
}
```



SoulSand is a block found naturally only in the Nether. SoulSand slows movement of mobs & players.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (SoulSand) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (SoulSand) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (SoulSand) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (SoulSand) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (SoulSand) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (SoulSand) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SoilFor">
```go
func (SoulSand) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

</Accordions>


---

### SoulSoil


<Callout type="info">Struct</Callout>



```go
type SoulSoil struct {
	solid
}
```



SoulSoil is a block naturally found only in the soul sand valley.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (SoulSoil) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (SoulSoil) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (SoulSoil) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (SoulSoil) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (SoulSoil) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SoilFor">
```go
func (SoulSoil) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

</Accordions>


---

### Sponge


<Callout type="info">Struct</Callout>



```go
type Sponge struct {
	solid

	// Wet specifies whether the dry or the wet variant of the block is used.
	Wet	bool
}
```



Sponge is a block that can be used to remove water around itself when placed, turning into a wet sponge in the
process.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Wet: {
      description: 'Wet specifies whether the dry or the wet variant of the block is used.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Sponge) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Sponge) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Sponge) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Sponge) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Model">
```go
func (Sponge) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Sponge) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick checks for nearby water flow. If water could be found and the sponge is dry, it will absorb the
water and be flagged as wet.

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Sponge) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Sponge) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock places the sponge, absorbs nearby water if it's still dry and flags it as wet if any water has been
absorbed.

</Accordion>

</Accordions>


---

### SporeBlossom


<Callout type="info">Struct</Callout>



```go
type SporeBlossom struct {
	empty
	transparent
}
```



SporeBlossom is a decorative block.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (SporeBlossom) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CompostChance">
```go
func (SporeBlossom) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (SporeBlossom) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (SporeBlossom) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (SporeBlossom) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (SporeBlossom) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (SporeBlossom) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (SporeBlossom) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (SporeBlossom) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (SporeBlossom) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (SporeBlossom) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### StainedGlass


<Callout type="info">Struct</Callout>



```go
type StainedGlass struct {
	transparent
	solid
	clicksAndSticks

	// Colour specifies the colour of the block.
	Colour	item.Colour
}
```



StainedGlass is a decorative, fully transparent solid block that is dyed into a different colour.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    solid: {
      description: '',
      type: 'solid',
    },
    clicksAndSticks: {
      description: '',
      type: 'clicksAndSticks',
    },
    Colour: {
      description: 'Colour specifies the colour of the block.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (StainedGlass) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (StainedGlass) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (StainedGlass) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (StainedGlass) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (StainedGlass) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (StainedGlass) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (StainedGlass) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### StainedGlassPane


<Callout type="info">Struct</Callout>



```go
type StainedGlassPane struct {
	transparent
	thin
	clicksAndSticks
	sourceWaterDisplacer

	// Colour specifies the colour of the block.
	Colour	item.Colour
}
```



StainedGlassPane is a transparent block that can be used as a more efficient alternative to glass blocks.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    thin: {
      description: '',
      type: 'thin',
    },
    clicksAndSticks: {
      description: '',
      type: 'clicksAndSticks',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Colour: {
      description: 'Colour specifies the colour of the block.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (StainedGlassPane) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (StainedGlassPane) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (StainedGlassPane) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (StainedGlassPane) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (StainedGlassPane) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (StainedGlassPane) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (StainedGlassPane) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (StainedGlassPane) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (StainedGlassPane) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

</Accordions>


---

### StainedTerracotta


<Callout type="info">Struct</Callout>



```go
type StainedTerracotta struct {
	solid
	bassDrum

	// Colour specifies the colour of the block.
	Colour	item.Colour
}
```



StainedTerracotta is a block formed from clay, with a hardness and blast resistance comparable to stone. In contrast
to Terracotta, t can be coloured in the same 16 colours that wool can be dyed, but more dulled and earthen.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Colour: {
      description: 'Colour specifies the colour of the block.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (StainedTerracotta) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (StainedTerracotta) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (StainedTerracotta) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (StainedTerracotta) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (StainedTerracotta) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (StainedTerracotta) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (StainedTerracotta) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="SoilFor">
```go
func (StainedTerracotta) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

</Accordions>


---

### Stairs


<Callout type="info">Struct</Callout>



```go
type Stairs struct {
	transparent
	sourceWaterDisplacer

	// Block is the block to use for the type of stair.
	Block	world.Block
	// UpsideDown specifies if the stairs are upside down. If set to true, the full side is at the top part
	// of the block.
	UpsideDown	bool
	// Facing is the direction that the full side of the stairs is facing.
	Facing	cube.Direction
}
```



Stairs are blocks that allow entities to walk up blocks without jumping.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Block: {
      description: 'Block is the block to use for the type of stair.',
      type: 'world.Block',
      typeLink: '../world.mdx#block',
    },
    UpsideDown: {
      description: 'UpsideDown specifies if the stairs are upside down. If set to true, the full side is at the top part of the block.',
      type: 'bool',
    },
    Facing: {
      description: 'Facing is the direction that the full side of the stairs is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Stairs) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Stairs) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Stairs) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Stairs) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Stairs) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Stairs) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Stairs) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Stairs) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Stairs) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Stairs) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Stairs) SideClosed(pos, side cube.Pos, tx *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Stairs) UseOnBlock(pos cube.Pos, face cube.Face, clickPos mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock handles the directional placing of stairs and makes sure they are properly placed upside down
when needed.

</Accordion>

</Accordions>


---

### StartCrackAction


<Callout type="info">Struct</Callout>



```go
type StartCrackAction struct {
	action
	BreakTime	time.Duration
}
```



StartCrackAction is a world.BlockAction to make the cracks in a block start forming, following the break time set in
the action.



#### Fields

<TypeTable
  type={{
    action: {
      description: '',
      type: 'action',
    },
    BreakTime: {
      description: '',
      type: 'time.Duration',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BlockAction">
```go
func (StartCrackAction) BlockAction()
```

BlockAction serves to implement the world.BlockAction interface.

</Accordion>

</Accordions>


---

### Stone


<Callout type="info">Struct</Callout>



```go
type Stone struct {
	solid
	bassDrum

	// Smooth specifies if the stone is its smooth variant.
	Smooth	bool
}
```



Stone is a block found underground in the world or on mountains.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Smooth: {
      description: 'Smooth specifies if the stone is its smooth variant.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Stone) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Stone) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Stone) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Stone) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Stone) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Stone) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Stone) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### StoneBricks


<Callout type="info">Struct</Callout>



```go
type StoneBricks struct {
	solid
	bassDrum

	// Type is the type of stone bricks of the block.
	Type	StoneBricksType
}
```



StoneBricks are materials found in structures such as strongholds, igloo basements, jungle temples, ocean ruins
and ruined portals.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Type: {
      description: 'Type is the type of stone bricks of the block.',
      type: 'StoneBricksType',
      typeLink: '../block.mdx#stonebrickstype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (StoneBricks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (StoneBricks) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (StoneBricks) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (StoneBricks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (StoneBricks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (StoneBricks) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (StoneBricks) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

</Accordions>


---

### StoneBricksType


<Callout type="info">Struct</Callout>



```go
type StoneBricksType struct {
	stoneBricks
}
```



StoneBricksType represents a type of stone bricks.



#### Fields

<TypeTable
  type={{
    stoneBricks: {
      description: '',
      type: 'stoneBricks',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Name">
```go
func (StoneBricksType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (StoneBricksType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (StoneBricksType) Uint8() uint8
```

Uint8 returns the stone bricks as a uint8.

</Accordion>

</Accordions>


---

### Stonecutter


<Callout type="info">Struct</Callout>



```go
type Stonecutter struct {
	bassDrum

	// Facing is the direction the stonecutter is facing.
	Facing	cube.Direction
}
```



Stonecutter is used to craft stone and copper related blocks in smaller and more precise quantities than crafting,
and is more efficient than crafting for certain recipes.



#### Fields

<TypeTable
  type={{
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Facing: {
      description: 'Facing is the direction the stonecutter is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (Stonecutter) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (Stonecutter) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Stonecutter) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Stonecutter) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Stonecutter) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Stonecutter) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Stonecutter) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Stonecutter) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### StopCrackAction


<Callout type="info">Struct</Callout>



```go
type StopCrackAction struct{ action }
```



StopCrackAction is a world.BlockAction to make the cracks forming in a block stop and disappear.



#### Fields

<TypeTable
  type={{
    action: {
      description: '',
      type: 'action',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BlockAction">
```go
func (StopCrackAction) BlockAction()
```

BlockAction serves to implement the world.BlockAction interface.

</Accordion>

</Accordions>


---

### SugarCane


<Callout type="info">Struct</Callout>



```go
type SugarCane struct {
	empty
	transparent

	// Age is the growth state of sugar cane. Values range from 0 to 15.
	Age	int
}
```



SugarCane is a plant block that generates naturally near water.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    Age: {
      description: 'Age is the growth state of sugar cane. Values range from 0 to 15.',
      type: 'int',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (SugarCane) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (SugarCane) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (SugarCane) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (SugarCane) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (SugarCane) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (SugarCane) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (SugarCane) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (SugarCane) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (SugarCane) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (SugarCane) RandomTick(pos cube.Pos, tx *world.Tx, _ *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (SugarCane) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ensures the placement of the block is OK.

</Accordion>

</Accordions>


---

### TNT


<Callout type="info">Struct</Callout>



```go
type TNT struct {
	solid
}
```



TNT is an explosive block that can be primed to generate an explosion.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (TNT) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, ctx *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (TNT) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (TNT) EncodeBlock() (name string, properties map[string]interface{})
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (TNT) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Explode">
```go
func (TNT) Explode(_ mgl64.Vec3, pos cube.Pos, tx *world.Tx, _ ExplosionConfig)
```

Explode ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (TNT) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (TNT) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Ignite">
```go
func (TNT) Ignite(pos cube.Pos, tx *world.Tx, _ world.Entity) bool
```

Ignite ...

</Accordion>

<Accordion title="Model">
```go
func (TNT) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="ProjectileHit">
```go
func (TNT) ProjectileHit(pos cube.Pos, tx *world.Tx, e world.Entity, _ cube.Face)
```

ProjectileHit ...

</Accordion>

</Accordions>


---

### Terracotta


<Callout type="info">Struct</Callout>



```go
type Terracotta struct {
	solid
	bassDrum
}
```



Terracotta is a block formed from clay, with a hardness and blast resistance comparable to stone. For colouring it,
take a look at StainedTerracotta.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Terracotta) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Terracotta) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Terracotta) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Terracotta) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Terracotta) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Terracotta) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SoilFor">
```go
func (Terracotta) SoilFor(block world.Block) bool
```

SoilFor ...

</Accordion>

</Accordions>


---

### Torch


<Callout type="info">Struct</Callout>



```go
type Torch struct {
	transparent
	empty

	// Facing is the direction from the torch to the block.
	Facing	cube.Face
	// Type is the type of fire lighting the torch.
	Type	FireType
}
```



Torch are non-solid blocks that emit light.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    empty: {
      description: '',
      type: 'empty',
    },
    Facing: {
      description: 'Facing is the direction from the torch to the block.',
      type: 'cube.Face',
      typeLink: '../block-cube.mdx#face',
    },
    Type: {
      description: 'Type is the type of fire lighting the torch.',
      type: 'FireType',
      typeLink: '../block.mdx#firetype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Torch) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Torch) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Torch) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Torch) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Torch) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Torch) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LightEmissionLevel">
```go
func (Torch) LightEmissionLevel() uint8
```

LightEmissionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Torch) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Torch) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Torch) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Tuff


<Callout type="info">Struct</Callout>



```go
type Tuff struct {
	solid
	bassDrum

	// Chiseled specifies if the tuff is chiseled.
	Chiseled	bool
}
```



Tuff is an ornamental rock formed from volcanic ash, occurring in underground blobs below Y=16.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Chiseled: {
      description: 'Chiseled specifies if the tuff is chiseled.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Tuff) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Tuff) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Tuff) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Tuff) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Tuff) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Tuff) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### TuffBricks


<Callout type="info">Struct</Callout>



```go
type TuffBricks struct {
	solid
	bassDrum

	// Chiseled specifies if the tuff bricks are chiseled.
	Chiseled	bool
}
```



TuffBricks are a decorational variant of Tuff that can be crafted or found naturally in Trial Chambers.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bassDrum: {
      description: '',
      type: 'bassDrum',
    },
    Chiseled: {
      description: 'Chiseled specifies if the tuff bricks are chiseled.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (TuffBricks) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (TuffBricks) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (TuffBricks) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (TuffBricks) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (TuffBricks) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (TuffBricks) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### Vines


<Callout type="info">Struct</Callout>



```go
type Vines struct {
	replaceable
	transparent
	empty
	sourceWaterDisplacer

	// NorthDirection is true if the vines are attached towards north.
	NorthDirection	bool
	// EastDirection is true if the vines are attached towards east.
	EastDirection	bool
	// SouthDirection is true if the vines are attached towards south.
	SouthDirection	bool
	// WestDirection is true if the vines are attached towards west.
	WestDirection	bool
}
```



Vines are climbable non-solid vegetation blocks that grow on walls.



#### Fields

<TypeTable
  type={{
    replaceable: {
      description: '',
      type: 'replaceable',
    },
    transparent: {
      description: '',
      type: 'transparent',
    },
    empty: {
      description: '',
      type: 'empty',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    NorthDirection: {
      description: 'NorthDirection is true if the vines are attached towards north.',
      type: 'bool',
    },
    EastDirection: {
      description: 'EastDirection is true if the vines are attached towards east.',
      type: 'bool',
    },
    SouthDirection: {
      description: 'SouthDirection is true if the vines are attached towards south.',
      type: 'bool',
    },
    WestDirection: {
      description: 'WestDirection is true if the vines are attached towards west.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Attachment">
```go
func (Vines) Attachment(direction cube.Direction) bool
```

Attachment returns the attachment of the vines at the given direction.

</Accordion>

<Accordion title="Attachments">
```go
func (Vines) Attachments() (attachments []cube.Direction)
```

Attachments returns all attachments of the vines.

</Accordion>

<Accordion title="BreakInfo">
```go
func (Vines) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Vines) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="CompostChance">
```go
func (Vines) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Vines) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Vines) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="EntityInside">
```go
func (Vines) EntityInside(_ cube.Pos, _ *world.Tx, e world.Entity)
```

EntityInside ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Vines) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Vines) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Vines) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Vines) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Vines) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Vines) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (Vines) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (Vines) ReplaceableBy(world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Vines) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Vines) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

<Accordion title="WithAttachment">
```go
func (Vines) WithAttachment(direction cube.Direction, attached bool) Vines
```

WithAttachment returns a Vines block with an attachment on the given cube.Direction.

</Accordion>

</Accordions>


---

### Wall


<Callout type="info">Struct</Callout>



```go
type Wall struct {
	transparent
	sourceWaterDisplacer

	// Block is the block to use for the type of wall.
	Block	world.Block
	// NorthConnection is the type of connection in the north direction of the post.
	NorthConnection	WallConnectionType
	// EastConnection is the type of connection in the east direction of the post.
	EastConnection	WallConnectionType
	// SouthConnection is the type of connection in the south direction of the post.
	SouthConnection	WallConnectionType
	// WestConnection is the type of connection in the west direction of the post.
	WestConnection	WallConnectionType
	// Post is if the wall is extended to the full height of a block or not.
	Post	bool
}
```



Wall is a block similar to fences that prevents players from jumping over and is thinner than the usual block. It is
available for many blocks and all types connect together as if they were the same type.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Block: {
      description: 'Block is the block to use for the type of wall.',
      type: 'world.Block',
      typeLink: '../world.mdx#block',
    },
    NorthConnection: {
      description: 'NorthConnection is the type of connection in the north direction of the post.',
      type: 'WallConnectionType',
      typeLink: '../block.mdx#wallconnectiontype',
    },
    EastConnection: {
      description: 'EastConnection is the type of connection in the east direction of the post.',
      type: 'WallConnectionType',
      typeLink: '../block.mdx#wallconnectiontype',
    },
    SouthConnection: {
      description: 'SouthConnection is the type of connection in the south direction of the post.',
      type: 'WallConnectionType',
      typeLink: '../block.mdx#wallconnectiontype',
    },
    WestConnection: {
      description: 'WestConnection is the type of connection in the west direction of the post.',
      type: 'WallConnectionType',
      typeLink: '../block.mdx#wallconnectiontype',
    },
    Post: {
      description: 'Post is if the wall is extended to the full height of a block or not.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Wall) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (Wall) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="ConnectionType">
```go
func (Wall) ConnectionType(direction cube.Direction) WallConnectionType
```

ConnectionType returns the connection type of the wall in the given direction.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Wall) EncodeBlock() (string, map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Wall) EncodeItem() (string, int16)
```

EncodeItem ...

</Accordion>

<Accordion title="Hash">
```go
func (Wall) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Wall) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (Wall) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Wall) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (Wall) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Wall) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

<Accordion title="WithConnectionType">
```go
func (Wall) WithConnectionType(direction cube.Direction, connection WallConnectionType) Wall
```

WithConnectionType returns the wall with the given connection type in the given direction.

</Accordion>

</Accordions>


---

### WallConnectionType


<Callout type="info">Struct</Callout>



```go
type WallConnectionType struct {
	wallConnectionType
}
```



WallConnectionType represents the connection type of a wall.



#### Fields

<TypeTable
  type={{
    wallConnectionType: {
      description: '',
      type: 'wallConnectionType',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Height">
```go
func (WallConnectionType) Height() float64
```

Height returns the height of the connection for the block model.

</Accordion>

<Accordion title="String">
```go
func (WallConnectionType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (WallConnectionType) Uint8() uint8
```

Uint8 returns the wall connection as a uint8.

</Accordion>

</Accordions>


---

### Water


<Callout type="info">Struct</Callout>



```go
type Water struct {
	empty

	// Still makes the water appear as if it is not flowing.
	Still	bool
	// Depth is the depth of the water. This is a number from 1-8, where 8 is a source block and 1 is the
	// smallest possible water block.
	Depth	int
	// Falling specifies if the water is falling. Falling water will always appear as a source block, but its
	// behaviour differs when it starts spreading.
	Falling	bool
}
```



Water is a natural fluid that generates abundantly in the world.



#### Fields

<TypeTable
  type={{
    empty: {
      description: '',
      type: 'empty',
    },
    Still: {
      description: 'Still makes the water appear as if it is not flowing.',
      type: 'bool',
    },
    Depth: {
      description: 'Depth is the depth of the water. This is a number from 1-8, where 8 is a source block and 1 is the smallest possible water block.',
      type: 'int',
    },
    Falling: {
      description: 'Falling specifies if the water is falling. Falling water will always appear as a source block, but its behaviour differs when it starts spreading.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BlastResistance">
```go
func (Water) BlastResistance() float64
```

BlastResistance always returns 500.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Water) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EntityInside">
```go
func (Water) EntityInside(_ cube.Pos, _ *world.Tx, e world.Entity)
```

EntityInside ...

</Accordion>

<Accordion title="FillBottle">
```go
func (Water) FillBottle() (world.Block, item.Stack, bool)
```

FillBottle ...

</Accordion>

<Accordion title="Harden">
```go
func (Water) Harden(pos cube.Pos, tx *world.Tx, flownIntoBy *cube.Pos) bool
```

Harden hardens the water if lava flows into it.

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (Water) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (Water) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (Water) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="LiquidDepth">
```go
func (Water) LiquidDepth() int
```

LiquidDepth returns the depth of the water.

</Accordion>

<Accordion title="LiquidFalling">
```go
func (Water) LiquidFalling() bool
```

LiquidFalling returns Water.Falling.

</Accordion>

<Accordion title="LiquidType">
```go
func (Water) LiquidType() string
```

LiquidType ...

</Accordion>

<Accordion title="Model">
```go
func (Water) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (Water) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="ReplaceableBy">
```go
func (Water) ReplaceableBy(b world.Block) bool
```

ReplaceableBy ...

</Accordion>

<Accordion title="ScheduledTick">
```go
func (Water) ScheduledTick(pos cube.Pos, tx *world.Tx, _ *rand.Rand)
```

ScheduledTick ...

</Accordion>

<Accordion title="SpreadDecay">
```go
func (Water) SpreadDecay() int
```

SpreadDecay returns 1 - The amount of levels decreased upon spreading.

</Accordion>

<Accordion title="WithDepth">
```go
func (Water) WithDepth(depth int, falling bool) world.Liquid
```

WithDepth returns the water with the depth passed.

</Accordion>

</Accordions>


---

### WheatSeeds


<Callout type="info">Struct</Callout>



```go
type WheatSeeds struct {
	crop
}
```



WheatSeeds are a crop that can be harvested to craft bread, cake, & cookies.



#### Fields

<TypeTable
  type={{
    crop: {
      description: '',
      type: 'crop',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BoneMeal">
```go
func (WheatSeeds) BoneMeal(pos cube.Pos, tx *world.Tx) bool
```

BoneMeal ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (WheatSeeds) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CalculateGrowthChance">
```go
func (WheatSeeds) CalculateGrowthChance(pos cube.Pos, tx *world.Tx) float64
```

CalculateGrowthChance calculates the chance the crop will grow during a random tick.

</Accordion>

<Accordion title="CompostChance">
```go
func (WheatSeeds) CompostChance() float64
```

CompostChance ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (WheatSeeds) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (WheatSeeds) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="GrowthStage">
```go
func (WheatSeeds) GrowthStage() int
```

GrowthStage returns the current stage of growth.

</Accordion>

<Accordion title="HasLiquidDrops">
```go
func (WheatSeeds) HasLiquidDrops() bool
```

HasLiquidDrops ...

</Accordion>

<Accordion title="Hash">
```go
func (WheatSeeds) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (WheatSeeds) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (WheatSeeds) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (WheatSeeds) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="RandomTick">
```go
func (WheatSeeds) RandomTick(pos cube.Pos, tx *world.Tx, r *rand.Rand)
```

RandomTick ...

</Accordion>

<Accordion title="SameCrop">
```go
func (WheatSeeds) SameCrop(c Crop) bool
```

SameCrop ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (WheatSeeds) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### Wood


<Callout type="info">Struct</Callout>



```go
type Wood struct {
	solid
	bass

	// Wood is the type of wood.
	Wood	WoodType
	// Stripped specifies if the wood is stripped or not.
	Stripped	bool
	// Axis is the axis which the wood block faces.
	Axis	cube.Axis
}
```



Wood is a block that has the log's "bark" texture on all six sides. It comes in 8 types: oak, spruce, birch, jungle,
acacia, dark oak, crimson, and warped.
Stripped wood is a variant obtained by using an axe on the wood.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    Wood: {
      description: 'Wood is the type of wood.',
      type: 'WoodType',
      typeLink: '../block.mdx#woodtype',
    },
    Stripped: {
      description: 'Stripped specifies if the wood is stripped or not.',
      type: 'bool',
    },
    Axis: {
      description: 'Axis is the axis which the wood block faces.',
      type: 'cube.Axis',
      typeLink: '../block-cube.mdx#axis',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Wood) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Wood) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Wood) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Wood) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (Wood) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Wood) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Wood) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Wood) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SmeltInfo">
```go
func (Wood) SmeltInfo() item.SmeltInfo
```

SmeltInfo ...

</Accordion>

<Accordion title="Strip">
```go
func (Wood) Strip() (world.Block, world.Sound, bool)
```

Strip ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (Wood) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) (used bool)
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### WoodDoor


<Callout type="info">Struct</Callout>



```go
type WoodDoor struct {
	transparent
	bass
	sourceWaterDisplacer

	// Wood is the type of wood of the door. This field must have one of the values found in the material
	// package.
	Wood	WoodType
	// Facing is the direction that the door opens towards. When closed, the door sits on the side of its
	// block on the opposite direction.
	Facing	cube.Direction
	// Open is whether the door is open.
	Open	bool
	// Top is whether the block is the top or bottom half of a door.
	Top	bool
	// Right is whether the door hinge is on the right side.
	Right	bool
}
```



WoodDoor is a block that can be used as an openable 1x2 barrier.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Wood: {
      description: 'Wood is the type of wood of the door. This field must have one of the values found in the material package.',
      type: 'WoodType',
      typeLink: '../block.mdx#woodtype',
    },
    Facing: {
      description: 'Facing is the direction that the door opens towards. When closed, the door sits on the side of its block on the opposite direction.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Open: {
      description: 'Open is whether the door is open.',
      type: 'bool',
    },
    Top: {
      description: 'Top is whether the block is the top or bottom half of a door.',
      type: 'bool',
    },
    Right: {
      description: 'Right is whether the door hinge is on the right side.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (WoodDoor) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, _ item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (WoodDoor) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (WoodDoor) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (WoodDoor) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (WoodDoor) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (WoodDoor) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (WoodDoor) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (WoodDoor) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (WoodDoor) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (WoodDoor) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (WoodDoor) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (WoodDoor) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (WoodDoor) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (WoodDoor) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock handles the directional placing of doors

</Accordion>

</Accordions>


---

### WoodFence


<Callout type="info">Struct</Callout>



```go
type WoodFence struct {
	transparent
	bass
	sourceWaterDisplacer

	// Wood is the type of wood of the fence. This field must have one of the values found in the wood
	// package.
	Wood	WoodType
}
```



WoodFence are blocks similar to Walls, which cannot normally be jumped over. Unlike walls however,
they allow the player (but not mobs) to see through them, making for excellent barriers.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Wood: {
      description: 'Wood is the type of wood of the fence. This field must have one of the values found in the wood package.',
      type: 'WoodType',
      typeLink: '../block.mdx#woodtype',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (WoodFence) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (WoodFence) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (WoodFence) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (WoodFence) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (WoodFence) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (WoodFence) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (WoodFence) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (WoodFence) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (WoodFence) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (WoodFence) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (WoodFence) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

</Accordions>


---

### WoodFenceGate


<Callout type="info">Struct</Callout>



```go
type WoodFenceGate struct {
	transparent
	bass
	sourceWaterDisplacer

	// Wood is the type of wood of the fence gate. This field must have one of the values found in the material
	// package.
	Wood	WoodType
	// Facing is the direction the fence gate swings open.
	Facing	cube.Direction
	// Open is whether the fence gate is open.
	Open	bool
	// Lowered lowers the fence gate by 3 pixels and is set when placed next to wall blocks.
	Lowered	bool
}
```



WoodFenceGate is a block that can be used as an openable 1x1 barrier.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Wood: {
      description: 'Wood is the type of wood of the fence gate. This field must have one of the values found in the material package.',
      type: 'WoodType',
      typeLink: '../block.mdx#woodtype',
    },
    Facing: {
      description: 'Facing is the direction the fence gate swings open.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Open: {
      description: 'Open is whether the fence gate is open.',
      type: 'bool',
    },
    Lowered: {
      description: 'Lowered lowers the fence gate by 3 pixels and is set when placed next to wall blocks.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (WoodFenceGate) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, u item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (WoodFenceGate) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (WoodFenceGate) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (WoodFenceGate) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (WoodFenceGate) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (WoodFenceGate) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (WoodFenceGate) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (WoodFenceGate) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (WoodFenceGate) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (WoodFenceGate) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (WoodFenceGate) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="NeighbourUpdateTick">
```go
func (WoodFenceGate) NeighbourUpdateTick(pos, _ cube.Pos, tx *world.Tx)
```

NeighbourUpdateTick ...

</Accordion>

<Accordion title="SideClosed">
```go
func (WoodFenceGate) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (WoodFenceGate) UseOnBlock(pos cube.Pos, face cube.Face, _ mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock ...

</Accordion>

</Accordions>


---

### WoodTrapdoor


<Callout type="info">Struct</Callout>



```go
type WoodTrapdoor struct {
	transparent
	bass
	sourceWaterDisplacer

	// Wood is the type of wood of the trapdoor. This field must have one of the values found in the material
	// package.
	Wood	WoodType
	// Facing is the direction the trapdoor is facing.
	Facing	cube.Direction
	// Open is whether the trapdoor is open.
	Open	bool
	// Top is whether the trapdoor occupies the top or bottom part of a block.
	Top	bool
}
```



WoodTrapdoor is a block that can be used as an openable 1x1 barrier.



#### Fields

<TypeTable
  type={{
    transparent: {
      description: '',
      type: 'transparent',
    },
    bass: {
      description: '',
      type: 'bass',
    },
    sourceWaterDisplacer: {
      description: '',
      type: 'sourceWaterDisplacer',
    },
    Wood: {
      description: 'Wood is the type of wood of the trapdoor. This field must have one of the values found in the material package.',
      type: 'WoodType',
      typeLink: '../block.mdx#woodtype',
    },
    Facing: {
      description: 'Facing is the direction the trapdoor is facing.',
      type: 'cube.Direction',
      typeLink: '../block-cube.mdx#direction',
    },
    Open: {
      description: 'Open is whether the trapdoor is open.',
      type: 'bool',
    },
    Top: {
      description: 'Top is whether the trapdoor occupies the top or bottom part of a block.',
      type: 'bool',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Activate">
```go
func (WoodTrapdoor) Activate(pos cube.Pos, _ cube.Face, tx *world.Tx, _ item.User, _ *item.UseContext) bool
```

Activate ...

</Accordion>

<Accordion title="BreakInfo">
```go
func (WoodTrapdoor) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="CanDisplace">
```go
func (WoodTrapdoor) CanDisplace(b world.Liquid) bool
```

CanDisplace returns true if the world.Liquid passed is of the type Water, not falling and has a depth of 8.

</Accordion>

<Accordion title="EncodeBlock">
```go
func (WoodTrapdoor) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (WoodTrapdoor) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (WoodTrapdoor) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="FuelInfo">
```go
func (WoodTrapdoor) FuelInfo() item.FuelInfo
```

FuelInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (WoodTrapdoor) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (WoodTrapdoor) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="LightDiffusionLevel">
```go
func (WoodTrapdoor) LightDiffusionLevel() uint8
```

LightDiffusionLevel ...

</Accordion>

<Accordion title="Model">
```go
func (WoodTrapdoor) Model() world.BlockModel
```

Model ...

</Accordion>

<Accordion title="SideClosed">
```go
func (WoodTrapdoor) SideClosed(cube.Pos, cube.Pos, *world.Tx) bool
```

SideClosed ...

</Accordion>

<Accordion title="UseOnBlock">
```go
func (WoodTrapdoor) UseOnBlock(pos cube.Pos, face cube.Face, clickPos mgl64.Vec3, tx *world.Tx, user item.User, ctx *item.UseContext) bool
```

UseOnBlock handles the directional placing of trapdoors and makes sure they are properly placed upside down
when needed.

</Accordion>

</Accordions>


---

### WoodType


<Callout type="info">Struct</Callout>



```go
type WoodType struct {
	wood
}
```



WoodType represents a type of wood of a block. Some blocks, such as log blocks, bark blocks, wooden planks and
others carry one of these types.



#### Fields

<TypeTable
  type={{
    wood: {
      description: '',
      type: 'wood',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="Flammable">
```go
func (WoodType) Flammable() bool
```

Flammable returns whether the wood type is flammable.

</Accordion>

<Accordion title="Name">
```go
func (WoodType) Name() string
```

Name ...

</Accordion>

<Accordion title="String">
```go
func (WoodType) String() string
```

String ...

</Accordion>

<Accordion title="Uint8">
```go
func (WoodType) Uint8() uint8
```

Uint8 returns the wood as a uint8.

</Accordion>

</Accordions>


---

### Wool


<Callout type="info">Struct</Callout>



```go
type Wool struct {
	solid

	// Colour is the colour of the wool.
	Colour	item.Colour
}
```



Wool is a colourful block that can be obtained by killing/shearing sheep, or crafted using four string.



#### Fields

<TypeTable
  type={{
    solid: {
      description: '',
      type: 'solid',
    },
    Colour: {
      description: 'Colour is the colour of the wool.',
      type: 'item.Colour',
      typeLink: '../item.mdx#colour',
    },
  }}
/>




#### Methods

<Accordions>

<Accordion title="BreakInfo">
```go
func (Wool) BreakInfo() BreakInfo
```

BreakInfo ...

</Accordion>

<Accordion title="EncodeBlock">
```go
func (Wool) EncodeBlock() (name string, properties map[string]any)
```

EncodeBlock ...

</Accordion>

<Accordion title="EncodeItem">
```go
func (Wool) EncodeItem() (name string, meta int16)
```

EncodeItem ...

</Accordion>

<Accordion title="FlammabilityInfo">
```go
func (Wool) FlammabilityInfo() FlammabilityInfo
```

FlammabilityInfo ...

</Accordion>

<Accordion title="Hash">
```go
func (Wool) Hash() (uint64, uint64)
```

</Accordion>

<Accordion title="Instrument">
```go
func (Wool) Instrument() sound.Instrument
```

Instrument ...

</Accordion>

<Accordion title="Model">
```go
func (Wool) Model() world.BlockModel
```

Model ...

</Accordion>

</Accordions>


---

### XPDropRange




```go
type XPDropRange [2]int
```



XPDropRange holds the min & max XP drop amounts of blocks.





#### Methods

<Accordions>

<Accordion title="RandomValue">
```go
func (XPDropRange) RandomValue() int
```

RandomValue returns a random XP value that falls within the drop range.

</Accordion>

</Accordions>


---


